# Concept Jury Verdict — 2026-08-22

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/hand/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` succeeded cleanly (fast-forwarded `9d634f5..f1f3d5d`, cycle 1191 `15760-conduct` at HEAD; no forced-update divergence this fire). Survey ran on the authoritative tree. Read-only on the lab per scope fence._

## Summary
You did the thing. For four windows running I said "beat pulse with a subsystem the lab has never run, or retire the number" — and cycle 1191 shipped `15760-conduct`: your two hands read by MediaPipe as a baton, driving the actual **time-base** (`playbackRate`) of a real take, over a ~20k-grain cloud running on a **WebGPU compute shader your hands physically push**. That is the first genuine 4/5 in ~12 cycles, and it cashed provocations #2, #3, and #4 from yesterday **in one ship.** Full credit — this is the ceiling break. The only caution worth raising: it's the **second camera piece in two cycles** (`leanin` 1190, `conduct` 1191), the output axis has quietly gone **Canvas2D-heavy (6×)**, and conduct's research anchor was honestly ~4 months old, not the same-week hook `recur` set the bar with.

## Diversity audit
_(last 15, numerically highest on disk: waveroom, pulse, schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin, conduct — sympathy & slimenerve aged out; leanin & conduct entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **~5×** (schlieren, transcript, bloomwork, recur, spheres), **down from ~7×** last window and now clearly trending out: the newest six ships (pulse rPPG, vigil sustain, antiphon navigate, keepsake grab, leanin lean, conduct hands) all put the human on the load path. The standing "passive-primary" ban stays, but note the reversal honestly. **The real news is the input hole is closing:** body/hand/pose tracking went **0× → 2×** (leanin face-proximity, conduct hands) — provocation #2 delivered. Still genuinely **0×: real-time MIDI/OSC out, and WebRTC multi-user** (sympathy aged out, so multi-user is back to 0×). **BANNED next cycle: passive/self-propelling as the PRIMARY relationship AND (new) a THIRD consecutive camera-as-primary-sensor piece** — two in a row is a swing; three is a new monoculture.
- **Over-represented output:** `Canvas2D flat single-surface` — **~6×** and the runaway window leader (transcript, toll, recur, negative, keepsake, leanin — leanin pushed it to 6). `three.js immersive room` **4×** (bloomwork, spheres, vigil-chord, antiphon), standing ban. `raw-WebGL2 field` **3×** (schlieren, coherence, waveroom), cooling but standing. Genuinely rare: **WebGPU 1× (conduct — slimenerve aged out, so it did NOT accumulate), inline-SVG/DOM 1× (pulse).** **BANNED next cycle: Canvas2D flat-single-surface output AND (standing) three.js walk/inhabit-a-room AND (standing) raw-WebGL2 field.**
- **Over-represented technique:** `conceptual/critical subtraction & consume-once` — **4×** (toll, negative, vigil-chord, keepsake) — still over-represented in the *window*, but the lab **correctly stopped shipping new ones** (provocation #1 obeyed: last two cycles were embodiment, not subtraction). These will age out on their own; keep the ban until they do. `physics/agent simulation` has **fallen to 2×** (waveroom FDTD, schlieren wave-field — sympathy Kuramoto & slimenerve physarum aged out), so **the standing physics-sim ban can LIFT.** The corpus-in-relationship lane (≥5 real takes conversing: antiphon, spheres, transcript) stays alive and welcome. **BANNED next cycle: subtraction/consume/negative-space technique.**
- **Over-represented vibe:** `achromatic / grayscale on near-black` — **~4×** (waveroom, schlieren, coherence, toll), **down from ~6×** (slimenerve aged out), still standing-banned but cooling. `conceptual/critical` as a *mood* — **~4×** (toll, negative, vigil, keepsake). The honest new register: **warm chromatic** (leanin's ember duotone, conduct's oxblood→ember→gold→violet) is genuinely fresh and reads as intentional — credit it. **BANNED next cycle: achromatic/grayscale AND conceptual/critical-subtraction mood.**
- **BANNED for next cycle:** `passive/self-propelling primary input` + `a third-straight camera-primary piece` + `Canvas2D flat-single-surface output` + (standing) `three.js inhabit-a-room` + (standing) `raw-WebGL2 field` + `subtraction/consume/negative-space technique + mood` + `achromatic/grayscale palette`. **LIFTED:** `recording-drives-a-physics/agent-sim` (fallen to 2×). **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Seventh straight fortnight with an empty floor. Floor height remains solved; stop optimizing it.
- **Hit 2–3 criteria — 13.** Still the bulk (waveroom, schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin). `leanin` (2/5) is the weakest of the newest cohort — Canvas2D output, face-detection copied wholesale from `1103-strange-face` (not a technique first), a single camera signal driving a single envelope over one take. `recur` remains the sharpest of this band on the honest axis (same-week dated research → a real self-composing engine).
- **Hit 4–5 criteria — 2 (pulse, conduct).** **First movement on this line in FOUR windows.** `conduct` clears **#1 (WebGPU-compute hands-pushed granular re-synthesis — a subsystem the lab has never run), #2 (hands + WebGPU compute + Web Audio granular/delay + camera = clearly ≥3 subsystems), #3 (named arXiv:2604.27957 conducting reference), #5 (same-cycle research hook)**, and is explicitly framed as a multi-cycle commitment (#4). Whether you score it 4/5 or 5/5, it is unambiguously above the plate — and, crucially, it clears via a *genuine first*, not the by-construction `#2+#3+#5` combo every orchestrated cycle satisfies for free.

Distribution **0 / 13 / 2** — the first break from `0/14/1` since I started tracking it. The metric finally moved because the lab finally shipped a never-run subsystem instead of a sharper recombination of a slicer + a canvas. That was the whole ask. Keep the bar exactly there: from here, a 4/5 still requires a subsystem the lab has never run (MIDI/OSC out, WebRTC multi-user, or the next WebGPU-compute engine) — a clever concept on Canvas2D is a 2/5 no matter how sharp.

## Standouts (positive)
- **15760-conduct** — the ceiling break, and it earns the headline. You don't press play, you conduct: conducting hand HEIGHT drives the actual `playbackRate`/time-base of his take (the paper's insight that conducting is control of *time*, not loudness), two-hand DISTANCE drives dynamics + spatial wet, hand OPENNESS drives the lowpass. The visual is his own waveform as ~20k grains on a WebGPU compute shader that your hands *push and sweep* — the "compute engine you push, not a field you watch" I asked for in #4, verbatim. Degrades cleanly (Canvas2D twin with no WebGPU; single-pointer sensor with no camera). **This is the lane to DEEPEN, not abandon** — see provocation #1.
- **15152-pulse** — no longer the *lone* 4/5, and that alone vindicates it. Still the only piece that reads the visitor's own cardiac signal, and still the cleanest inline-SVG/DOM output in the window. It held the ceiling honestly for four windows until conduct arrived to keep it company.
- **15392-recur** — still the model of the mandate on the honest axis: a genuinely fresh dated finding (Hawley, arXiv:2608.04378, 2026-08-05) → a self-composing engine that through-composes a new movement from real slices with recurrence arcs back to first hearings. `today's research → today's build` literal and same-week. Conduct beat it on subsystem-novelty; recur still beats conduct on research freshness.
- **15440-spheres** — the cleanest "real external data as the *carrier*, not a sim of it": true planetary ephemeris layering his takes by live aspect geometry. Different every open, boundless-cosmic without faking the world.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15696-leanin** (2/5) — the weak sibling of the two new embodied pieces. The *concept* is lovely (his take only fully present when you physically lean in), but the *execution* lands on the over-represented axes: Canvas2D flat output (now 6×), face-detection copied from `1103-strange-face` rather than a technique-first, and a single proximity scalar `p` driving a single envelope over one take. Next to conduct it reads as the safe, Canvas2D draft of "camera-as-instrument." A local minimum by output+technique, not by concept.
- **15104-waveroom** (2/5) — unchanged across four verdicts: a physics grid you walk through, FDTD precedented, raw-WebGL2 + pointer, and the origin of the inhabit-a-room template. The clearest local minimum of the window; it will age out soon and should not be re-templated.
- **15344-toll** (2/5) — where the (now-paused) subtraction lane began repeating itself. toll (spend-to-hear-once) and keepsake (keep-only-what-you-grab) are two framings of one consume-once verb; keepsake is the sharper. toll reads as the earlier draft of an idea you later nailed.
- **15312-transcript** (2/5) — self-describes as "a deep extension of `14656 · answerback`"; incremental harmonic scoring, no external named reference, no recent-research hook. Local-minimum-by-extension.

## Provocations for tomorrow's dream cycle
1. **DEEPEN conduct — do NOT run away from your first 4/5 in a month.** For four windows I begged for a piece worth extending; you just built one. Its README already lists the real next moves: **two independent voices** (left hand a slowed low-register pad, right hand the melody's time-base, playing against each other), **recorded conducting playback** (capture a pass as a gesture-automation curve, replay or layer two conductors), **beat-synced grain emission** (onset-detect his take, burst fresh grains on the beat). Any one of these is a legitimate DEEP cycle-2 and exactly the multi-cycle commitment the mandate wants. Extending a 4/5 into a 5/5 beats minting a fresh 2/5.
2. **If you go WIDE instead, do NOT ship a third-straight camera-primary piece.** Two in two cycles (leanin, conduct) is a healthy swing toward the body; three is the next monoculture. The still-**0×** inputs are the frontier now: **real-time MIDI/OSC out** (a controller or keyboard performing *over* his take, or the piece emitting MIDI a hardware synth answers) and **WebRTC multi-user** (two people conducting/listening to one take together — sympathy aged out, so multi-user is back to zero). Either would be a genuine subsystem-first and a legitimate shot at a 4/5.
3. **Get off Canvas2D — it's now 6× and the runaway output leader.** You already own a working WebGPU-compute grain engine (conduct). Reuse it, don't re-invent the next piece on a flat canvas. WebGPU sits at 1× *precisely because* slimenerve aged out — the engine you just built is the thing to build *on*, so the technique compounds instead of resetting.
4. **Restore same-week research freshness — recur's bar, not conduct's caveat.** Conduct was honest that its anchor (arXiv:2604.27957) is ~4 months old; recur remains the window's best because its dated finding was <30 days and the research→build chain was literal. RESEARCH.md's newest entry is today (2026-08-22), so the freshness is available — make tomorrow's build *name a <30-day dated finding as its hook* in STATE.md, the way recur did.
5. **The subtraction/consume lane is correctly paused — keep it paused until it ages out.** toll · negative · vigil · keepsake are still 4× in the window but you've shipped none of them in the last two cycles. Good. Don't reopen it; let it clear.

## Karel-facing line
You finally broke the ceiling: `conduct` is the first 4/5 in a month — your hands drive the actual time-base of your own recording over a WebGPU grain-cloud you physically push — so tomorrow, **deepen it, don't abandon it**, and get off Canvas2D while you're at it.
