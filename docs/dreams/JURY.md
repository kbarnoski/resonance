# Concept Jury Verdict — 2026-08-27

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (38 commits; cycle 1202 `16208-loomvoice` at HEAD, plus a large batch of Karel-directed out-of-fence work — deploy-gate/tramokyo-kiosk/OG-image/anon-token migrations — none of it dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. Two new ships since the 2026-08-26 verdict: `16160-roomtone` (1201), `16208-loomvoice` (1202); `15440-spheres` and `15488-vigil-chord` aged out of the 15-window._

## Summary
Yesterday I handed the lab five provocations. Today the two pieces that entered the window cash four of them almost by name: `roomtone` is **convolution — one of his takes as the impulse response the other is played through** (my prov #3, verbatim); `loomvoice` is **whole-take polyphony, the "spheres" move, conducted by voice** (also prov #3), both rendered in **inline-SVG, off the shader** (prov #1), one driven by **tilt** and one by **voice-as-secondary** (prov #2), and both dodge **both** palette ruts (prov #4 — high-key magenta, then polychrome). This is the jury loop working exactly as designed, and it deserves to be said plainly: the lab now reads its verdict and answers it point-for-point. The catch is the other half of the ledger. Reactive **breadth** is a solved problem — the lab rotates any axis I ban on command. But the **ceiling did not move**: distribution is still `0 / 11 / 4`, still exactly **one** 5/5 (`canon`), still un-extended three straight weeks after I first named its deepen. The lab has gotten superb at not-repeating-itself and no better at reaching higher. And a fresh structural tell: the escape hatch is clustering — **inline-SVG jumped 2× → 4× in a single window** (both new ships used it), so the anti-shader surface is now on the exact trajectory the shader was on a month ago.

## Diversity audit
_(last 15, numerically highest on disk: antiphon, keepsake, leanin, conduct, mididuet, canon, answerpiano, duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice — spheres/vigil-chord aged out; roomtone/loomvoice entered)_

- **Over-represented input:** `camera/webcam-as-primary` — **4×** (leanin, conduct, canon, headnave), and `passive/self-propelling — no load-bearing human verb` — **4×** (answerpiano, morphonate, seismscore, spiralkeep). Both cooled by one from last window (spheres + vigil aged out of the passive/camera buckets; roomtone's tilt and loomvoice's voice are genuine active non-camera verbs — credit). Neither dropped under the floor, so both stay banned. **BANNED next cycle:** `camera/webcam-as-primary` (4×) **+ `passive/self-propelling primary input`** (4×, unless carrying a genuinely-grepped never-run subsystem — the `spiralkeep` exemption).
- **Over-represented output:** `GPU generative field (WebGL2/WebGPU shader you watch)` — **7×** and still the single most-used surface (conduct, canon, morphonate, inkstroke, seismscore, spiralkeep, duetlink). But the direction finally reversed — **the last two ships in a row are inline-SVG, not shader**, the first non-shader back-to-back pair in the window. The cost of that win is a new one: **inline-SVG/DOM surged 2× → 4×** (mididuet, answerpiano, roomtone, loomvoice) — the two newest both used it. The rested hatch is becoming the next rut. Rested-and-real: **three.js inhabited room** 2× (antiphon, headnave), **Canvas2D flat** 2× (keepsake, leanin), and the genuinely-0× lanes — audio-only/embedded/haptic and projection/installation. **BANNED next cycle:** `GPU generative-field-as-the-visual` (7×) **+ `inline-SVG/DOM as the primary surface`** (now 4× — banned pre-emptively so the escape hatch doesn't harden the way the shader did).
- **Over-represented technique:** `granular grain-triggering of his take` — **6×** (conduct, canon, mididuet, inkstroke, seismscore, keepsake). Unchanged and still the default *audio* engine. The good news sits right next to it: **the two newest audio mechanisms are both non-grain** — `roomtone` convolution cross-synthesis and `loomvoice` whole-take polyphony, both of which I named last window as the alternatives. Healthy 2× clusters: `conducting→time-base` (conduct, canon), `HRTF spatial navigation` (antiphon, headnave), `reaction/excitable-field morphogenesis` (morphonate, spiralkeep), `dual-voice crossover` (canon, duetlink). Now-1× and freshly-cashed (do not re-ban into extinction): convolution (roomtone), whole-take polyphony (loomvoice), real-external-data-as-carrier (seismscore — spheres aged out, back to 1×). **BANNED next cycle:** `granular grain-triggering as the audio engine` (6×).
- **Over-represented vibe:** `warm chromatic (ember→gold / warm pitch-class-hue)` — **5×** (leanin, conduct, mididuet, canon, answerpiano), entirely in the aging tail under a standing ban. The deeper cluster is still `cool-severe on bone-or-black` — inkstroke (indigo/bone), seismscore (Ikeda B-W-R), spiralkeep (cyan-teal/bone), keepsake (cyan-white/black), duetlink (cold achromatic), antiphon (bone+oxblood/black), headnave (dark severe) — that is the actual house style now. The only two fresh palette decisions in the window are the two newest: `roomtone` high-key light + magenta, `loomvoice` polychrome — **exactly the two registers I asked for, and they're at 2×, i.e. still rested.** **BANNED next cycle:** `warm-chromatic / warm pitch-class-hue` (5×) **+ `cool-severe-ink-on-bone/black`** (indigo/cyan/B-W-R on bone or black — the standing house rut).
- **BANNED for next cycle:** `camera-primary` (4×) + `passive/self-propelling primary input` (4×) + `GPU generative-field output` (7×) + `inline-SVG/DOM primary surface` (4×, new) + `granular grain-triggering` (6×) + `warm-chromatic palette` (5×) + `cool-severe-ink-on-bone/black palette`. **Not banned (rule 10):** catalog playback as the audio source. **Explicitly rested / encouraged:** three.js inhabited room, Canvas2D, audio-only/embedded/haptic, projection; tilt/gamepad/mic-secondary active verbs; convolution / physical-model / spectral non-grain audio; high-key-light and polychrome palettes.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Twelfth straight fortnight with an empty floor. Solved; stop watching it.
- **Hit 2–3 criteria — 11** (antiphon 2, keepsake 2, leanin 2, answerpiano 3, duetlink 2, morphonate 3, inkstroke 3, headnave 3, seismscore 3, roomtone 3, loomvoice 3). The honest middle, and where **both** new ships landed. `roomtone` is a strong 3/5 nudging 4 — #2 (five real subsystems: catalog loader + raw-IR builder + convolver cross-synth graph + tilt/drag/auto-demo control + SVG cross-section) + #3 (Nugen *Paragon* 2026 convolution-as-cross-synthesis) + #5 same-cycle research — it misses #1 only because `ConvolverNode` is used 57× in the lab already; the his-take-**as-IR** twist is genuinely grep-0 but rides an existing subsystem, so I hold it to a disciplined 3. `loomvoice` is a clean 3/5 — #2 + #3 (arXiv:2604.27957 as counterpoint) + #5 — and correctly claims **no** first, negating it in its own README (extends the `spheres` polyphony lineage).
- **Hit 4–5 criteria — 4 (conduct 4, mididuet 4, canon 5, spiralkeep 4).** Distribution **`0 / 11 / 4`** — **flat versus last window's `0/11/4`.** The two entrants (roomtone 3, loomvoice 3) replaced two aged-out mids (spheres 3, vigil 2) one-for-one, so the shape held without the top band adding a single piece. That is the whole story this window: **breadth churned, the ceiling stood still.** `canon` remains the lone 5/5, and its named cycle-2 (a third mid voice / onset-synced grain emission from his own onsets, or a recorded-conducting replay) is *still* unbuilt — held because grains were the banned technique, which is a real reason, but three weeks is three weeks.

## Standouts (positive)
- **16160-roomtone** — the piece of the window on discipline. It is the cleanest *non-grain* way the lab has ever sounded his catalog: **one of his takes, raw, becomes the impulse response the other is played through** — convolution as cross-synthesis between two of his own performances, not a synth reverb. It cashes my prov #3 by name, does it off the shader (inline-SVG cross-section), off the camera (tilt), and in a palette (high-key light + magenta) that is wrong next to *both* ruts. And its novelty statement is exemplary — it grepped 57 prior convolvers, named the four that pair a convolver with his audio, and stated precisely what is and isn't new. **This is the disciplined-3/5 model: honest scope, fresh on four axes at once.**
- **16208-loomvoice** — cashes the one lane I flagged as *still 0×*: the **mic used as a real second layer over his own music** (control-only, analyser terminates dead, you only ever hear his four takes). And it is genuine **whole-take polyphony** — four complete recordings held in relationship and rebalanced by voice, never chopped into grains. Off the shader, off the camera, off passive-watching, off both palettes. Exactly the piece the verdict asked for.
- **15824-canon** — still the lone genuine 5/5 (two decoupled time-bases from one take, one per hand) and **still un-extended.** It is now the single most valuable un-cashed move in the lab: I have two proofs the extend-honestly play works (`spiralkeep` off `morphonate`, and this window's discipline generally), and canon's deepen is the biggest prize on the board.
- **16144-spiralkeep** — last window's breakout holds as the standing top-band exemplar (Barkley excitable medium + real persistence, no "first" claimed). Still the template alongside canon: extend a real 4/5 honestly rather than mint a headline.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15696-leanin** (2/5) — still the local-minimum exemplar: camera-primary + Canvas2D-flat + warm-ember + a single proximity scalar as the entire control surface. It lands on the most over-represented axes at once and adds nothing a reviewer hasn't seen in `conduct`/`canon`. The thing to climb out of, named again.
- **15920-duetlink** (2/5) — unchanged: strip the "first multi-user" banner (the lab has ~24 multi-user priors) and it is a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors. A competent plumbing demo, not a big concept.
- **15600-keepsake** (2/5) — the consume-once verb, correctly paused and now aging toward the window edge; residue, not a live problem. Its ACT-R graded-decay deepen (banked from the `15632` sibling) is the only thing that would lift it, and it isn't a priority against canon.

## Provocations for tomorrow's dream cycle
1. **The ceiling, not the spread. Build a second 5/5 — extend `canon`.** Breadth is solved; the top band has been stuck at four pieces and one peak for two windows. `spiralkeep` and this window's roomtone/loomvoice prove the lab can execute anything I name — so I'm naming the hard one: **do to `canon` what you did to `morphonate`.** Its cycle-2 is a **third mid voice** (head-tilt or two-hand centroid) or **onset-synced grain emission from his own onsets**, or a **recorded-conducting replay** so the canon performs itself back. Grains are banned as a *default* engine, not inside the lab's lone 5/5 where they're load-bearing — take the recorded-replay arm if you want to stay grain-free. A second honest 5/5 is worth more than any cycle-1 first.
2. **Don't let SVG become the next shader.** inline-SVG went 2× → 4× in one window because both new ships fled the shader into it. Ban BOTH `GPU generative field` AND `inline-SVG primary surface` next cycle and force a genuinely rested surface: **three.js inhabited room** (2×, and `antiphon`/`headnave` prove it carries a spatial piece), **Canvas2D** (2×), or the still-0× **audio-only / embedded / haptic** lane — test the screen bias entirely, ship a piece with *no primary visual*.
3. **Keep the non-grain audio momentum — but a NEW non-grain technique, not a re-run.** convolution (roomtone) and whole-take polyphony (loomvoice) are freshly cashed; don't repeat either. The still-0× non-grain audio moves: a **physical-model resonator excited by his audio** (his take drives a Karplus-Strong / modal body), **spectral freeze / cross-synthesis** (the banked `fold` — two takes fused in the frequency domain), or **additive resynthesis** from his partials. Any of these keeps the "stop chopping his take into grains" line advancing.
4. **Pick an active verb that is neither camera nor tilt nor voice — those three are now spent-fresh.** Camera 4× (banned), tilt just cashed (roomtone), mic-secondary just cashed (loomvoice), pen cashed (inkstroke), MIDI-in/out cashed (mididuet/answerpiano). The most-rested un-cashed live inputs with banked builds ready: **Gamepad** (`15904-gamepad`), and genuine **multi-touch gesture** as a bodily verb. Rotate to one of those.
5. **Hold the palette line — the two fresh registers are one-offs, don't let them calcify either.** high-key-light+magenta (roomtone) and polychrome (loomvoice) are the two decisions I asked for; they're at 2× and rested. If the next piece reaches for *either* of them it becomes 3× and I'll be writing this same complaint about them in a fortnight. Warm and cool-ink are both banned; keep finding a register wrong next to *all four* — a monochrome-plus-one-signal-color, a genuinely earthy/organic non-ink palette, or a single saturated hue nobody's used.

## Karel-facing line
The lab is now reading this jury and answering it point-for-point — `roomtone` and `loomvoice` cash yesterday's exact provocations (convolution, whole-take polyphony, off-shader, off-camera, fresh palettes) — but the ceiling hasn't risen in two windows: still one lone 5/5 (`canon`, still un-extended), the distribution stuck at 0/11/4, and the SVG escape hatch quietly becoming the next rut at 4× — so tomorrow should chase a second 5/5, not a sixth fresh axis.
