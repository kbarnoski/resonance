# Concept Jury Verdict — 2026-09-15

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.**
>
> _meta: `git fetch && checkout main && pull --ff-only` fast-forwarded cleanly (330ce6b→38163f0, 9 commits). Read-only on `src/app/dream/**` — no prototype touched. **The window moved by TWO again:** `16752-chordnebula` and `16768-harmonicswarm` roll off the bottom; `17200-hall` and `17232-tunesignal` roll on the top. Review set (numeric last-15): attune · gushbody · tunevoice · strata · nodalfigure · tensethread · vaultloom · inkpressure · voiceloom · firstlook · breathline · tidemark · hearth · **hall** · **tunesignal**. Both newest are real builds (hall 1108 lines WebGL2, tunesignal 890 lines WebGL2; both passed the authoritative `npm run build`). RESEARCH.md is fresh — today's cycle-1239 dive is dated 2026-09-15. Renderers re-verified from source, not READMEs: tidemark = Canvas2D, hall = WebGL2 (its 2D context only reads webcam pixels)._

## Summary
The lab executed again — cleanly, for the third jury running. Yesterday's verdict named four specific moves (claim the 0× projection shelf; take a live non-multi-user body; bring a third/neutral temperature; build something impossible to ignore on a cold muted glance) and the lab shipped `hall` and `tunesignal`, which between them hit **all four**. `tunesignal` in particular is the *firstlook instinct done right as a real concept*: whole-screen 1-bit static that resolves into a figure of the music as you tilt — engineered for exactly the muted phone glance the loop has been starving on. Full credit: this is now three straight windows of read-the-jury-and-move. **And it has changed nothing that matters.** The single fact of this verdict is the same as the last three: **five bold, thin-shelf pieces now sit unverified — ~2 weeks, zero opens, votes flat** (breathline, tidemark, hearth, hall, tunesignal). The build engine is flawless. The review loop is a corpse. Stacking a *sixth* bold build is no longer diversity or courage — it is the lab choosing "build another thing Karel won't open" over the one move that would actually break the freeze.

## Diversity audit
- **Over-represented input:** `none / autonomous — no live body` **8×** (strata, nodalfigure*, tensethread*, vaultloom*, inkpressure, voiceloom, firstlook, breathline, tidemark → strict-autonomous 8, counting the three starred as view-only-orbit/perturbation, not a body driving the art). **Down from 10× — and the trend is genuinely good:** the two rolled-off pieces were autonomous, and BOTH rolled-on pieces take a live body (hall = camera optical-flow, tunesignal = device-tilt as the *primary* instrument). Live inputs: multi-user 2× (attune, hearth), camera 2× (gushbody, hall), tilt-as-primary 1× (tunesignal), mic 1× (tunevoice, secondary), touch-perturbation 1× (nodalfigure). The autonomous plurality is real but finally receding on its own.
- **Over-represented output:** **`raw WebGL2` — 5× (the NEW output rut):** attune, gushbody, tunevoice, hall, tunesignal. The prior jury ordered "rest Canvas2D" — the lab obeyed (Canvas2D down 5×→**3×**: voiceloom, breathline, tidemark) but piled straight onto raw WebGL2 instead, and **both newest builds are WebGL2.** `SVG` holds at **3×** (nodalfigure, inkpressure, firstlook — WATCH, do not make 4×). `three.js` **2×** (tensethread, vaultloom) and `WebGPU` **2×** (strata, hearth) are the rested substrates now. `audio-only` still ≈1× (tunevoice, the perpetual singleton).
- **Over-represented technique:** **`chord-as-subject` — 4× (RECEDING, keep banned):** gushbody (hue), tunevoice (consonance), nodalfigure (consonance), tensethread (tension). Down from 5–6; chordnebula rolled off and none of the last five builds is chord-anchored. The ban is working — hold it so the four age out. `memory/accretion-as-subject` **3×** (strata, tidemark, hearth) — the rut the last jury flagged as "the new chord" has NOT grown: the two newest are both non-memory. Good. `optical-flow advection` quietly reached **2×** (gushbody, hall) — watch, don't ban.
- **Over-represented vibe:** `warm` **5×** (attune, gushbody, breathline, tidemark, hearth) — still the over-corrected run the last jury named, BUT it is **aging, not growing**: those five are the *middle* of the window and the **two most recent builds broke to a genuinely neutral third temperature** (hall = graphite→silver, tunesignal = achromatic 1-bit black↔white). The "bring a third temperature" order was answered outright. `cool-luminous` sits at ~5× (strata, nodalfigure, tensethread, vaultloom, voiceloom). Neutral **2×** and rising is the healthiest palette signal in a month.
- **BANNED for next cycle:** `chord-as-subject` (carryovers only — keep aging out) · `raw WebGL2` (NEW — 5×, both newest; next renderer is three.js / WebGPU / **audio-only**, NOT SVG at 3×) · a **sixth consecutive bold unverified build** (see provocation #1 — the true ban this cycle is on *more inventory*) · a **fifth warm OR fifth cool** palette if a build happens (stay neutral, or go audio-only where palette is moot).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. Still a lure, not a concept: inventory dressed as a build, and still aimed at the wrong target (it fronts `inkpressure`, a mid SVG, while five bolder pieces sit un-fronted behind it). Local-minimum ship of the window, unchanged.
- **Hit 2–3 criteria:** **9** — gushbody (2), nodalfigure (2), tensethread (2), vaultloom (3), tunevoice (3), inkpressure (3), voiceloom (3), **hall (3)**, **tunesignal (3)**. Honest mediums. The two newest land here squarely: each clears #2 (≥3 subsystems) + #3 (named ref) + #5 (same-day research), and each opens a genuinely new shelf (hall = first installation/projection framing; tunesignal = first 1-bit/ordered-dither + first tilt-as-primary), but neither is a hard grep-0 technique-first, so they're strong 3s, not 4s.
- **Hit 4–5 criteria:** **5 — the strongest top band the lab has ever posted.** attune (4 — first WebRTC/multi-user), strata (4 — first cross-session memory), breathline (4 — grep-0 rubato-breath + bioRxiv + same-day research), tidemark (4 — first per-position collective-attention sediment), and **hearth (5 — the sole confirmed 5/5: novel WebGPU-compute co-presence + ≥3 subsystems + named refs + a genuine cycle-2 fusion of two prior firsts + same-day research).** These five are the ones to *extend or front*, never to bury under a sixth sibling.

## Standouts (positive)
- **17232-tunesignal** — the most *strategically* right build in the window, because it's the first that targets the actual bottleneck instead of the concept shelf. Whole-viewport achromatic static that resolves into a radial figure of his music as you tilt your body toward a hidden sweet spot — and the take's lowpass opens in your ears at the same instant, so it clears up twice at once. It is legible in one cold muted glance, needs a body not a mic, and is a true third temperature. This is what the firstlook instinct should have been all along: a real concept that is *also* impossible to ignore.
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: two prior firsts (multi-user co-presence + cross-session memory) welded into one room that remembers its listeners. Reads warm on a cold muted glance via ~12 pre-seeded prior sessions. The genuine cycle-2 every prior mandate begged for.
- **17200-hall** — claimed the single biggest unclaimed shelf: the lab's first installation/projection framing (whole viewport as a venue wall, room's own motion rippling the light via webcam optical-flow), in a deliberately neutral Ikeda-adjacent palette. Answers two standing provocations (projection/spatial 0×, third temperature) in one move.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: recover the ~0.36 Hz "breath" from the signed rubato of his real take; the aperture inhales on lean-back, exhales on press-forward, with a measured Hz readout, not a faked one.
- **17136-tidemark** — the memory shelf shipped honest: a recording growing a geological core-sample of where collective attention pooled, with x=time so "most-returned passage at M:SS" is spatially truthful.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it is now aimed at the wrong target *three ways over*: it fronts `inkpressure` while hearth, breathline, tidemark, hall, AND the purpose-built-for-glance `tunesignal` all sit un-fronted. If the lab builds another front door, it should front `tunesignal`, which already does firstlook's job as a real piece.
- **16944-nodalfigure** / **16960-tensethread** — the weakest of the four surviving chord-as-subject pieces: a harmonograph that closes on consonance, a web that reddens on tension. Elegant, and exactly the reflex still banned.
- **16848-tunevoice** — thinnest live-input piece and still chord-anchored: a mic that opens a filter when you sing a chord tone. Its near-audio-only shape is a virtue on the rested shelf; the concept is slight.
- **16832-gushbody** — lowest ambition floor of the live-body pieces (2/5): a warm optical-flow feedback field you stir on webcam, with chord-hue tint. Pleasant, but it's the camera-input piece with the least new to say — and optical-flow is now 2×.

## Provocations for tomorrow's dream cycle
1. **STOP BUILDING A SIXTH. The loop is the only broken part, and you already built the tool to fix it.** Five bold thin-shelf pieces stand unverified ~2 weeks; `tunesignal` is *literally engineered* to be the impossible-to-ignore cold-glance piece the last jury demanded, and it too has zero opens. A sixth concept is avoidance wearing a lab coat. The honest fire tomorrow is NOT a new prototype — it's **one unified front-door gallery** (a single new page, immutability-clean) that shows the five bold pieces resolving/blooming at a glance, muted, one tap each — OR a deliberate research/rest fire. Do not add to the pile you can't get looked at.
2. **You just started a raw-WebGL2 rut — don't feed it.** Output is now WebGL2 5× (both newest). If a build genuinely must happen, it renders in `audio-only`, `WebGPU` (2×), or `three.js` (2×) — never a fourth SVG and never a sixth WebGL2.
3. **The two truly-untouched shelves are `audio-only/no-screen` (≈1×) and `>2-presence ensemble`.** Multi-user is 2× but BOTH pieces are strictly two-person; a real ensemble weave (the banked `commons`/`ensemble`, IDEAS §1237) is 0×, and hall's venue framing + hearth's co-presence both already point straight at a venue-scale, many-listener room. If tomorrow must build, cash `commons` — it kills projection-scale AND >2-presence at once.
4. **The depth-camera / MediaPipe-hand embodied-spatial piece has been named in RESEARCH for FIVE WEEKS and never built** (§1091, 2026-08-11: "body-tracking-as-spatial-audio-controller is grep-0"). It hits `audio-spatial` (thin) AND `embodied-non-social body` (thin) in a single move. It is the highest-ambition unbuilt hook in the file — a real 4/5 candidate, not a sixth honest-medium. If you override provocation #1 and build, build THIS.
5. **Give hearth its cycle-3 only *after* a verification — otherwise you're deepening a piece no one has confirmed reads.** hearth is the only 5/5; the mandate says extend a first into its next cycle. But extending an unopened piece is the same freeze in a bigger form. The correct order is: get ONE of the five opened first (front door, provocation #1), *then* cycle-3 whichever Karel's tap validates. Sequence matters more than ambition right now.

## Karel-facing line
The lab nailed your brief a third straight time — shipped the projection wall (`hall`) and a body-tuned 1-bit signal (`tunesignal`) built to grab you in a single muted glance — but that's now FIVE bold pieces you haven't opened; the engine is perfect and the only broken part is one tap from you.
