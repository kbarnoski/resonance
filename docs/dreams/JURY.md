# Concept Jury Verdict — 2026-09-04

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/data/peer allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→110a4a3; cycle 1217 `16800-attune` at HEAD, with the 2026-09-03 verdict in the history). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. ONE new ship since the 2026-09-03 verdict: `16800-attune` (cycle 1217); `16208-loomvoice` aged out of the 15-window. RESEARCH.md is fresh (dated 2026-09-04 entry for cycle 1217; the research→build chain is visible). NOTE: cycle 1218 (`16832-gushbody`, camera-motion/optical-flow) landed on main mid-run, after this survey was taken — this verdict is a snapshot reviewed **through cycle 1217 / `16800-attune`**; gushbody enters next window (and, on first read, correctly followed provocation #2 below — WIDE, fresh sensors, off every ban)._

## Summary
**Three nights, and the lab has now cashed every single swing this jury ever named.** WebGPU-compute (the swarm), volumetric raymarch (the nebula), and now — with `16800-attune` — **multi-user / WebRTC**, the "last uncashed big swing," the one I called for months "the biggest single swing available." It is a genuinely good piece and the discipline is again exemplary. **And the ceiling still did not move.** Fifth straight day at zero verified 5s; the distribution is frozen at `0 / 14 / 1` for the second window running; the ship-for-ship trajectory is now **eleven consecutive honest 3s**. This is no longer a hypothesis — it is proven: the jury named two 0× swings, the lab cashed both inside three nights, and the top band did not gain a single point, **because the ceiling was never gated by surface. There are now no more surfaces to cash.** The next verified 4/5 cannot come from a build of any kind — every category is cashed. It can only come from Karel opening a page. Worse for the diversity story: because this was a DEEP *single-ship* cycle, only one window slot rotated — and it displaced a singleton (`loomvoice`), so **every over-represented tag sits exactly where it did yesterday.** The lab cashed its last swing without draining a single ban.

## Diversity audit
_(last 15, numerically highest on disk: revenant, ringcanon, manyreturns, phosphene, driftchoir, terrafret, spectralbraid, longvigil, tonguescript, scriptorium, albumvoyage, cadencemap, chordnebula, harmonicswarm, attune — `loomvoice` aged out; `attune` entered)_

- **Over-represented input:** **`keyboard / typing` — still 4× and still at the ban floor** (tonguescript, scriptorium, albumvoyage, cadencemap — the manuscript block). Unchanged from yesterday: no fifth was added (attune is catalog-playback + slow pointer-presence + a multi-user peer), but none of the four aged out either, so it holds maxed. `catalog-playback + pointer/tilt secondary` **3×** (chordnebula, harmonicswarm, attune — attune's pointer-presence sits in this cluster) — now AT the WATCH ceiling, one from a ban. `autonomous/passive` 2× (manyreturns, longvigil). Everything else 1×: `multi-user peer / WebRTC` (attune — the 0×→1× headline axis, in-window-first but NOT lab-first — see below), tilt/orient (phosphene), gamepad (ringcanon), press-hold (driftchoir), multi-touch (revenant), pen/pointer-paint (spectralbraid), live-external-data (terrafret). **BANNED next cycle: `keyboard/typing` (4×).** **WATCH:** `catalog-playback + pointer/tilt` (3× — AT ceiling, AVOID).
- **Over-represented output:** **Both big bans are exactly as stuck as yesterday — the predicted aging-out did NOT happen.** `Canvas2D` holds at **5×** (ringcanon, manyreturns, terrafret, spectralbraid, longvigil) and `DOM/typographic` at **4×** (the four manuscripts) — yesterday's verdict predicted ringcanon + manyreturns would age out and "Canvas2D will finally break," but a single-ship DEEP cycle rotated out `loomvoice` (an inline-SVG singleton) instead, so **neither count dropped by one.** A week-plus of hard bans and both are frozen. `three.js` holds **3×** (revenant, phosphene, chordnebula) — still AT the WATCH ceiling, still one ship from a ban. `raw WebGPU compute+render` 1× (harmonicswarm). **`raw WebGL2 fragment field` registers 1× this window** (attune — the deliberate off-three.js, off-Canvas2D surface). `audio-only` 1× (driftchoir); `inline-SVG/DOM art` dropped 1×→0× (loomvoice aged out). **BANNED next cycle:** `Canvas2D` (5×) **and** `DOM/typographic` (4×). **WATCH:** `three.js` (3× — AT ceiling, AVOID).
- **Over-represented technique:** `prosody / text-score cross-modal mapping` — **still 4×** (the four manuscripts), banned, unmoved. `loops-of-one-take` family (canon + incommensurate phase-drift) holds **4× hard** (revenant, ringcanon, manyreturns, driftchoir) **plus the four manuscripts run incommensurate-loop phasing as substrate** — effectively **~8×**; keep hard-closed, no ninth. The 1× singletons to preserve and extend, not re-ban: **co-presence attunement scalar** (attune — a shared parameter only two attuned presences can raise; genuinely new to the lab), volumetric FBM raymarch (chordnebula), GPU particle-life w/ chord attraction (harmonicswarm), harmony-carved geometry (phosphene), spectral cross-synthesis (spectralbraid), stateful memory-accretion (longvigil), live-data→sample sonification (terrafret). **BANNED next cycle:** `prosody/text-score` (4×) + `loops-of-one-take family` (4× hard / ~8× w/ substrate — keep closed).
- **Over-represented vibe:** `near-dark / intimate typographic` — **still 5×** (driftchoir + the four manuscripts), banned, unmoved (all five still in-window). `monochrome-base-plus-one signal` holds **3×** (graphite+chartreuse ringcanon, ashlight+bone manyreturns, silver+jewel phosphene) — WATCH, one from a ban. `bright luminous cosmic field` holds **2×** (chordnebula, harmonicswarm) — WATCH, the emerging cluster the last verdict flagged; credit to the lab for NOT making it 3× this cycle (attune is warm-candlelit, a deliberate anti-cosmic choice). `two-signal duotone` 2× (terrafret, spectralbraid). **NEW: `warm candlelit / intimate co-presence` 1×** (attune) — the clean palette break. `polychrome` 1× (longvigil; loomvoice's polychrome aged out). **BANNED next cycle:** `near-dark/intimate typographic` (5×). **WATCH:** `monochrome-base-plus-one` (3× — AT ceiling), `bright luminous cosmic field` (2× — hold at 2, do not add a third), `two-signal duotone` (2×).
- **BANNED for next cycle:** `keyboard/typing input` (4×) + `Canvas2D output` (5×) + `DOM/typographic output` (4×) + `prosody/text-score technique` (4×) + `loops-of-one-take technique family` (4× hard, ~8× w/ substrate — keep closed) + `near-dark/intimate typographic palette` (5×). **WATCH (all AT or near the ceiling — AVOID):** `three.js output` (3×), `catalog-playback+pointer/tilt input` (3×), `monochrome-base-plus-one palette` (3×), `bright luminous cosmic field palette` (2×), `two-signal duotone palette` (2×). **The honest headline of this audit: the ban list is byte-for-byte identical to yesterday's.** A DEEP single-ship cycle cannot drain a ban — it rotates one slot, and this one rotated a singleton. **Every 0× category is now cashed** (WebGPU-compute, multi-user/WebRTC both in-window): there is no longer a "cash a fresh surface" move left to make.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Twentieth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 14** (revenant 3, ringcanon 3, phosphene 2, driftchoir 2, terrafret 2, spectralbraid 3, longvigil 3, tonguescript 3, scriptorium 3, albumvoyage 3, cadencemap 3, chordnebula 3, harmonicswarm 3, **attune 3**). **attune** cleared #2 (7 subsystems: catalog loader + FFT band engine + time-matched chord tracker + `_shared/peerSync` multi-user transport [WebRTC data channel + BroadcastChannel + NTP shared clock + presence] + synced-playback anchor scheduler + WebGL2 render field + pointer-presence controller) + #3 (Pauline Oliveros *Deep Listening* + "Co-Sound: spatial synchronization" 2026) + #5 (RESEARCH §2026-09-04 c1217) — an honest 3.
- **Hit 4–5 criteria — 1 (manyreturns 4).** Distribution **`0 / 14 / 1` — frozen, identical to last window.** The ship-for-ship ambition trajectory is now **… 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3** across ELEVEN straight ships. **Fifth straight day at zero verified 5s.** attune, like the ten before it, ships stamped "not run — `tsc`/ESLint only, needs Karel at the page (and a second tab)." **The single most important sentence in this whole verdict:** the lab has now cashed *both* big swings I named — WebGPU-compute AND multi-user/WebRTC — inside three nights, and the top band gained *zero* points, which is the cleanest possible proof that **surface was never what gates the 5. Verification is.** There is no longer any 0× surface left to cash as an alternative to getting the verdict. The build lever is spent.
- **Note on attune's #1 discipline (good):** it did NOT claim "lab-first multi-user" — grep found `15920-duetlink` already imports the same `peerSync`, so multi-user is 0× *in the recent 15-window* but not lab-first. The README's honest first is "the first co-presence piece where shared *attention*, not conducting, deepens one shared room parameter." That is the terrafret false-first lesson correctly applied for the third time running (harmonicswarm, chordnebula, now attune).

## Standouts (positive)
- **16800-attune** — the headline, and a real milestone: it cashes the **last named 0× category** (multi-user / WebRTC), the "biggest single swing available," three nights after the WebGPU swing landed. And it *earns* the surface conceptually rather than decorating it: the room deepens — the warm field blooms, a honey bridge forms, and the take itself audibly *opens* (a lowpass lifts) — **only when the two presences are close together AND both moving slowly.** Shared attention is the instrument; nobody conducts. It is the clean categorical opposite of `duetlink`'s two-voice mixer, the purest Deep-Listening fit in the lab, and — crucially — it ships a **solo two-tab fallback** so Karel can get a verdict alone on his phone in ~20 seconds. Same exemplary discipline (grep-checked, honest-first declined). Pending only the eye/ear call.
- **16768-harmonicswarm** — still the technical high-water mark of the window: the lab's first raw-WebGPU-compute piece, Karel's sounding chord rewriting a 6×6 inter-species attraction matrix so you can *see the physics of consonance*. Phone-renderable, best-disciplined README of its own cycle. Pair it with attune as the two freshest, fastest eye-calls.
- **16368-manyreturns** — for the second window running, the **lone 4** and the single most obvious deepen target the lab keeps walking past. Its self-building incommensurate ensemble reached 4 by extending a real thing until it deepened. It remains the likeliest honest path to a verified 5 — but NOT via the ~8×-hard-closed loop engine it stands on; the deepen must come on a new axis (memory you can inspect, a return you can hear the accumulation of).

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The manuscript block** (tonguescript → scriptorium → albumvoyage → cadencemap, all 3/5) — not a quality knock, a structural drag with a new consequence worth naming: this 4-wide block is *the reason four of the six bans (keyboard, DOM, prosody, near-dark) cannot drop.* At one-slot-per-DEEP-cycle rotation, those bans are pinned for roughly **four more single-ship cycles**. The only thing that drains them faster is a WIDE cycle that ships one but pushes the window forward, or simply time — but four consecutive DEEP single-ships is how the lab painted itself into this corner in the first place.
- **16496-terrafret** (2/5) — the false-"first" cautionary tale, now with a *third* counter-example (attune joins harmonicswarm + chordnebula as honest-first-declined). Cite the set as the before/after of first-claim discipline.
- **16448-phosphene** (2/5) & **16464-driftchoir** (2/5) — two honest disciplined-2s that read well and reach for nothing (three.js-solid; audio-only). Fine as rested-surface breathers, not concepts to extend toward a 5.

## Provocations for tomorrow's dream cycle
1. **Verification is now the ONLY lever left — and it is finally proven, because there are no more swings.** Both 0× categories the jury ever named are cashed and the ceiling didn't move; "we need a fresh surface" is now an empty excuse — every surface is spent. Three phone-renderable **eye-calls** sit ready (attune — a 20-second two-tab test; harmonicswarm; chordnebula) and four **ear-calls** (the manuscript block). Nothing new should ship until Karel puts eyes/ears on at least one. A verified 4 on any of the three GPU/WebGL pieces would be the first ceiling movement in over a month.
2. **Stop shipping DEEP *single-ship* cycles — they cannot drain a ban.** This window one slot rotated (loomvoice→attune) and it displaced a singleton, so Canvas2D 5×, DOM 4×, prosody 4×, near-dark 5×, keyboard 4× are all frozen at yesterday's counts. If diversity is a goal (it is — Karel asked for it by name), a **WIDE 3-explorer cycle** that ships one but moves the window forward faster is the only build-side way to break the manuscript/Canvas2D lock. Run WIDE next.
3. **Do NOT ship the banked `16816-tether` next.** Co-presence is 1× (attune). tether is its sibling and fully built, and shipping it immediately makes co-presence 2× back-to-back — the exact two-in-two-nights pattern that started the manuscript and the cosmic-field ruts. The STATE queue already flags this; honor it. Ship tether later, on a non-cosmic, non-co-presence cycle.
4. **If the fire must go DEEP, aim it at manyreturns (the lone 4) on a NEW axis — not the loop engine.** Its path to a 5 is real state you can *return to and read the history of*. The freshly-cashed multi-user surface offers a natural version of this: give attune's room **memory** — a slow-decaying trace of where two people attuned, so returning to a track re-forms the shape of a past shared session. That is the spiralkeep persistent-medium → verified-4 path, on a brand-new surface. But it, too, is a guess about a guess until attune itself is verified (see #1).
5. **Discipline nit, third ship running:** attune's README records verification as "`tsc --noEmit` and `eslint` pass" and calls `npm run build` "run by the orchestrator." AGENT.md rule 3 is explicit that `tsc` alone is NOT the gate — Vercel's ESLint + full production compile is. The piece is live, so `npm run build` presumably passed at commit; the README should *say so*, not cite `tsc`. Same wording flagged on harmonicswarm and chordnebula — fix the template so the gate that actually protects Karel's app is the one on record.

## Karel-facing line
Three nights, and the lab cashed the last two swings I ever asked it for — WebGPU and now a real multi-user shared-listening room (`attune`, and it's good) — but the ceiling is *still* frozen at zero verified 5s for the fifth straight day, which now proves the point past argument: fresh surfaces were never the missing thing, your eyes were — attune is a 20-second two-tab test on your phone, and that test is the whole ballgame now.
