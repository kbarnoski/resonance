# Concept Jury Verdict — 2026-08-21

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git pull --ff-only origin main` succeeded cleanly today (Already up to date at HEAD `21ff40a`/cycle 1189 line) — no forced-update divergence this cycle, unlike 08-20. Survey ran on the authoritative tree. Read-only on the lab per scope fence._

## Summary
You did the assignment. Yesterday's verdict said "build the banked keepsake as a dedicated DEEP — the curation-by-loss ceiling lane, and it is NOT a walk-a-room and NOT three.js," and cycle 1189 shipped exactly that: `15600-keepsake`, his take through once, keep only what you reach for, replay is the negative of your own attention. It's a real piece and it's not a room. **Credit, in full.** But read the window with fresh eyes and the pattern is uncomfortable: keepsake is the **sixth** "cut his catalog into slices and make it comment on its own absence/scarcity" piece in this fortnight (toll · negative · vigil · antiphon · transcript · keepsake). The conceptual/critical subtraction lane you were *praised* for last week is now the monoculture — **the exact pendulum the last three verdicts kept naming, this time swinging on the axis the jury itself blessed.** And `pulse` is the lone 4/5 for a **fourth** straight window.

## Diversity audit
_(last 15, numerically highest on disk: sympathy, slimenerve, waveroom, pulse, schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake — phantomline aged out, keepsake entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **~7×** (slimenerve, schlieren, transcript, bloomwork, recur, negative-slider, spheres-slider; sympathy is nudge-on-passive). Standing ban from 08-19, still earned — though the *newest* half is where the human is load-bearing (pulse PPG, coherence breath, toll spend, vigil sustain, antiphon navigate, keepsake grab), so the trend is up. **BANNED next cycle: passive/self-propelling as the PRIMARY relationship.** The real hole underneath: **body/pose tracking 0×, camera 1× (pulse), mic 1× (coherence), MIDI 0×, multi-user 1× (sympathy).** The input axis is barely explored — it's mouse/keyboard/slider on top of playback, plus two biosignal one-offs.
- **Over-represented output:** `Canvas2D flat single-surface` — **~5×** and now the window leader (transcript, toll, recur, negative, keepsake — keepsake pushed it to 5). `raw-WebGL2 field` **4×** (sympathy, waveroom, schlieren, coherence — standing 08-19 ban, still shipping). `three.js immersive room` **3–4×** (bloomwork, spheres, vigil-chord; antiphon is the same *inhabit-a-space* form). Genuinely rare and stuck: **WebGPU 1× (slimenerve), inline-SVG/DOM 1× (pulse).** **BANNED next cycle: Canvas2D flat-timeline output AND (standing) three.js walk/inhabit-a-room AND (standing) raw-WebGL2 field.**
- **Over-represented technique:** two clusters at 4×. `physics/agent simulation` — **4×** (sympathy Kuramoto, slimenerve physarum, waveroom FDTD, schlieren wave-field), all front-loaded; the 08-18 ban killed it in the back half, keep it standing. The **rising** one is the problem: `conceptual/critical subtraction & consume-once` — **4× hard** (toll, negative, vigil-chord, keepsake) and **6× if you add the corpus-talks-to-itself pieces** (transcript, antiphon). This is your best work AND your new rut simultaneously. **BANNED next cycle: subtraction/consume/negative-space technique.** The corpus lane #4 asked for is genuinely alive (≥5 real takes in relationship in sympathy, antiphon, spheres) — that stays.
- **Over-represented vibe:** `achromatic / grayscale on near-black` — **~6×** (slimenerve, waveroom, schlieren, coherence, transcript, recur/toll), standing 08-19 ban. `conceptual/critical` as a *mood* — **~5–6×** (toll, negative, vigil, antiphon, keepsake, transcript). The one honest new register this window is keepsake's **clinical cyan scan-line ledger** — which is exactly the Ikeda-datamatics direction 08-20 asked for (#4), so credit that pickup. Full-chromatic has cooled to ~3× (bloomwork, negative, vigil-chord). **BANNED next cycle: achromatic/grayscale AND conceptual/critical-subtraction mood.**
- **BANNED for next cycle:** `passive/self-propelling primary input` + `Canvas2D flat-surface output` + (standing) `three.js inhabit-a-room` + (standing) `raw-WebGL2 field` + `subtraction/consume/negative-space technique+mood` + `achromatic/grayscale palette` + (standing) `recording-drives-a-physics/agent-sim`. **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Sixth straight fortnight with an empty floor. The gate works; floor *height* is solved. Stop optimizing it.
- **Hit 2–3 criteria — 14.** Almost the whole window, and almost all via the same `#2 subsystems + #3 named ref + #5 same-day research` combo every orchestrated cycle satisfies *by construction*. keepsake (2/5), toll (2/5), vigil-chord (2/5), antiphon (2/5) — **your four sharpest concepts of the window all score 2/5**, at or below a settling-particle plate.
- **Hit 4–5 criteria — 1 (pulse).** Unchanged for a **FOURTH** window. keepsake — the jury's own #3 pick, cashed cleanly — scored 2/5. **No new 4/5 has been created in ~12 cycles.**

Distribution **0 / 14 / 1**, flat against the last two windows (0/14/1, 0/14/1). Three verdicts running have said "beat pulse or retire the number." The lab did neither — it built the concept I asked for and the metric scored it a 2. **I'm calling it: subsystem-counting can no longer see your best work, and I will stop pretending the number is the ambition axis.** From here, the only 4/5 that counts is *a subsystem the lab has never run* (see provocation #3). Everything else, score by "does Karel lean forward."

## Standouts (positive)
- **15600-keepsake** — you took yesterday's #3 head-on and it landed. His take plays once and never loops; you press-and-hold to keep what's sounding; the unkept scrolls off and is gone; replay is only your keepsakes, gaps closed — *the negative image of your own attention.* A committing, irreversible verb that is NOT locomotion (jury 08-20 #2), not a 3D room (#3), and the clinical scan-line palette the verdict asked for (#4). This is the lane to widen — but see the audit: it's also the sixth subtraction piece, so widen it by **technique/input**, not by another slice-Bath concept.
- **15392-recur** — still the model of the mandate: it cited a genuinely fresh finding (Hawley, *arXiv:2608.04378*, 2026-08-05) and built a self-composing engine that through-composes a NEW movement from real slices, drawing recurrence arcs back to each theme's first hearing. `today's research → today's build` visible in one commit. The best piece in the window on the honest axis.
- **15440-spheres** — the cleanest "real external data as the *carrier*, not a sim of it": true planetary ephemeris (zero network, zero synth) layering his takes by live aspect geometry. Different every open; boundless-cosmic without faking the world.
- **15152-pulse** — still the lone 4/5 and still the only piece that reads the visitor's own body. It should not be the ceiling for a fourth window — that's a statement about the lab, not about pulse.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15104-waveroom** (2/5) — unchanged across three verdicts: a physics grid you walk through, FDTD precedented, raw-WebGL2 + pointer, the banned triad, and the origin of the inhabit-a-room template. The clearest local minimum of the window.
- **15200-schlieren** (3/5) — a lab-first *technique* riding the most worn-out *relationship* in the lab: audio → GPU field → you watch. Gorgeous, but the same passive verb as waveroom in grayscale clothing.
- **15344-toll** (2/5) — where the subtraction lane starts to repeat itself. toll (spend-to-hear-once) and keepsake (keep-only-what-you-grab) are two framings of the same consume-once verb, and keepsake is the sharper of the two. toll now reads as the earlier draft of an idea you've since nailed.
- **15312-transcript** (2/5) — self-describes as "a deep extension of `14656 · answerback`"; the harmonic-scoring addition is incremental, no external named reference, no recent-research hook. Local-minimum-by-extension.

## Provocations for tomorrow's dream cycle
1. **Kill the subtraction/consume lane for a week — it is now the monoculture the last three verdicts warned about, on the axis I blessed.** toll · negative · vigil · keepsake in ~7 cycles, plus transcript/antiphon. You own this move; you have nothing left to prove on it. The next surprise will NOT come from another clever way to slice Bath into an absence. Banned: subtraction, negative-space, consume-once, "the catalog comments on its own scarcity."
2. **Ship the first body.** Pose/hand tracking is **0×** in the whole window; camera and mic appear once each. A MediaPipe pose or hand skeleton as a SECONDARY conductor over a real take — his recording bends to how you move your arms/hands, catalog stays the source (rule 10 satisfied) — is the biggest unfished input axis AND the embodied-non-locomotion verb three verdicts have asked for. Not WASD. The hands.
3. **Beat pulse with a subsystem the lab has never run — or the 4/5 is officially retired.** Fourth window with pulse as the lone ceiling. The next 4/5 must be a genuine first: **MediaPipe pose/hand, real-time MIDI/OSC out, WebRTC multi-user composition, or a WebGPU-compute engine that is NOT a field-you-watch.** Anything that only recombines a slicer + a canvas is a 2/5 no matter how sharp the concept.
4. **WebGPU is stuck at 1× (slimenerve) and it was a real technical first.** Commit a multi-cycle WebGPU-compute piece that is NOT a wave/field you passively watch — a fluid, an n-body, or a compute-driven granular re-synthesis of his takes you can push around in real time.
5. **Cash a <30-day RESEARCH.md finding as a *dated build hook*, the way recur did.** keepsake used yesterday's Oblivion/ACT-R decay concept but not as a dated anchor. recur is the window's best piece precisely because the research→build chain was literal and same-week. Make tomorrow's build name the finding, the date, and the hook in STATE.md — and check freshness: RESEARCH.md's newest entry is 2026-08-21, so the freshness is there; use it as a build seed, not just a log.

## Karel-facing line
You built the exact piece I asked for (keepsake) and it's a gem — but the conceptual-subtraction lane I blessed last week is already your new monoculture (6 in a row), and "pulse" has been your lone ceiling for four windows straight. Tomorrow: a body, not a mouse — and a first you've never shipped.
