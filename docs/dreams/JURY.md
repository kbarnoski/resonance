# Concept Jury Verdict — 2026-09-25

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.** The 2026-09-16 EMBODIED-CAMERA directive is also binding: `camera` / `body-tracking` INPUT is EXEMPT from the ≥4-in-15 ban — it is *supposed* to repeat. This verdict drives diversity through the OTHER axes.
>
> _meta: `git fetch && checkout main` — origin/main had been **force-updated** (`bb5907f → 496ad7d`) and the fresh clone had diverged 50/50, so `pull --ff-only` aborted; I `git reset --hard origin/main` to survey the true head (read-only on prototypes, nothing committed from the reset). Numeric last-15 set: resonantpair · bodycast · mirrorfold · mudra · limn · stillpoint · tremor · strand · overhead · firmament · scriptorium · ledger · reharmonize · **cantormap · aureole** (`commons` and `timeheads` have now rolled OFF the bottom). **I read the LIVE love signal this cycle** (`curl /api/dream/votes`) rather than carry STATE's figure — and it corrects the last verdict: `17536-mudra: 1` **is loved**, alongside `17200-hall`, `16960-tensethread`, `16032-headnave`, `15920-duetlink`, `15824-canon`. Karel's entire recent-loved set is embodied-camera. RESEARCH.md is fresh — top entry dated **today, 2026-09-25** (listener-position auralization), chaining into `aureole`._

## Summary
The single most important fact this window is one the last five juries got wrong: **the camera lane is not dead — it is the only lane Karel has loved.** `17536-mudra` earned a ♥ and every one of the six recent loves is an embodied-conducting piece; meanwhile the entire 5-piece real-world-DATA detour (tremor → overhead → firmament → scriptorium → ledger) earned **zero** love and is exactly where ambition collapsed (both 0–1-criteria builds live there, on generic citations). The lab has correctly snapped back to the loved lane and — to its real credit — is now conducting genuinely *new* musical parameters there (spatialization in `cantormap`, reverberant distance in `aureole`), which is where all the criterion-A novelty in this window comes from. Two problems remain: it burned ~2.5 days chasing a "pivot off camera" thesis the vote had already falsified, and the three freshest embodied pieces (`limn`, `cantormap`, `aureole`) all ship **unverified** `wip` — the lab is stockpiling untested inventory in the very lane that just got validated.

## Diversity audit
- Over-represented input: **camera / embodied tracking** (8×: resonantpair, bodycast, mirrorfold, mudra, limn, stillpoint, cantormap, aureole) — **EXEMPT per the 2026-09-16 directive; not banned.** The real runner-up to ban is **real-world data-feed** (5×: tremor, overhead, firmament, scriptorium, ledger).
- Over-represented output: **three.js** (5×: bodycast, mirrorfold, overhead, scriptorium, ledger). Raw WebGL2 is the healthy runner-up (3×: resonantpair, limn, cantormap); WebGPU sits at 1× (`mudra`).
- Over-represented technique: **granular-scrub of the real take** (~6×: resonantpair, stillpoint, strand, scriptorium, ledger, +overhead) and **event/data sonification** (5×: tremor, overhead, firmament, scriptorium, ledger).
- Over-represented vibe: **warm-candlelit-amber, 5×** (resonantpair, scriptorium, ledger, reharmonize, **cantormap**) — and note `cantormap` went warm *after* the 2026-09-24 jury explicitly banned it, so only `aureole` (achromatic silver/bone) actually broke the streak. Cool-luminous-violet is 3× (mirrorfold, limn, strand); near-black cosmic-sky 3× (stillpoint, overhead, firmament).
- **BANNED for next cycle:** real-world-data-feed input · three.js output · granular-scrub-of-the-take technique · event/data-sonification technique · **AND warm-candlelit-amber palette** (one break in fifteen is not a break). *(Camera/body-tracking input remains EXEMPT — rotate the body SIGNAL and the conducted PARAMETER instead, see Provocations.)*

## Ambition floor stats (last 15 prototypes)
_(criteria: A novel-technique-to-lab · B ≥3 subsystems · C specific named reference · D multi-cycle commitment · E research from last 14 days)_
- **Hit 0-1 criteria: 2** — `overhead` and `firmament`. Unchanged local minima: both clear ≥3 subsystems nearly for free off the shared loader+analyser+renderer, but neither brought a technique the lab hadn't run (`tremor` established data-sonification a week earlier) and both lean on a generic ICAD/Sonification-Handbook citation. They exist because the data lane was comfortable.
- **Hit 2-3 criteria: 10** — resonantpair, bodycast, mirrorfold, mudra, stillpoint, tremor, strand, scriptorium, ledger, reharmonize. The crowded-but-healthy middle. `reharmonize` sits here honestly at 3/5 (B+C+E) — STATE grep-falsified its own "never-shipped MIDI/pitch-shift" claim and refused to badge criterion A, which is exactly the integrity this log should keep. `ledger` is 3/5 by execution not concept: the 6th granular-scrub and 5th feed in one piece.
- **Hit 4-5 criteria: 3** — `limn` (4/5), `cantormap` (4/5: A spatialization-placement is a genuine lab-first + B camera/HRTF-panners/WebGL2/band-split + C Chowning + GestureSync + E same-day), `aureole` (4/5: A conducting the direct-to-reverberant *ratio* as the verb + B face-track/convolver-from-own-buffer/dual-path/WebGL + C Blesser & Salter + arXiv 2511.11930 + E same-day). **This is up from last verdict, and every one of the three is embodied** — the lane the last five juries wanted killed is the lane carrying all the ambition.

Notable: **criterion D (multi-cycle) is now claimed by exactly ZERO of fifteen.** `commons` — the only 5/5 in the lab and the piece three juries named the multi-cycle build — has rolled entirely off the bottom of the window, still un-deepened. The lab keeps cold-starting singular one-shots; the one thing it committed to as multi-cycle it never returned to, and now it's out of frame.

## Standouts (positive)
- **`17536-mudra`** — the one that matters: **the only recent piece Karel actually loved**, and the piece that corrects the record. A musical sign-language — MediaPipe `GestureRecognizer(2)` treats each recognized gesture as a discrete *committed* event (Open_Palm bloom / Closed_Fist choke / Pointing_Up lift / Victory octave-shimmer), two hands stacking two mudras at once, on a WebGPU compute-particle field. It's the lab's first gesture-vocabulary conductor and its first WebGPU-compute embodied piece. It earned the tap; build from it.
- **`18000-aureole`** — the most conceptually complete piece in the window: you conduct the *acoustic space* the recording lives in with your face, and the reverb IR is literally a decaying slice of Karel's own take, so lean back and the piano dissolves into a cathedral made of itself. A conducted parameter (reverberant distance) nothing else has touched, and the achromatic register that finally broke the warm streak. Its only gap is verification.
- **`17968-cantormap`** — first spatialization conductor: two hands grab, place, gather and scatter four decorrelated band-voices of one take around your head in real HRTF 3-D. Genuinely new verb; only mars are the warm palette (post-ban) and the `wip` status.
- **`17552-limn`** — still the freshest single idea (first gaze conductor, cites a within-window finding) and still `wip` a full week later. Verify it or it stays a concept.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The whole data-feed cluster (`tremor` → `overhead` → `firmament` → `scriptorium` → `ledger`)** is the window's clearest local minimum *as a run*. `tremor` was a legitimate lane-change; the four that followed mostly swapped the feed (quakes → ISS → sky → Wikipedia → money) while holding the sonification pattern fixed. Five cycles, zero love, two 0–1 builds. The variable was "what data," never "what idea."
- **`17856-ledger`** specifically: lovely to look at, but the 6th granular-scrub, 5th feed, a three.js river — all three were banned the cycle before for exactly this reason — and the piece that deepened the warm rut. A different feed is not a different idea.
- **`17344-resonantpair` / `17968-cantormap` (as a pair with the older two-hand pieces)**: the two-hand-conduct sub-lane is starting to eat itself the way the last jury warned — resonantpair and cantormap are both "two hands do X to one take." They're good; the risk is a *fourth* before other body signals (gait, multi-person, full-body) get a second turn.

## Provocations for tomorrow's dream cycle
1. **Say it in STATE: the data pivot was a mistake the vote already flagged.** The live love signal (`mudra: 1`, six-for-six embodied loves, zero for five data pieces) falsifies the "camera is dead → pivot to feeds" thesis five juries built on. The lab is right to be back in the embodied lane — do **not** relapse into a sixth data feed to "diversify." Diversify inside the loved lane instead (see #4).
2. **Verify the `wip` backlog before building a fourth camera piece.** `limn`, `cantormap`, and `aureole` all ship unverified because the agent can't point a webcam at itself headless. That's **three concepts one 30-second Karel check away from being real pieces** — higher expected value than a 4th untested build. Put a single crisp line in MORNING: *"limn / cantormap / aureole — do these track your face on your desk webcam? 30 sec each tells me whether to keep deepening or fix calibration."* Converting inventory beats minting more of it.
3. **Rotate the BODY SIGNAL, not just the parameter.** Eight camera pieces, but they cluster: two-hand (resonantpair, cantormap), face (limn, aureole), with full-body (`bodycast`) and multi-person (`mirrorfold`) each run exactly ONCE. Next embodied cycle should be full-body or multi-person or **gait** (still 0×), not a third two-hand or third face piece.
4. **The still-uncashed criterion-A verb that pairs with the loved lane: score-following.** His take follows HIM — onset/beat detection so a played phrase (from the `reharmonize` keyboard lane, or hummed) steers his recording's tempo/section. It's a genuine lab-first (real reactive accompaniment, not MIDI-in ×81), it's phone-testable, and it's the most Karel-shaped thing left un-built. Do it and criterion A is cleared on concept, not execution.
5. **Stop naming `commons` if you won't build it — or build it.** It rolled off the window un-deepened after three juries named it the multi-cycle build; criterion D is now claimed by literally nobody. Either commit a cycle-2 (a conducted-ensemble layer, a shared score) or write one honest STATE line retiring it as "single-cycle, not returning" so the log stops carrying a promise it doesn't keep.

## Karel-facing line
The lab was chasing "cameras are dead" for a week — but your ♥ on Mudra proves the opposite, so it's back conducting your music with your body and finding genuinely new verbs (place the sound in 3-D space, lean in/out to change the room); the single highest-value thing today is 30 seconds of you checking whether Limn, Cantormap and Aureole actually track your face, because three good ideas are sitting one test away from being real.
