# Concept Jury Verdict — 2026-09-24

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.**
>
> _meta: `git fetch && checkout main && pull --ff-only origin main` — fast-forwarded cleanly (38 commits behind, no diverge). Read-only on `src/app/dream/**`; no prototype touched. **The window moved by TWO from the top:** `17232-tunesignal` and `17264-vestibule` roll off the bottom; `17856-ledger` and `17904-reharmonize` roll on the top. Numeric last-15 review set: commons · timeheads · resonantpair · bodycast · mirrorfold · mudra · limn · stillpoint · tremor · strand · overhead · firmament · scriptorium · **ledger · reharmonize**. RESEARCH.md is fresh — top entry dated **today, 2026-09-24** (recording-as-playable-instrument), chaining visibly into `reharmonize`; the research→build chain is intact. Votes: STATE carries the love signal as still **flat on all 17xxx — `17200-hall` (rolled off the window ~2.5 weeks ago) remains the newest piece Karel loved.** No live server read; standing figure carried forward._

## Summary
Yesterday's ship was the first real course-correction in weeks: `reharmonize` broke off BOTH ruts at once — no camera, no data feed — and is the most Karel-specific concept the lab has produced in a month (a pianist playing chords on his own recordings). Credit that honestly, and credit the honesty in STATE: the agent grep-checked its own jury's "never-shipped" claims, found them false (MIDI-in ×81, pitch-shift ×50), and refused to claim a novelty badge it didn't earn. But the disease is unchanged. The vote is still dead at ~16 pieces, the palette pendulum just swung from a violet rut straight into a warm rut (three consecutive warm ships), the strongest concept in the lab (`commons`) has been named the multi-cycle build by TWO juries and deepened by neither, and criterion A — a technique the lab has never shipped — has not been genuinely hit since `commons`. The engine finally aimed at the right target (the one tap) and still hasn't landed it.

## Diversity audit
- Over-represented input: **camera / embodied tracking** (7×: timeheads, resonantpair, bodycast, mirrorfold, mudra, limn, stillpoint) — with **real-world-data feed** the runner-up at 5× (tremor, overhead, firmament, scriptorium, ledger)
- Over-represented output: **three.js** (5×: timeheads, mirrorfold, overhead, scriptorium, ledger)
- Over-represented technique: **granular-scrub of the real take** (6×: timeheads, resonantpair, stillpoint, strand, scriptorium, ledger) — with **event/data sonification** at 5× (tremor, overhead, firmament, scriptorium, ledger)
- Over-represented vibe: **the palette just flipped ruts.** Cool-luminous-violet is still 4× (timeheads, mirrorfold, limn, strand) and grows to ~7× merged with the near-black cosmic-sky family (stillpoint, overhead, firmament) — BUT the live story is **warm-candlelit-amber, now 4× and camping the last three ships** (resonantpair, scriptorium, ledger, reharmonize). We banned violet and the lab went all-warm. Swapping one monoculture for another is not diversity.
- **BANNED for next cycle:** camera/embodied-tracking input · three.js output · granular-scrub-of-the-take technique · event/data sonification technique · **AND both palette poles** — no cool-violet AND no warm-candlelit-amber (force a genuinely third register: rigorously achromatic/1-bit, or a hue discipline the lab hasn't run)

## Ambition floor stats (last 15 prototypes)
_(criteria: A novel-technique-to-lab · B ≥3 subsystems · C specific named reference · D multi-cycle commitment · E research from last 14 days)_
- **Hit 0-1 criteria: 2** — `overhead` and `firmament`. Still the local-minimum builds: both clear ≥3 subsystems (nearly free given the shared loader+analyser+renderer infra) but neither brought a technique the lab hadn't already run (tremor established data-sonification), and both lean on a generic ICAD/Sonification-Handbook citation.
- **Hit 2-3 criteria: 11** — timeheads, resonantpair, bodycast, mirrorfold, mudra, stillpoint, tremor, strand, scriptorium, ledger, reharmonize. The healthy-but-crowded middle. Note `reharmonize` sits here honestly at 3/5 (B+C+E): STATE correctly refused to claim A after grep-falsifying the "never-shipped" MIDI/pitch-shift claim. `ledger` is also 3/5 execution-not-concept — the 6th granular-scrub and 5th data feed in one piece.
- **Hit 4-5 criteria: 2** — `commons` (5/5) and `limn` (4/5). **Still the only two, and still the ones to extend — unchanged from last verdict because neither was touched.**

Notable: **criterion D (multi-cycle) is STILL claimed by exactly one of fifteen** (`commons`), and criterion A hasn't been cleanly hit since `commons`. The lab keeps cold-starting singular one-shots and never deepening the best of them.

## Standouts (positive)
- **`17904-reharmonize`**: the right instinct, executed cleanly. A playable instrument built entirely from Karel's own recordings — hold a triad, hear three transposed copies of his real take stack into a live choir of itself. It is off BOTH over-fished lanes (camera, data feed), it's the most Karel-shaped verb in the window (he is a pianist — let him play), it's phone-testable in the first tap, and it cashed provocation #4 (warm-only, zero violet). This is what "build for the one tap" looks like as a concept. The open question is only whether the choir sounds good by ear — which is Karel's 30-second check, not a jury call.
- **`17280-commons`**: still the best build in the lab and STILL un-deepened. Raw WebGPU compute, ~9,200 particles, N-listener co-presence woven into one figure by a coherence scalar, the only honest 5/5. Two juries have now named it the multi-cycle build. It sits waiting.
- **`17664-strand`**: honorable mention — cashed the long-form-stateful menu item (>5 min, audibly different at minute five) that three juries asked for and nobody had built.
- **`17552-limn`**: still the freshest idea (first gaze conductor, cites a within-window finding) and still `wip` — verify the camera path or it's a concept, not a piece.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **`17856-ledger`**: the clearest local minimum this window. It is genuinely lovely, but it is the 6th granular-scrub, the 5th data feed, a three.js river (all three banned last cycle for exactly this reason), AND the piece that deepened the warm rut. Its ambition is execution, not concept — the sonification pattern was set by `tremor` five cycles ago and the only new variable is "money instead of quakes." A different feed is not a different idea.
- **`17760-firmament` / `17728-overhead`**: unchanged from last verdict — the second and third sky pieces, execution over concept, generic citations. They exist because the data lane was comfortable.
- **`17344-resonantpair` / `17504-mirrorfold`**: the camera-conduct cluster eating itself — each a variation on `timeheads`/`canon`, together the reason the input tag hit 7×. What was missing: a reason to build a fourth and fifth hand-conduct piece instead of one new verb.

## Provocations for tomorrow's dream cycle
1. **You aimed at the tap and missed — so stop guessing and ASK.** `reharmonize` was the right kind of bet (singular, personal, off both ruts) and it's bet #16 with a flat vote. STATE itself concluded guessing a 17th time is the wrong move, and I agree. The highest-value action tomorrow may not be a build at all: put ONE direct question to Karel in MORNING — *"what would actually make you tap ♥?"* — and hold the build budget. One sentence from him beats another cycle of inference. This is the honest move; take it.
2. **Break the warm streak with the piece you already built.** `17920-antiphon` (achromatic 1-bit call-and-response duet) is banked RESURRECT-NEXT, one rebuild from a ship. It is exactly the rigorously-achromatic datamatics register three juries have praised (`tunesignal` was the lone palette-breaker and a standout for it), and it snaps the three-ship warm camp. If you build, build this.
3. **Finally ship a criterion-A technique — the ones NAMED and STILL at zero.** Real MIDI/OSC-**OUT** (not the ×81 MIDI-in), score-following / onset-detection reactive accompaniment, or a Tone.js modular DSP graph. **Score-following is the most Karel-shaped:** his take follows HIM — it pairs directly with the playable-instrument lane `reharmonize` just opened, and it is a genuine lab-first. Do it and criterion A is cleared on its own.
4. **Extend `commons`. Two juries, zero action.** It is the strongest concept in the lab and an explicit WebGPU multi-user room begging for a cycle-2/3 arc (a real conducted-ensemble layer, a shared score). Deepening the best thing you've built beats a seventh cold start. If a cold start still wins tomorrow, at minimum say in STATE why `commons` keeps getting deferred.
5. **Protect the one new lane you opened.** `reharmonize` is the first genuine alternative to "watch a feed" and "conduct with hands" in months — a playable instrument made of his own recordings. Don't abandon it after one. `17936-mixhand` (play-the-mix console) and `17920-antiphon` (the duet) are both one rebuild from a ship and both deepen this verb.

## Karel-facing line
The engine finally broke both ruts — Reharmonize lets you play chords on your own recordings, off the camera and off the data feed — but it traded a violet rut for an all-warm streak and the vote's still flat at ~16, so tomorrow the honest move is to just ask you what earns a tap, or ship the achromatic duet and a real never-used technique (score-following) instead of guessing a 17th time.
