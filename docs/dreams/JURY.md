# Concept Jury Verdict — 2026-09-02

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→6593caf; cycle 1214 `16720-cadencemap` at HEAD). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. THREE new ships since the 2026-09-01 verdict: `16672-scriptorium` (1212) + `16688-albumvoyage` (1213) + `16720-cadencemap` (1214); `16016-inkstroke`, `16032-headnave`, `16064-seismscore` aged out of the 15-window. RESEARCH.md is fresh (dated entries through 2026-09-02); both 09-02 entries self-flag that <30-day freshness in this score-following niche is "honestly mixed/thin."_

## Summary
The lab heard "deepen ONE real thing" and built the **same piece four times.** tonguescript → scriptorium → albumvoyage → cadencemap are four consecutive DEEP ×2 cycles on one concept — writing-plays-your-piano — and they now occupy a quarter of the entire window while driving **input, output, technique, AND vibe to the ban floor simultaneously** (keyboard 4×, DOM/typographic 4×, prosody 4×, near-dark typographic 5×). It is a genuinely coherent, genuinely good body of work — and it is also a monoculture, the exact "too similar" the mandate exists to prevent, arrived at from the opposite direction. And the ceiling **still did not move**: the ambition distribution is **`0/13/2` for the third straight day** — two honest-4s (manyreturns, spiralkeep, both untouched all window), zero verified 5s, and every one of the four manuscript pieces ships stamped "not yet reviewed on device." Seven-plus audio-forward ships are now stacked on an ear-call nobody has made.

## Diversity audit
_(last 15, numerically highest on disk: spiralkeep, roomtone, loomvoice, revenant, ringcanon, manyreturns, phosphene, driftchoir, terrafret, spectralbraid, longvigil, tonguescript, scriptorium, albumvoyage, cadencemap — inkstroke + headnave + seismscore aged out; scriptorium + albumvoyage + cadencemap entered, all three the same lineage)_

- **Over-represented input:** **`keyboard / typing` — 4× and newly at the ban floor** (tonguescript, scriptorium, albumvoyage, cadencemap). The manuscript lineage IS the input concentration this window. Behind it: `autonomous/passive` 3× (spiralkeep, manyreturns-ensemble-authoring, longvigil — WATCH, one from a ban), `tilt/orient` 2× (roomtone, phosphene). Everything else fell to 1×: pen/pointer-paint (spectralbraid — inkstroke aged out), live-external-data (terrafret — seismscore aged out, so the duplicated-category flag from yesterday **cleared on its own**), mic-secondary (loomvoice), multi-touch (revenant), gamepad (ringcanon), press-hold (driftchoir). **BANNED next cycle: `keyboard/typing` (4×).** **WATCH:** `autonomous/passive` (3×).
- **Over-represented output:** **TWO tags at the ban floor, and this is the headline.** `Canvas2D` holds at **5×** (ringcanon, manyreturns, terrafret, spectralbraid, longvigil) — a full week of hard bans and it **has not dropped a single count**; every non-manuscript ship still defaults to it. And `DOM/typographic` **calcified from 1×→4×** in ONE week (tonguescript, scriptorium, albumvoyage, cadencemap) — the surface I *celebrated last verdict as the fresh escape from Canvas2D* is now itself banned, because the lineage ran four deep on it. The escape from one rut went straight into a new one. Elsewhere, aging-out did real work: `GPU generative-field` collapsed **3×→1×** (only spiralkeep left; inkstroke + seismscore carried the counts out) and is fully available again; `three.js` 2× (revenant inhabited-room, phosphene orbited-solid); `inline-SVG/DOM art` 2× (roomtone, loomvoice); `audio-only` 1× (driftchoir). **BANNED next cycle:** `Canvas2D` (5×) **and** `DOM/typographic` (4×). **WATCH:** `three.js` (2×), `inline-SVG/DOM art` (2×).
- **Over-represented technique:** **TWO families at the floor.** `prosody / text-score cross-modal mapping` — **4×** (tonguescript, scriptorium, albumvoyage, cadencemap), the lineage's real novelty, now over-run by its own repetition. And `loops-of-one-take-against-itself` (canon + incommensurate-loop phase-drift) is **worse than it looks: 4× hard** (revenant, ringcanon, manyreturns, driftchoir) **plus all four manuscript pieces use incommensurate-loop phasing as their SUBSTRATE** — so this retired family is effectively an **8×** presence and has now been re-fed for two straight verdicts. Keep it hard-closed. Healthy 1× singletons, do NOT re-ban to extinction: convolution cross-synthesis (roomtone), voice-balance polyphony (loomvoice), harmony-carved geometry (phosphene), Barkley excitable-medium (spiralkeep), stateful memory-accretion (longvigil), two-take spectral cross-synthesis (spectralbraid), live-data→sample sonification (terrafret — no longer duplicated; seismscore aged out). cadencemap's **harmonic-tuning** (pulling slice playbackRate toward the sounding chord's triad) is the one genuinely new sub-mechanism added all window — 1×, worth extending. **BANNED next cycle:** `prosody/text-score` (4×) + `loops-of-one-take family` (4× hard / ~8× with substrate — no ninth). **WATCH:** none else at floor.
- **Over-represented vibe:** `near-dark / intimate typographic` — **5×** (driftchoir, tonguescript, scriptorium, albumvoyage, cadencemap), the new most-concentrated palette, and again the manuscript lineage is four of the five. Meanwhile yesterday's banned tyrant **dissolved**: `cool-severe ink-on-bone/black` fell **4×→1×** (only spiralkeep's cyan-teal-on-bone left; inkstroke + headnave + seismscore aged out) — genuinely off the ban floor. `monochrome-base-plus-one signal` holds **3×** (graphite+chartreuse ringcanon, ashlight+bone manyreturns, silver+jewel phosphene) — WATCH, one from a ban. `two-signal duotone` holds **2×** (blue↔magenta terrafret, copper↔verdigris spectralbraid) — WATCH, still not calcified. `polychrome` 2× (loomvoice, longvigil mineral/oxide; albumvoyage + cadencemap layer a circle-of-fifths harmonic tint over their near-dark base but the base vibe is the typographic one). Fresh 1× registers: roomtone high-key-magenta, revenant earthy. **BANNED next cycle:** `near-dark/intimate typographic` (5×). **WATCH:** `monochrome-base-plus-one` (3×), `two-signal duotone` (2×).
- **BANNED for next cycle:** `keyboard/typing input` (4×) + `Canvas2D output` (5×) + `DOM/typographic output` (4×) + `prosody/text-score technique` (4×) + `loops-of-one-take technique family` (4× hard, ~8× with substrate — keep closed) + `near-dark/intimate typographic palette` (5×). In plain terms: **the entire manuscript-lineage tag-stack is banned, and so is Canvas2D — the next ship may share none of these.** **WATCH:** `autonomous/passive input` (3×), `three.js output` (2×), `inline-SVG/DOM-art output` (2×), `monochrome-base-plus-one palette` (3×), `two-signal duotone palette` (2×). **Cleared on their own this window (aging-out did the work):** cool-severe-ink-on-bone (4×→1×), GPU generative-field (3×→1×), the duplicated live-data category (2×→1×). **Not banned (rule 10):** catalog playback as the audio source. **Still 0× after months:** WebGPU compute shader (never once), multi-user/WebRTC.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Eighteenth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 13** (roomtone 3, loomvoice 3, revenant 3, ringcanon 3, phosphene 2, driftchoir 2, terrafret 2, spectralbraid 3, longvigil 3, tonguescript 3, scriptorium 3, albumvoyage 3, cadencemap 3). The honest middle, now including all three new ships. **scriptorium** cleared #2 (catalog loader + prosody engine + form-voyage region-reader + lookahead scheduler + stereo placement + DOM form-ribbon) + #3 (Reich / Cardew *Treatise* / 2026 score-following) + #4 (multi-cycle deepen) — a real 3; no clean #1 (it's a deepen). **albumvoyage** cleared the same #2/#3/#4 and adds a genuine long-form achievement — the always-on **read-drift** makes minute-5 ≠ minute-1 *by elapsed time, not input* — but long-form-stateful is not a lab-first (longvigil + spiralkeep hold it), so it lands an honest 3. **cadencemap** cleared #2 (fetch-all-track-analysis + walkable harmonic map + harmonic-tuning + read-drift + scheduler) + #3 + #4 and has the window's most defensible new sub-mechanism (harmonic-tuning-to-chord), but frames no hard #1 — honest 3, the best-realized of the three deepens.
- **Hit 4–5 criteria — 2 (spiralkeep 4, manyreturns 4).** Distribution **`0 / 13 / 2` — identical to the last TWO verdicts.** Three ships came in, three aged out, and the top band has not moved for three days. The two 4s are the same two 4s, and they have gone **untouched for the entire window** while the lineage was deepened four times beside them. The ship-for-ship ambition trajectory is now **… 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3** — the middle is a solved, competent, stable 3; the ceiling is the whole remaining problem and four consecutive deepens of the *same concept* did not touch it. **The lesson has inverted:** last month the failure mode was breadth (a new corner every night, never deepened). This month it is depth-without-verification (one corner deepened nightly, never *listened to*). Neither reaches the 5. The missing ingredient in both is the same: **Karel's ears.**

## Standouts (positive)
- **16720-cadencemap** — the strongest realization of the manuscript lineage, and the one deepen that added a real new subsystem rather than a new view. It lifts the harmony that was buried as per-glyph hue in albumvoyage into the **headline object**: Karel's actual whole-album chord progression rendered as a walkable map, one row per track, with each written voice a marker that *walks the progression* as the read-drift migrates — and, crucially, **harmonic-tuning** makes the map audible by pulling each slice's pitch toward the sounding chord's triad. That is the lineage finally doing something you can *hear the structure of*, not just read. If any manuscript piece is the latent 4, it is this one — pending the listen.
- **16688-albumvoyage** — for the read-drift specifically: an always-on migration that walks the whole choir deeper through the *album* over 2–6 minutes with zero typing, so the piece genuinely evolves by elapsed time. That is a real long-form-generative move (Eno *77 Million Paintings* territory) and the clearest answer in the window to "minute-5 ≠ minute-1."
- **16368-manyreturns** & **16144-spiralkeep** — still the *only* two 4+ pieces in the window, and now conspicuously the deepen targets the lab has skipped for three verdicts running. manyreturns' self-building incommensurate ensemble and spiralkeep's persistent, per-album excitable medium each reached 4 by extending a real thing until it deepened. One of them — NOT via the banned loop engine — is the likeliest honest path to a verified 5.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **16672-scriptorium** (3/5) — a good piece, listed here for a structural reason, not a quality one: it is **the point where the lineage should have paused for a listen and instead accelerated.** scriptorium deepened tonguescript before anyone confirmed tonguescript *reads*; albumvoyage then deepened scriptorium, and cadencemap deepened albumvoyage — three further bets compounded on an unverified base. Concept-level lesson: a deepen of an un-listened-to piece is a guess about a guess.
- **16496-terrafret** (2/5) — still the cautionary tale even though its rival aged out. It shipped claiming "the FIRST real external-API sonification, greps to zero" while seismscore (then in-window) already sonified the same live USGS feed through his piano. seismscore has now aged out, so the *category* duplication cleared — but the false-"first" discipline lesson stands: verify against the current window, and quote the check.
- **16448-phosphene** (2/5) & **16464-driftchoir** (2/5) — two honest disciplined-2s that read well and reach for nothing (both READMEs concede no first). Fine as rested-surface breathers (three.js-solid; audio-only), not concepts to extend.

## Provocations for tomorrow's dream cycle
1. **Break the manuscript monoculture — go WIDE, not DEEP, and the next ship must share NONE of the lineage's tag-stack.** Four straight DEEP cycles put keyboard-input, DOM-output, prosody-technique and near-dark-typographic all at the ban floor at once. Tomorrow: no fifth manuscript deepen, no keyboard-primary input, no DOM/typographic surface, no prosody mapping, no near-dark typographic palette — and no Canvas2D either (it's still 5×). Run a WIDE fire with three unrelated briefs to force the spread back open.
2. **You cannot deepen your way to a verified 4 on an UNVERIFIED base — GET KAREL'S LISTEN before touching the lineage again.** Every manuscript piece (tonguescript, scriptorium, albumvoyage, cadencemap) is stamped "not yet reviewed on device," and the whole lineage rests on one ear-call — does the form-voyage *read* as a coherent journey or as arbitrary windows? That question has been deferred through four ships. It is no longer a build question; it is Karel's. Put the four in front of him back-to-back and get the verdict, then decide whether cadencemap is the 4 or the deepen continues.
3. **Canvas2D has not dropped a single count in a week of hard bans — the only "escape" was into the DOM corner, which then also maxed out.** The rut is not one surface, it's the *reflex* of picking the two most-familiar surfaces. Pick a genuinely third one: the **never-once-built WebGPU compute shader** (still 0× after months), a fresh GPU generative-field (just relaxed to 1×, available again), three.js (rested at 2×), or audio-only (1×, and driftchoir proved it works).
4. **Two 0× categories have gone uncashed for months — WebGPU compute and multi-user/WebRTC — and the lab has PROVEN it can cash a category when it commits** (installation and cross-modal both cashed in the last fortnight). Point that same commitment at one of them. A shared listening room over WebRTC, or a WebGPU-compute particle/fluid field driven by his harmony, would be the biggest single swing available.
5. **Deepen a DIFFERENT real thing than the manuscript — manyreturns or spiralkeep, the two actual 4s, untouched all window.** If the fire goes DEEP at all (after the listen in #2), aim it at the pieces with a real, named path to 5: spiralkeep's persistent excitable medium (push the per-album memory into something you can *return to and read the history of*) or manyreturns' aging ensemble — but NOT by re-running the incommensurate-loop engine, which is already at ~8× with substrate. New depth on a new axis, or a new surface entirely.

## Karel-facing line
The lab took "deepen one thing" and built the same writing-plays-your-piano piece four nights running — real, coherent work, but now a monoculture (keyboard/DOM/prosody/near-dark all maxed) with the ceiling frozen at zero verified 5s for a third straight day; tomorrow, stop deepening the manuscript, put the four in front of your ears, and swing somewhere genuinely new.
