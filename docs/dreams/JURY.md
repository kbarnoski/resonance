# Concept Jury Verdict — 2026-09-18

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.** The **2026-09-16 PRIORITY DIRECTIVE (embodied camera conducting)** is also live and governs the next several BUILD winners; this verdict is written inside it, not against it.
>
> _meta: `git fetch && checkout main && pull --ff-only` fast-forwarded cleanly (330ce6b→288ea53, 21 commits). Read-only on `src/app/dream/**` — no prototype touched. **The window moved by TWO, and the whole story with it:** `16848-tunevoice` and `16896-strata` roll off the bottom; `17312-timeheads` and `17344-resonantpair` roll on the top. Numeric last-15 review set: nodalfigure · tensethread · vaultloom · inkpressure · voiceloom · firstlook · breathline · tidemark · hearth · hall · tunesignal · vestibule · commons · **timeheads · resonantpair**. RESEARCH.md is fresh — the c1245 dive is dated **yesterday, 2026-09-17** (chained into resonantpair); freshness honesty is being logged correctly (Huì Sù flagged ~5mo, biomimetic-piano ~30–40d, labeled as current-year anchors, not dressed as <30d). Votes were last verified by the c1245 fire at **48 loved, flat, none on any 17xxx** — the jury reads no live server (read-only) and takes that figure as of yesterday's evening fire. **The one fact that governs this verdict:** the last jury said "cash the intimate two-hand duet" — and the lab did it TWICE in a row (timeheads symmetric, resonantpair asymmetric). The first of the two has now had its clean 06:30 review and drew **no love.** The prescription was filled; the first dose did not resonate. That, not the build engine, is the story._

## Summary
The lab did exactly what the last jury asked — and then did it again. It cashed the intimate two-hand duet the whole love-cluster pointed at: `timeheads` (two identical granular tape-heads) and, the same day, `resonantpair` (two *asymmetric* hands, voice vs. ground, negotiated to a lock). Both are clean, both clear the floor honestly, and resonantpair is the sharper concept of the pair. But the honest read is not celebration: `timeheads` got its first clean 06:30 review yesterday and drew **no love**, votes are still flat at 48, and — the thing a critic has to say out loud — **canon, timeheads, and resonantpair are now a three-deep run of "two hands of one person conduct one take."** The embodied directive exempts camera *input* from the diversity ban, and rightly; but it was never a license to rebuild the same duet three times. The "too similar in design and theme" wound that founded the whole ambition mandate is reopening *inside* the directive. The lab is aimed and building well — but it is one more hand-piece away from a new monoculture, and the one loved thread it has never actually touched is sitting in plain sight.

## Diversity audit
_(governs the next BUILD; camera/body-tracking INPUT is EXEMPT while the 2026-09-16 directive stands — diversify on body-signal, technique, palette instead)_
- **Over-represented input:** `autonomous / no-live-body` **6×** (tensethread [view-orbit], inkpressure, voiceloom, firstlook, breathline, tidemark) — down from 7× as strata rolled off, still the lone plurality over the ≥4 line, still aging out on its own. The live shelf stays healthy: `camera` **3×** (hall, **timeheads, resonantpair** — EXEMPT, and *supposed* to repeat under the directive), multi-user **2×** (hearth, commons), tilt/gyro **2×** (vaultloom, tunesignal), touch/curation **2×** (nodalfigure, vestibule). The autonomous ban is real but moot: the directive already forces embodied. **The live watch is not the input axis — it is that camera is now doing all the diversifying work and the *concept* underneath it has stopped moving.**
- **Over-represented output:** `Canvas2D` **4×** (voiceloom, breathline, tidemark, vestibule) — unchanged, still the one substrate at ban-tier. Everything else is out of banned-tier and evenly spread: `raw WebGL2` **3×** (hall, tunesignal, **resonantpair**), `three.js` **3×** (tensethread, vaultloom, **timeheads**), `SVG` **3×** (nodalfigure, inkpressure, firstlook — WATCH, do not make 4×), `WebGPU` **2×** (hearth, commons — rested, the freshest GPU shelf), `audio-only` **~0×** (genuinely rested). Note the two newest ships split their substrates deliberately (three.js then WebGL2) — the curation is diversifying output correctly even as concept repeats.
- **Over-represented technique:** **still nothing at 4×** — the ≥4 ban does not bite. But the discretionary picture has a NEW and important cluster: **two-hand camera-conducting** is **2×** in-window (timeheads, resonantpair) and **3-deep counting the loved `canon`** just outside it — the tightest concept-repetition run in the file. `granular` is also **2×** and both times it's *these two* pieces. Aging-down: `chord-as-subject` **2×** (nodalfigure, tensethread — dropped from 3× as tunevoice rolled off, no longer over-represented). `co-presence / N-body` **2×** (hearth, commons), `memory / accretion` **2×** (tidemark, hearth), `dynamics / touch-weight` **2×** (inkpressure, firstlook). No technique BAN on the ≥4 rule — but the discretionary call this cycle is sharp: **do not ship a third two-hand-conducting piece.**
- **Over-represented vibe:** the two-week cool-luminous monoculture finally **broke — and immediately rotated.** With tunevoice + strata gone, `cool-luminous` **dropped to 4×** (nodalfigure, tensethread, vaultloom, voiceloom); `warm` **rose to 4×** (breathline, tidemark, hearth, **resonantpair**); and `neutral` **climbed to 5×** (hall, tunesignal, vestibule, commons, **timeheads**) — crossing the exact 5× line the last jury explicitly warned against ("do NOT let neutral reach 5×"). So the palette is no longer *one*-ended; it is *evenly camped across all three temperatures* (5/4/4). That is healthier than a monoculture and worse than it sounds: there is no rested temperature left. `ink/charcoal-on-cool` **2×** (inkpressure, firstlook).
- **BANNED for next cycle:** `autonomous / no-live-body` input (6× — moot, the directive forces embodied) · `Canvas2D` output (4× — next renderer is `WebGPU` / `audio-only` / three.js, and NOT a fourth `SVG`) · **a sixth `neutral` palette** (5× — over the line; and do not push `warm` or `cool-luminous` to 5× either, both at 4×) — **the honest palette move is audio-forward (palette moot) or a temperature family the lab does not own.** No ≥4 technique ban, but **DISCRETIONARY HARD STOP: no third two-hand camera-conducting piece** (canon→timeheads→resonantpair is already 3-deep). Diversify the **body signal**, per the directive itself.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. Unchanged: a lure dressed as a build, superseded by `vestibule` (the real front door) and by the arrival of an actual signal that made the lure project moot. The local-minimum ship of the window.
- **Hit 2–3 criteria:** **10** — nodalfigure (2), tensethread (2), vaultloom (3), inkpressure (3), voiceloom (3), hall (3), tunesignal (3), vestibule (2), **timeheads (3)**, **resonantpair (3)**. Both new ships land here honestly: each declined the grep-0 #1 (granular + resonant filtering both pre-exist) and cleared via #2 (≥4 subsystems) + #3 (named refs) + #5 (same-cycle research). Correct, un-inflated floor work — and a reminder that hall sits at 3 yet is the piece Karel *loved*: the floor measures build ambition, not resonance.
- **Hit 4–5 criteria:** **4** (down from 5 — strata rolled off) — breathline (4 — grep-0 rubato-breath + bioRxiv + same-day research), tidemark (4 — first per-position collective-attention sediment), commons (4 — WebGPU N-body weave + WebRTC + hall's genuine cycle-3), and **hearth (5 — still the lab's SOLE confirmed 5/5:** novel WebGPU-compute co-presence + ≥3 subsystems + named refs + a real cycle-2 fusion of two prior firsts + same-day research). The extension bench shrank by one and gained none — neither new ship reached it, which is fine for a directive-execution cycle but worth naming: two builds, zero movement of the ceiling.

## Standouts (positive)
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: multi-user co-presence welded to cross-session memory into a room that remembers its listeners, warm on a cold muted glance via ~12 pre-seeded sessions. With Karel endorsing the co-presence lineage, still the single strongest verified-if-opened deepen target — toward intimacy, not scale.
- **17344-resonantpair** — the stronger of the two new ships and a genuinely crisp idea: two hands holding *different* powers (granular VOICE vs. resonant-filtered GROUND) over one take, mapped onto a WebGL2 interference field so the *negotiation itself is the image* — two wave systems that fuse into a warm moiré lattice at lock and tear apart at contention. Praised for the concept and for breaking the substrate-sameness trap (timeheads was three.js points; a second points piece would have looked alike). Held short of the top bench for two honest reasons: it is unreviewed, and by the lab's own log it shipped the *weaker* resolve mechanic (self-referential whole-mix centroid) while the banked `duetresolve` carried the truer voice-bus tap.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: the ~0.36 Hz breath recovered from the *signed rubato* of his real take, a measured Hz readout, not a faked prior. The best example in the window of ambition that stays honest and legible at once.
- **17232-tunesignal** — still the most strategically-right *build*: whole-viewport 1-bit static resolving into a radial figure of his music as you tilt, ears clearing at the same instant. Purpose-built for the muted-phone glance.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it is now triply superseded: by `vestibule`, and by the arrival of the very signal it existed to manufacture. The clearest local-minimum example in the window.
- **17312-timeheads** — *not* a weak build (it is the truest heir to `canon` and clean), but the clearest concept-level local minimum of the new embodied run: the **symmetric** two-hand reading, superseded within one cycle by resonantpair's **asymmetric** one, and — the fact that matters — it drew **no love** on its first clean review. It was the necessary first step; keeping a *second* symmetric-ish hand-piece in the pipeline would be the redundancy.
- **16944-nodalfigure** / **16960-tensethread** — the two weakest survivors of the now-sub-ban chord-as-subject cluster (a harmonograph that closes on consonance; a web that reddens on tension). Elegant, and the reflex the lab has correctly stopped indulging — note Karel loved *tensethread* anyway, so "weakest chord piece by ambition" and "a piece worth his heart" remain different axes; the jury owns the first.

## Provocations for tomorrow's dream cycle
1. **Two hands is cashed — TWICE. Do not build a third.** `canon → timeheads → resonantpair` is a three-deep run of "two hands of one person conduct one take." The embodied directive exempts camera *input* from the ban; it does not exempt *concept*. One more hand-piece and the founding "too similar in design and theme" complaint is back, this time wearing the directive as cover. The directive itself says to diversify the **body signal** — so do: **full-body gait→tempo**, **face/head-lean as a distinct power**, **gesture-vocabulary** (`createGestureTracker` canned gestures, nearly untouched — only 3880-conjure), or the big one below.
2. **The one loved thread never actually addressed: `duetlink` is two PEOPLE, not two hands.** Every "duet" the lab has shipped is two hands of *one* body. `duetlink` and `canon` are loved for two *people* sharing one take — and a genuine multi-person embodied piece (WebRTC / MediaPipe `numPoses>1`, two bodies conducting one take) is **0× under the directive** and answers a love head-on. This is the sharpest uncashed bet in the file. Consider seriously that canon/duetlink may be loved for the *recognition / two-people* quality, not the two-hands mechanic — in which case the last two builds optimized the wrong variable.
3. **The first dose didn't resonate — read that before doubling down.** The last jury prescribed the intimate duet; timeheads filled it and drew no love on a clean review; resonantpair gets its first review *today*. Let it stand — do not stack a third duet on top of an unread second one (the lab did exactly the right thing holding after commons; do it again here). If resonantpair also draws no love, treat the two-hand-duet hypothesis as *falsified*, not under-built, and move to provocation #2.
4. **If resonantpair IS the thread, fix the negotiation before adding to it.** It shipped a knowingly self-referential resolve (whole-mix centroid), and the banked `duetresolve` (IDEAS §1245) has the truer dedicated voice-bus tap. The cycle-2 move is folding *that* in so the lock actually reads as voice↔ground negotiation — not bolting a third power (head-lean) onto a core interaction that may not yet feel like one.
5. **Palette broke and immediately re-camped — there is no rested temperature left.** cool-luminous 4×, warm 4×, **neutral 5×** (over the line the last jury drew). The genuinely fresh aesthetic move is now either **audio-forward** (palette moot — a real test of the lab's screen bias, and `audio-only` is 0× on the output shelf) or a temperature family the lab simply does not own. Do not paint the next piece any of the three camped temperatures by reflex.

## Karel-facing line
You asked for the intimate duet and got it twice (timeheads, resonantpair) — but timeheads drew no love on review and both are two hands of *one* person; the sharper uncashed bet is two real PEOPLE sharing one take (your loved duetlink), before "two hands" becomes its own monoculture.
