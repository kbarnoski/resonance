# Concept Jury Verdict — 2026-08-24

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/hand/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded `9d634f5..e8fd62d` cleanly (cycle 1196 `16000-morphonate` at HEAD; no forced-update divergence this fire). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` per scope fence — no prototype touched._

## Summary
On the surface this is a banner fortnight: the two subsystems I've nagged about for weeks — **MIDI-out** and **multi-user** — both got cashed, and `morphonate` finally broke the "you perform your catalog live" monoculture. But I ran the grep the ambition floor mandates, and two of the three headliners are wearing borrowed medals: `answerpiano` claims to be "the lab's **first** piece that emits MIDI" (false — `291-harmonograph` and `9768-mpelace` both call `output.send`) and `duetlink` calls itself "the dream lab's **first** multi-user (peer-to-peer) prototype" (false — **~24** prior prototypes use RTCPeerConnection/BroadcastChannel, and `duetlink` literally reused the shared `_shared/peerSync` module). They **revived lanes that had gone cold for 15+ cycles** — real diversity value — but dressed a cold-lane comeback as a categorical first, which is exactly the un-grepped overclaim criterion #1 exists to stop. `morphonate` is the one honest breakout of the three, and it's the one to build on.

## Diversity audit
_(last 15, numerically highest on disk: bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin, conduct, mididuet, canon, answerpiano, duetlink, morphonate — schlieren/coherence/transcript aged out; answerpiano, duetlink, morphonate entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **6×** (bloomwork, recur, negative, spheres, answerpiano, morphonate). The tail I said was "aging out on its own" last window **regrew**: both new autonomous pieces (`answerpiano` — his take auto-plays, the visitor's synth only receives; `morphonate` — fully headless) landed back in this bucket. That is fine when the technique earns it (`morphonate` genuinely reads harmony), and a red flag when it doesn't. `camera/hands` steady at **3×** (leanin, conduct, canon). Still flatly **0×** on any embodied **webcam-head-position** input (see provocation #2). **BANNED next cycle:** `passive/self-propelling primary input` — *unless* paired with a genuine never-run subsystem.
- **Over-represented output:** `Canvas2D flat single-surface` — **5×** and still the runaway leader (toll, recur, negative, keepsake, leanin). The healthy news: **GPU-field output is now 4×** (conduct WebGPU, canon WebGPU, morphonate WebGL2, duetlink WebGL2) — that's the escape substrate compounding, exactly right; **celebrate it, do not ban it**. `three.js inhabit-a-room` has fallen to **3×** (bloomwork, spheres, vigil) — **downgrade from standing ban to WATCH**. inline-SVG/DOM rested at 2× (mididuet, answerpiano). **BANNED next cycle:** `Canvas2D flat-single-surface output`. **LIFTED to watch:** `three.js inhabit-a-room` (down to 3×).
- **Over-represented technique:** `conceptual/critical subtraction & consume-once` — **4×** (toll, negative, vigil-chord, keepsake), *correctly* unshipped in all three new pieces — it's decaying on schedule; hold the ban one more window and it clears. `conducting→time-base` 2× (conduct, canon), `concatenative corpus` 2× (recur, mididuet) — both healthy. Note for the record: `morphonate`'s **reaction-diffusion is NOT novel** — it's one of the lab's most-used techniques ever (~20 prior RD ships incl. `9-reaction-diffusion`, `9320-morphochoir`, `1972-morphosong`, `14576-reefmind`). `morphonate` is honest about this (it never claims first-RD), which is exactly why it scores clean — contrast with the two overclaimers above. **BANNED next cycle:** `subtraction/consume-once/negative-space technique`.
- **Over-represented vibe:** `warm chromatic (ember→gold→violet / warm pitch-class-hue)` — **5×** (leanin, conduct, canon, mididuet, answerpiano). I flagged this at **3×** two windows ago as "a signature turning into a habit"; it is now a **5× rut** and has fully colonized the good new lane. `conceptual/critical` as a *mood* still **4×** (toll, negative, vigil, keepsake, aging out). The one genuinely good palette shift: `cold/achromatic` is now **4×** (toll, keepsake, morphonate, duetlink) — the grayscale ban I lifted last window got taken up honestly; it is no longer rare, so stop over-rewarding it too. **BANNED next cycle:** `warm-chromatic-ember / warm pitch-class-hue palette` (the hardened rut) + `conceptual/critical-subtraction mood`.
- **BANNED for next cycle:** `passive/self-propelling primary input` (unless carrying a never-run subsystem) + `Canvas2D flat-single-surface output` + `subtraction/consume-once technique + mood` + `warm-chromatic / warm-pitch-class-hue palette`. **WATCH (not ban):** `three.js inhabit-a-room` (3×). **CELEBRATED, keep building:** `GPU-field output` (4×) + `cold/achromatic palette` (4×). **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Ninth straight fortnight with an empty floor. The floor-minimum is solved; the interesting question is the top band, not the bottom.
- **Hit 2–3 criteria — 12** (bloomwork 2, toll 2, recur 3, negative 2, spheres 3, vigil-chord 2, antiphon 2, keepsake 2, leanin 2, answerpiano 3, duetlink 2, morphonate 3). **All three new pieces land here, not in the 4/5 band** — and that is the honest headline the READMEs obscure. `answerpiano` (3/5: ≥3 subsystems + a real chord→MIDI-OUT accompaniment compiler + inline-SVG dual roll) and `morphonate` (3/5: ≥3 subsystems + named ref Turing/Gray-Scott + same-cluster Aug-2026 research) are the strongest of the band. `duetlink` is a **2/5**: it re-runs an existing subsystem (peerSync), its "first multi-user" claim is false, and I found no cited named reference in its README.
- **Hit 4–5 criteria — 3 (conduct 4, mididuet 4, canon 5).** Distribution **0 / 12 / 3**, *unchanged* from last window's `0/12/3`. **This is the number to sit with.** It *looks* like MIDI-out and multi-user should have minted two new 4/5s — but criterion #1 ("uses a technique never used in the lab; grep INDEX + all READMEs, and if it appears anywhere prior it does NOT count") disqualifies exactly those claims: MIDI-out was shipped by `291-harmonograph`/`9768-mpelace`; multi-user by ~24 priors. So the top band held flat. The lab did not get more ambitious this fortnight; it got better at *announcing* ambition it had already spent.

## Standouts (positive)
- **16000-morphonate** — the honest hero of the window, and the only new piece I'll defend without an asterisk. It's the first build in six cycles to leave the "visitor performs his catalog live" vein: a fully autonomous, never-reset Gray-Scott reaction–diffusion membrane where his **chord changes** seed splats and re-steer feed/kill climate, so a five-minute take paints an organism genuinely different at minute 5 than minute 1. Crucially it **reads harmony, not an FFT of it** (chord quality → coral vs. blob morphology), and it's honest about its lineage — it never claims first-RD, so it earns its 3/5 clean. Extend this.
- **15824-canon** — still the window's lone 5/5 (from last fortnight, still in frame): two decoupled time-bases pulled from ONE take, one per hand via MediaPipe handedness, his piano in counterpoint against itself. The model of what a real cycle-2 deepen looks like.
- **15872-answerpiano** — docked for the false "first to emit MIDI" claim, but the *build* is legit craft: a knowledge-based chord→MIDI-OUT accompaniment compiler answering his real recording out to the visitor's own hardware, on a clean inline-SVG dual piano-roll. It **did** revive a subsystem cold for 15+ cycles — I just want it labeled "revived," not "first."
- **15392-recur** — still the model on the research-freshness axis: a <30-day dated finding (Hawley, arXiv:2608.04378) → a self-composing engine whose recurrence arcs draw the movement's form.
- **15440-spheres** — still the cleanest "real external data as the *carrier*, not a sim of it": live planetary ephemeris layering his takes by aspect geometry, different every open.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15920-duetlink** (2/5) — the sharpest local-minimum of the new window, precisely *because* it looks like a headline. Strip the false "first multi-user" banner and what remains is: an existing WebRTC/peerSync subsystem (24 priors, module reused wholesale) + a Linkwitz-Riley crossover the lab has done repeatedly + two cursors. Good engineering, no new ground; the announcement is doing the work the concept didn't.
- **15696-leanin** (2/5) — unchanged from last window and it stayed in frame: lands on *every* over-represented axis at once (Canvas2D flat 5×, warm-ember palette 5×, face-detect copied wholesale from `1103-strange-face`, a single proximity scalar over one envelope over one take). Next to `conduct`/`canon` it's the safe Canvas2D draft of "camera-as-instrument."
- **15600-keepsake / 15344-toll** (2/5 each) — the two framings of the consume-once verb still sitting side by side; `keepsake` (keep-only-what-you-grab) is the sharper, `toll` (spend-to-hear-once) the earlier draft. The lane is correctly paused; these are its residue.
- **15424-negative** (2/5) — a beautiful sentence and real references (Cage 4′33″), but conceptually the fourth subtraction piece and technically a single inverted-gain envelope over Canvas2D. Concept-strong, subsystem-thin.

## Provocations for tomorrow's dream cycle
1. **Stop announcing "firsts" you didn't grep.** `answerpiano`'s "first to emit MIDI" is refuted by `291-harmonograph` + `9768-mpelace`; `duetlink`'s "first multi-user P2P" by ~24 priors including the `peerSync` module it imported. Criterion #1 of the mandate literally says *grep INDEX + all READMEs first*. Reviving a 15-cycle-cold subsystem is a legitimate, valuable move — so **say that**: "revived the MIDI-out lane, cold since `9768`," not "the lab's first." One factual overclaim per piece erodes the trust the whole log runs on.
2. **The next GENUINE 0× is the embodied webcam-head-position spatial-audio room.** `antiphon` proved catalog-as-spatial-HRTF-field works, but you *navigate it by keyboard*. Make the **webcam your head** — lean and walk to compose the mix from a field of his takes. RESEARCH flagged exactly this as grep-0 and a Tauri/installation fit; unlike this week's two, it would be a real first, and it graduates the `antiphon` idea instead of restarting it.
3. **Retire the warm palette — it's 5× now, worse than the 3× I warned about two windows ago.** Ikeda **black-white-red** is still grep-0 as a palette and your own RESEARCH (§1084, §1088) named the *datamatics* anchor and nothing has built on it. Force a palette that would look *wrong* next to everything else — that discomfort is the point. Ban warm-chromatic + warm pitch-class-hue for a week.
4. **Build ON `morphonate` before it strands as a one-off — that's the mistake you made in reverse with the conduct lane.** It's your one honest breakout: autonomous, long-form, harmony-reading. The banked `inkstroke` (Calliphony, arXiv:2608.03040, Aug 2026 — brush-as-bow over his take) is a cross-modal, named-reference partner that would deepen the same "his music grows a living surface" vein without the hands.
5. **Real-world-data-as-carrier is still 1× (spheres) after all this time.** Ship one of the banked sonification builds — `seismscore` (live global earthquake feed) or `heliosong` (NOAA space-weather sky) — external live API as composition input. You keep flagging this lane in RESEARCH and never cashing it; that's the definition of a local minimum you can see and step around.

## Karel-facing line
Banner-*looking* fortnight with an asterisk: two headliners announce "lab firsts" the grep refutes (MIDI-out and multi-user were both done years ago) — `morphonate` is the one honest breakout, so build on it, retire the now-5× warm palette, and go embodied-spatial for a real first.
