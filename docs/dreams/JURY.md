# Concept Jury Verdict — 2026-09-22

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.** The **2026-09-16 embodied-camera directive** remains on the books — but the news of this window is a THIRD consecutive ship that isn't under it. Read the Summary before invoking the directive.
>
> _meta: `git fetch && checkout main && pull --ff-only origin main` — fast-forwarded cleanly (21 commits; the dream additions are c1252 `strand` SHIP and c1253 `overhead` SHIP, plus `tramokyo:`/`journeys:`/`harvest:` work all outside the dream fence). Read-only on `src/app/dream/**`; no prototype touched. **The window moved by TWO from the top and the story is continuity, not a turn:** `17120-breathline` and `17136-tidemark` roll off the bottom; `17664-strand` and `17728-overhead` roll on the top. Numeric last-15 review set: hearth · hall · tunesignal · vestibule · commons · timeheads · resonantpair · bodycast · mirrorfold · mudra · limn · stillpoint · tremor · **strand · overhead**. RESEARCH.md is fresh and honest — the top entry is dated **today, 2026-09-22** (live-feed sonification deepens; the 2026 real-time space-weather stack) and chains visibly into `overhead`; the research→build chain is intact this cycle and last. Votes: STATE cycle 1253 records the love signal as still **flat on all 17xxx — `17200-hall` is the newest piece Karel loved, ~2 weeks / ~13 pieces ago.** The jury reads no live server and carries that standing figure forward._

## Summary
The last jury changed the lane; **this window proves the engine can hold it.** In two consecutive cycles the lab shipped `17664-strand` (a take that re-composes itself from a five-motif memory over 5+ minutes — the long-form-stateful menu item nobody had ever cashed) and `17728-overhead` (the ISS crossing the sky *right now* conducts a real take, dimming reverb-distant as it slips into Earth's shadow). Both are non-embodied, both self-verify on a cold muted phone before you tap Begin, both were named almost verbatim in the last verdict's Provocations #1 and #3. That is not one lucky ship anymore — it is a **lane with three pieces in it** (`tremor` → `strand` → `overhead`), built to the reviewability floor three juries begged for. Credit the engine loudly: it did exactly what it was told, twice. But two honest cautions keep this from being a victory lap. First, **the vote is still flat** — thirteen of the last fifteen pieces (everything after `hall`) have zero love, so the lane has proven it *builds* but not yet that it *lands*; only Karel's tap closes that loop. Second, the lane is quietly **camping one temperature**: quakes, the ISS, stillness — the three freshest reads are all cosmic/planetary, "music about the sky." The lane is real; the risk now is that it narrows into a single mood the way the camera-conduct verb narrowed into seven pieces. The task is not a new lane and emphatically not a return to the body axis — it is a human-scale feed and a first landed vote.

## Diversity audit
_(governs the next BUILD; the 2026-09-16 directive exempts camera INPUT — but the jury's read is the whole verdict: the input axis is exhausted as a diversifier and the body verb is finished at 7×)_
- **Over-represented input:** `camera / body-tracking` **7×** (timeheads, resonantpair, bodycast, mirrorfold, mudra, limn, stillpoint; 8× counting `hall`'s secondary optical-flow) — still the dominant input and formally EXEMPT under the directive, but the vote has falsified it by silence. Rested and rising honestly: `real-world-data / autonomous-no-body` **3×** (tremor, strand, overhead — the freshest and the right lane). Rested: `multi-user co-presence` **2×** (hearth, commons), `tilt/gyro` **1×** (tunesignal), `touch/curation` **1×** (vestibule).
- **Over-represented output:** `WebGL2` **4×** (hall, tunesignal, resonantpair, limn) — at ban-tier again. `Canvas2D` **4×** (vestibule, timeheads, bodycast, stillpoint) — also over. `WebGPU` **3×** (hearth, commons, mudra — the healthiest GPU shelf), `three.js` **1×** (mirrorfold — genuinely rested). Freshest and least-camped: `audio-forward + minimal-SVG on ink` **3×** (tremor, strand, overhead) — the shelf that just proved three times over it reads on a cold phone.
- **Over-represented technique:** on strict single-tag count, **MediaPipe body/hand/face tracking is 7×** — the formal ≥4 ban bites, and the discretionary read is worse: "conduct one of Karel's takes with a camera-tracked body-signal" is the same 7 pieces, the exact verb three juries drew a hard stop at. The genuinely new-and-repeatable technique is **external-live-feed sonification** (tremor, overhead = 2×) and **long-form motif-memory recomposition** (strand = 1×, the first stateful >5-min piece). Sub-clusters: `granular scrub / slice` 3× (timeheads, resonantpair, stillpoint; strand's motif-slicing adjacent).
- **Over-represented vibe:** `neutral` **4×** (hall, tunesignal, commons, timeheads) — over. `cosmic / ambient-dark` **~4×** (stillpoint, tremor, strand, overhead) — **newly camped, and this is the watch of the window**: the real-world-data lane is escaping the body verb straight into a single planetary mood. `warm` **3×** (hearth, resonantpair, bodycast — fell from 5× as breathline/tidemark aged off; the ban worked). `cool-luminous` **~3×** (mirrorfold, mudra, limn).
- **BANNED for next cycle:** `camera / body-tracking` input + `MediaPipe-conductor` technique (7×, formal AND discretionary — no ninth) · `WebGL2` output (4×) · `Canvas2D` output (4×) · `neutral` palette (4×) · **and a soft ban the lane earned itself: `cosmic / planetary` vibe** — do NOT let the next real-world feed be another slice of the sky. The honest move is a **human-scale / mundane data feed**, voiced audio-forward or on the rested `three.js` / `WebGPU` shelves, in a palette that is warm-human or palette-moot rather than another dark cosmic wash.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **0** — the floor held a second window running. `breathline` (4) and `tidemark` (4) aged off the bottom; `strand` (3) and `overhead` (3) replaced them, both clearing the floor honestly. No local-minimum ship in the window (the nearest is `vestibule` at 2).
- **Hit 2–3 criteria:** **11** — hall (3), tunesignal (3), vestibule (2), timeheads (3), resonantpair (3), bodycast (3), mudra (3), stillpoint (3), tremor (3), **strand (3 — #1 first long-form >5-min stateful piece via a live five-motif memory; #2 catalog loader + track-analysis motif carving + self-rescheduling recall loop + SVG memory ribbon = 4; #4 explicit multi-cycle long-form design)**, **overhead (3 — #2 catalog loader + keyless ISS telemetry poll + lat/long/terminator→DSP mapping + orbit SVG = 4; #5 today's 2026-09-22 dive; real-world-data lane)**. Honest, un-inflated floor work.
- **Hit 4–5 criteria:** **4** — commons (4), mirrorfold (4), limn (4 — grep-0 `createFaceTracker`, first gaze conductor), and **hearth (5 — still the lab's SOLE 5/5).** The ceiling stays high; the note the last jury made still stands — a rising ceiling in a room with a flat vote is still an unopened room — but the lane's three self-verifying pieces are the first built specifically to be walked under without Karel in frame.

## Standouts (positive)
- **17728-overhead** — the freshest ship and the proof the lane is a lane: the one crewed outpost overhead conducts a real take as it actually passes, and you *hear* it cross into Earth's shadow (bright/present → low-passed/reverb-wide). One-line hook a stranger repeats, alive and moving the instant you look, and self-verifying via a sample orbit before Begin. This is Provocation #3 from the last verdict, executed clean.
- **17664-strand** — the long-form-stateful lane finally cashed: motifs held in a memory of five and re-composed over 5+ minutes so minute five differs audibly from minute one, with a watchable SVG ribbon and zero camera. Provocation #1, executed. The rarest floor criterion in the whole file (multi-cycle / long-form state) built into one honest piece.
- **17616-tremor** — still the strategic keystone and the reviewability template the lane was cast from: every earthquake on Earth right now voicing a fragment of his take, self-demoing offline with zero network or permission. The origin of the only lane the lab has that reads on a cold glance.
- **17168-hearth** — still the sole 5/5 and the standing ceiling: co-presence welded to cross-session memory into a room that remembers its listeners. If the lab ever builds embodied again, deepen THIS toward intimacy (two listeners), not another camera-conduct instrument.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17264-vestibule** — a front-door / curation lure is still not a concept (honest 2/5, the window's lowest), and it is now fully superseded: it was gesturing at reviewability-by-construction, and `tremor`/`strand`/`overhead` have since *built* that instead of framing it. The local-minimum shape to stop reaching for — don't build scaffolding for unreviewed work; build work that reviews itself.
- **17536-mudra** — still the least-reviewable ship in the set: its own README states there is no autonomous demo, so it cannot be seen at all without a camera on live hands. `wip`, un-loved, two windows deep as the reviewability anti-example. The lane has now shown three times how cheap the self-demo fix is; nothing should ship like this again.
- **The camera-conduct cluster as a whole** (timeheads · resonantpair · bodycast · mirrorfold · mudra · limn · stillpoint = 7) — no single one is a bad build, but seven "MediaPipe body-signal conducts one take" pieces in a row, none of them loved, IS the local minimum the lab just climbed out of. The pruning note is directional, not per-slug: do not descend back into it.

## Provocations for tomorrow's dream cycle
1. **The lane is real — now widen its TEMPERATURE, not its count.** Three ships and all three are cosmic/planetary (quakes, the ISS, stillness). The next real-world feed must be **human-scale or mundane**: live transit arrivals for one named line, currency/interest ticks, a language-usage or search-trend curve, tide-and-crowd at a specific beach, a city's live noise level. "Music about something other than the sky." Same reviewability floor, a different world.
2. **No NINTH camera-conduct piece.** The body verb is 7× and the flat vote has falsified it by silence (last love `hall`, ~2 weeks). The 2026-09-16 directive exempts camera INPUT but cannot resurrect a verb the audience has ignored thirteen pieces running. Leave the banked body-verb seeds banked.
3. **Cash the multi-cycle criterion nobody banks — deepen `strand`.** It proved long-form state works and is the rarest floor criterion in the file, yet no piece has ever done a cycle-2 on itself. Give the memory a second take to cross-pollinate (shallow motifs from one recording, deep from another), or an audible arc the ribbon narrates, before opening a fourth lane.
4. **Reviewability is the floor, and the standing debt is the three WIP camera pieces** (`mudra`, `limn`, `stillpoint`) that still can't demonstrate themselves on a muted phone. Don't add a fourth. If anything embodied ships at all, it must self-demo cold (the `tremor` sample-fallback template) — or it doesn't ship.
5. **Output/palette: `WebGL2` (4×), `Canvas2D` (4×) and `neutral` (4×) are all banned this cycle.** The rested shelves are `audio-forward / minimal-SVG on ink` (the lane already lives here) and, for visual, `three.js` (1×) or `WebGPU` — NOT a third dark cosmic wash. And genuinely test the one menu item the lab has never touched: **embedded / non-screen** — an audio-only or near-bare piece that survives a muted glance by being about the sound, not the light.

## Karel-facing line
The lab did exactly what the last three juries ordered — three non-embodied, self-verifying pieces in a row (earthquakes, a take that remembers itself, the ISS overhead) — so today's tone is earned momentum, not alarm; keep the lane, but point the next feed at something human instead of the sky, and let's finally get one of these actually opened.
