# Concept Jury Verdict — 2026-08-28

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (cycle 1203 `16256-revenant` at HEAD, plus the standing batch of Karel-directed out-of-fence work — middleware/tripwire, anon-token migrations, OG-image routes, now-playing-bar — none of it dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. One new ship since the 2026-08-27 verdict: `16256-revenant` (1203); `321-antiphon` aged out of the 15-window._

## Summary
Yesterday I said the same thing I'd said for two weeks — the ceiling, not the spread — and named the hard job by name: **build a second 5/5, extend `canon`.** Today's single ship, `revenant`, did the thing I've been unable to get for three weeks: it **aimed at the ceiling.** It's `canon`'s cycle-2, it took the grain-free recorded-replay arm I pointed at, it clears all seven of yesterday's bans at once, and it's the first ship in a month pointed at the top band instead of a fresh corner. Credit where due — the *aim* finally turned. But the arrival didn't. `revenant` landed a disciplined **3/5**, not the 5 I asked for, and it's a *new folder* rather than a deepening of canon in place. So the distribution is `0 / 11 / 4` again — **flat for the third straight window** — one lone 5/5, still `canon`, now formally "extended" by a piece that doesn't reach it. This is a better failure than last window's: last time breadth churned and nothing pointed up; this time something pointed up and fell short. The task tomorrow is no longer "aim higher," it's "**finish the shot** — push revenant to the 5, don't bank it as another honest 3 and move on."

## Diversity audit
_(last 15, numerically highest on disk: keepsake, leanin, conduct, mididuet, canon, answerpiano, duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice, revenant — antiphon aged out; revenant entered)_

- **Over-represented input:** `camera/webcam-as-primary` — **4×** (leanin, conduct, canon, headnave), unchanged and still over the floor. `passive/self-propelling — no load-bearing human verb` — **4×** (answerpiano, morphonate, seismscore, spiralkeep). `revenant`'s **multi-touch drag** is a genuine active, non-camera, non-passive verb — credit, and it's a still-rested one (multi-touch is ~1× as a primary bodily verb). **BANNED next cycle:** `camera/webcam-as-primary` (4×) **+ `passive/self-propelling primary input`** (4×, unless carrying a genuinely-grepped never-run subsystem — the `spiralkeep` persistence exemption).
- **Over-represented output:** `GPU generative field (WebGL2/WebGPU shader you watch)` — **7×** (conduct, canon, morphonate, inkstroke, seismscore, spiralkeep, duetlink) — still the single most-used surface. `inline-SVG/DOM as the primary surface` — **4×** (mididuet, answerpiano, roomtone, loomvoice) — the escape hatch I flagged last window as hardening into the next shader; it held at 4× only because revenant went to three.js instead. **But watch the new tell: `three.js inhabited room` is now the surface the *ceiling-aimed* ships flee into** — headnave + revenant = 2×, and revenant is the second serious piece to land there. It's rested today; it will not be if the next canon-deepen also picks it. **BANNED next cycle:** `GPU generative-field-as-the-visual` (7×) **+ `inline-SVG/DOM as the primary surface`** (4×) **+ WATCH `three.js inhabited room`** (2×, one ship from becoming the escape-hatch rut). Genuinely 0× and owed: **audio-only / embedded / haptic** (a piece with *no primary visual*), **projection/installation**.
- **Over-represented technique:** `granular grain-triggering of his take` — **6×** (conduct, canon, mididuet, inkstroke, seismscore, keepsake). Unchanged, still the default *audio* engine, still banned. Healthy 2–3× clusters not yet at the ban floor: `dual-voice / band-split crossover` — **3×** (canon, duetlink, revenant), `conducting→time-base` 2× (conduct, canon), `reaction/excitable-field morphogenesis` 2× (morphonate, spiralkeep). Freshly-cashed and now 1× — do NOT re-ban into extinction: convolution cross-synthesis (roomtone), whole-take polyphony (loomvoice), gesture record/replay automation (revenant — new this window). **BANNED next cycle:** `granular grain-triggering as the audio engine` (6×). **Note the crossover creep** — band-split is at 3× and climbing (revenant added it); one more and it joins the ban list.
- **Over-represented vibe:** `warm chromatic (ember→gold / warm pitch-class-hue)` — **5×** (leanin, conduct, mididuet, canon, answerpiano), all in the aging tail under a standing ban. The deeper cluster is still `cool-severe on bone-or-black` — inkstroke (indigo/bone), seismscore (Ikeda B-W-R), spiralkeep (cyan-teal/bone), keepsake (cyan-white/black), duetlink (cold achromatic), morphonate (cyan/ink), headnave (dark severe) — **7×, the actual house style.** The three fresh registers are all rested one-offs: `roomtone` high-key light+magenta (1×), `loomvoice` polychrome (1×), `revenant` **earthy-organic** loam/moss/olive/bone (1×, new — exactly the "genuinely earthy/organic non-ink palette" I asked for). **BANNED next cycle:** `warm-chromatic / warm pitch-class-hue` (5×) **+ `cool-severe-ink-on-bone/black`** (7×, the standing house rut). **Encouraged:** the three fresh registers — keep any of them under 2×.
- **BANNED for next cycle:** `camera-primary` (4×) + `passive/self-propelling primary input` (4×) + `GPU generative-field output` (7×) + `inline-SVG/DOM primary surface` (4×) + `granular grain-triggering` (6×) + `warm-chromatic palette` (5×) + `cool-severe-ink-on-bone/black palette` (7×). **WATCH (not yet banned):** `three.js inhabited room` (2×), `band-split/dual-voice crossover` (3×). **Not banned (rule 10):** catalog playback as the audio source. **Explicitly rested / encouraged:** Canvas2D, audio-only/embedded/haptic (still 0×), projection; gamepad / multi-touch active verbs; physical-model resonator / spectral cross-synthesis / additive resynthesis (still-0× non-grain audio); the earthy/high-key/polychrome fresh palettes (keep each <2×).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Thirteenth straight fortnight with an empty floor. Solved; stop watching it.
- **Hit 2–3 criteria — 11** (keepsake 2, leanin 2, answerpiano 3, duetlink 2, morphonate 3, inkstroke 3, headnave 3, seismscore 3, roomtone 3, loomvoice 3, **revenant 3**). The honest middle, and where the new ship landed *again*. `revenant` cleared **#2** (five subsystems: catalog loader + Linkwitz-Riley band-split crossover + gesture record/loop/replay automation state-machine + multi-touch input + three.js point-cloud room) **+ #3** (named refs: The Living Looper NIME'23, arXiv:2604.07612, Reich phase pieces) **+ #4** (explicit cycle-2 of canon, seeded in canon's own README). It **honestly declined #1** — gesture record/replay has grep priors in the lab — which is the right call and the disciplined-3 model, but it's still a 3.
- **Hit 4–5 criteria — 4 (conduct 4, mididuet 4, canon 5, spiralkeep 4).** Distribution **`0 / 11 / 4`** — **flat versus the last two windows' `0/11/4`.** revenant (3) replaced antiphon (2) one-for-one in the honest middle; the top band did not gain a piece. **Three windows, one 5/5.** The difference this window is that the *attempt on the ceiling was finally made* — revenant is genuinely `canon`'s cycle-2, not a fresh axis — but the extend produced a 3, not a 5, and it produced it in a new folder. `canon` is still the lone peak; its deepen is now begun but unfinished.

## Standouts (positive)
- **16256-revenant** — the piece of the window for its *aim*, not its altitude. After three weeks of me naming canon's deepen and getting fresh corners instead, this ship finally took it: the grain-free **recorded-conducting replay**, so the canon literally performs itself back — a baked gesture curve auto-performs the whole two-voice canon on load with zero input, then you take over the second voice against your own recorded first pass. It clears all seven bans, brings a genuinely earthy palette and an active multi-touch verb, and is scrupulously honest about declining "first." **This is what pointing at the ceiling looks like — now it has to arrive.**
- **15824-canon** — still the lone genuine 5/5, and now, for the first time, *formally extended* — but by a 3, not a peer. It remains the most valuable object in the lab: the proof that a decoupled-time-base instrument reads as a real 5, and the target revenant is still climbing toward. Its deepen is begun, not done.
- **16144-spiralkeep** — holds as the top-band exemplar of the honest play (Barkley excitable medium + real persistence, no "first" claimed, a real 4). Still the template alongside canon: extend a real 4/5 honestly until it *becomes* a 5.
- **16208-loomvoice / 16160-roomtone** — last window's disciplined-3 pair, aging gracefully. Both remain the model for *breadth done right* (non-grain audio, off-shader, fresh palette). The lab has fully internalized this move; the whole point of tomorrow is that it now needs the *other* move — finishing a deepen into the top band.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15696-leanin** (2/5) — the local-minimum exemplar, named again: camera-primary + Canvas2D-flat + warm-ember + a single proximity scalar as the entire control surface. Lands on the most over-represented axes at once and adds nothing `conduct`/`canon` didn't. The floor we're climbing off.
- **15920-duetlink** (2/5) — strip the "first multi-user" banner (the lab has ~24 multi-user priors) and it's a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors. Competent plumbing, not a big concept — and its crossover is now part of the 3× band-split creep worth watching.
- **15600-keepsake** (2/5) — the consume-once verb, correctly paused, aging toward the window edge. Residue, not a live problem. Its ACT-R graded-decay deepen is the only lift and it isn't a priority against finishing canon.

## Provocations for tomorrow's dream cycle
1. **Finish the shot — push `revenant` to the actual 5, don't bank it and start a new corner.** The aim is right; the altitude isn't. revenant's own README seeds the path: **stack 2–3 captured passes into a self-built ENSEMBLE of ghosts**, each a different band or bar-length phasing out against the others — that's true authored Reich drift, and it's the subsystem that turns a 3 into a 5 (a piece genuinely different at minute 5 than minute 1, with real state and memory — ambition #1 territory if the ensemble scheduler is grep-0). Or **variable loop bars (8/12/16s)** so the canon breathes. **A deepen that ships as another 3 is the trap** — the whole verdict this window is: the lab can *aim* at the ceiling now, prove it can *reach* it.
2. **If you deepen revenant, do NOT stay in the three.js room.** Two ceiling-aimed ships in a row have now landed there (headnave, revenant); a third makes it the escape-hatch rut the shader and SVG both became. Push the ensemble onto a rested surface — **Canvas2D**, or the still-0× **audio-only / haptic** lane (an ensemble of ghost-canons is a piece that could carry with *no primary visual* at all — test the screen bias directly).
3. **Ship `refrain` — the banked gamepad runner-up (IDEAS §1203) is the rotation you keep asking me to want.** It's the same self-answering canon rendered as a **Steve Reich phase-score** (two loop-heads drifting in/out of phase) and conducted by **Gamepad** — the single most-rested un-cashed active verb (`15904-gamepad` banked; gamepad ~1× vs multi-touch's climb). It's built, clean, and it advances the *same* canon concept on a fresh input + a cleaner analytical surface. Two shots on the ceiling for the price of one already-built folder.
4. **The still-0× non-grain audio move is a physical-model resonator — take it.** convolution (roomtone), whole-take polyphony (loomvoice), gesture-replay (revenant) are all freshly cashed; the band-split crossover is now at 3× and creeping toward a ban. The un-run frontier: **his take excites a Karplus-Strong / modal resonant body** (his recording as the exciter, not the grain source), **spectral freeze / cross-synthesis** (the banked `fold` — two takes fused in the frequency domain), or **additive resynthesis from his partials**. Any keeps "stop chopping his take into grains" advancing on a genuinely new axis.
5. **Hold the palette line — three fresh registers now, all still one-offs.** high-key-magenta (roomtone), polychrome (loomvoice), earthy-organic (revenant) are the three registers I asked for and they're each at 1×. That's the win; don't spend it — if tomorrow reaches for any of the three it becomes 2× and starts the same calcification I'm warning about with three.js. Warm and cool-ink are both banned; find a *fourth* register wrong next to all of them (a monochrome-plus-one-signal-color, a metallic/leaf register, a single saturated hue nobody's used) or keep an existing fresh one strictly under 2×.

## Karel-facing line
For the first time in three weeks a ship aimed straight at the ceiling — `revenant` is `canon`'s cycle-2, the deepen I've named all month — but it landed a disciplined **3/5**, not the 5 I asked for, so the distribution is flat at **0/11/4 for a third window running**: the aim finally turned, now it has to actually *reach* — so tomorrow, **finish revenant into a real 5 (an ensemble of self-answering ghosts) and ship the banked gamepad `refrain`**, don't bank another honest 3 and start a fresh corner.
