# Concept Jury Verdict — 2026-09-20

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.** The **2026-09-16 embodied-camera directive** is still live and this verdict is written inside it — but see the Summary: the directive mandates embodied *input*, and the lab has begun wearing it as cover for a single *interaction verb*.
>
> _meta: `git fetch && checkout main && pull --ff-only origin main` — fast-forwarded 6 commits to `origin/main`; read-only on `src/app/dream/**`, no prototype touched. **The window moved by TWO and the story hardened:** `16992-vaultloom` and `17024-inkpressure` roll off the bottom; `17536-mudra` and `17552-limn` roll on the top. Numeric last-15 review set: voiceloom · firstlook · breathline · tidemark · hearth · hall · tunesignal · vestibule · commons · timeheads · resonantpair · bodycast · mirrorfold · **mudra · limn**. RESEARCH.md is fresh and honest — the top entry is dated **today, 2026-09-20** (Frontiers gaze paper, 2026-09-14, 6 days old) and chains visibly into `limn`; the research→build chain is intact every cycle. Votes: MORNING.md (cycle 1249) states plainly that **all six embodied camera pieces have zero votes — `hall` is the last piece Karel loved.** The jury reads no live server and takes that as the standing figure. **The one fact that governs this verdict:** the last two juries drew a discretionary hard stop at the camera-conduct verb, ordered the lab to make `mirrorfold` reviewable rather than add to it, and to change *lane* not just *verb* if the votes stayed flat. The lab did the opposite — it shipped TWO more camera-conduct pieces (`mudra`, `limn`), both `status: wip`, neither the ordered `mirrorfold` front door. The build engine is superb. It is aimed at a wall._

## Summary
Credit first, because it is real: the lab's build ambition is the highest this file has measured — the ambition-floor distribution moved UP again (six of fifteen now clear 4–5 criteria, only one sits at the local minimum), and `limn` honestly claims a genuine grep-0 novel technique (first `createFaceTracker` conductor) while cashing a 6-day-old paper head-on. Nobody is phoning in a build. But a critic has to name what the two juries before this one already named, louder each time, and which the lab has now driven straight past: **"a camera watches your body and one of Karel's takes responds" is the lab's entire worldview — six pieces running (timeheads · resonantpair · bodycast · mirrorfold · mudra · limn), plus BOTH banked seeds (`cadence`, `vane`), and it has drawn exactly zero votes.** The input axis has done heroic diversifying work — two hands → full body → two people → hand-gesture → gaze — but it is rearranging deck chairs. The *verb* is one verb. Worse, the last three ships (`mirrorfold`, `mudra`, `limn`) are all `status: wip` and all self-unverifiable in the cloud — they need Karel's webcam (or a second person) to know if they even work. So the lab is now shipping unverifiable pieces, in a category with no votes, while explicitly declining the reviewability work the last jury ordered. The build loop is a marvel; the review loop has been shut for ~2 weeks and the gap between "shipped" and "known to work" is now three deep and widening. This is no longer a diversity problem. It is a validation problem, and only Karel — or a piece built to be seen without him — can fix it.

## Diversity audit
_(governs the next BUILD; the 2026-09-16 directive exempts camera INPUT — but the jury's read is the whole verdict: the directive was never a licence for one interaction verb)_
- **Over-represented input:** `camera / body-tracking` **7×** (hall, timeheads, resonantpair, bodycast, mirrorfold, mudra, limn) — the most concentrated single-axis run the file has ever held, and technically EXEMPT under the directive. `autonomous / no-live-body` **4×** (voiceloom, firstlook, breathline, tidemark) — over the line but aging out as the window advances. Rested: `multi-user co-presence` **2×** (hearth, commons), `tilt/gyro` **1×** (tunesignal), `touch/curation` **1×** (vestibule). **The input axis is not the watch — it is doing all the diversifying by design. The watch is what the camera is pointed AT, and that has not moved.**
- **Over-represented output:** `Canvas2D` **4×** (voiceloom, breathline, tidemark, vestibule) — still at ban-tier. `WebGL2` also crossed to **4×** (hall, tunesignal, resonantpair, limn). Rested and freshest: `three.js` **fell to 3×** (timeheads, bodycast, mirrorfold — the last jury's ban worked, credit the lab), `WebGPU` **3×** (hearth, commons, mudra — the healthiest GPU shelf), `SVG` **1×** (firstlook), and `audio-only` **0×** — genuinely, conspicuously, still-rested-after-months.
- **Over-represented technique:** on a strict single-tag count, **still nothing at 4×** — the formal ≥4 ban does not bite. But the honest discretionary read is the whole verdict: **"conduct one of Karel's takes with a camera-tracked body-signal" is now 6×** (timeheads, resonantpair, bodycast, mirrorfold, mudra, limn) — up from the 4× the last jury already called a hard stop. Sub-clusters: `granular scrub` **2×** (timeheads, resonantpair), `memory / accretion` **2×** (tidemark, hearth), `co-presence / N-body` **2×** (hearth, commons), `lure / front-door` **2×** (firstlook, vestibule). The body-signal diversified; the verb metastasized.
- **Over-represented vibe:** `warm` **6×** (voiceloom's parchment-sepia-gold, breathline, tidemark, hearth, resonantpair, bodycast) — the worst-camped temperature, well over the line. `neutral` **4×** (hall, tunesignal, commons, timeheads) — also over. `cool-luminous` **3×** (mirrorfold, mudra, limn — the newest run pivoted here, so it is warming toward the line, not rested). `ink/charcoal` **1×** (firstlook), `mixed` **1×** (vestibule). The one genuinely rested aesthetic remains **audio-forward** (palette-moot; `audio-only` 0×).
- **BANNED for next cycle:** `Canvas2D` output (4×) · `WebGL2` output (4×) · `warm` palette (6×) · `neutral` palette (4×) — **the honest move is audio-forward (palette-moot) or the rested `three.js`/`WebGPU` shelves, NOT a reflex warm wash.** `autonomous` input is over but aging out; `camera` input is over but formally EXEMPT. No ≥4 technique ban formally, but **DISCRETIONARY HARD STOP, third time drawn and now non-negotiable: no SEVENTH "camera-tracked body conducts one take" piece — not gait, not head-turn, not any body-signal.** The input axis is exhausted as a diversifier. Change the verb, or change the lane.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook` (a lure dressed as a build; triply superseded — by `vestibule`, and by the arrival of the signal it existed to manufacture). Still the sole local-minimum ship of the window.
- **Hit 2–3 criteria:** **8** — voiceloom (3), hall (3), tunesignal (3), vestibule (2), timeheads (3), resonantpair (3), bodycast (3), **mudra (3 — #2 gesture-recognizer + effect graph + WebGPU particle field; #3 Gesture2Music / NIME "one gesture, one event"; #5 same-day 2026-09-19 dive; honestly declined a grep-0 #1 since gesture-vocabulary pre-exists in `3880-conjure`)**. Un-inflated floor work throughout — note again that `hall` sits at 3 yet is the piece Karel *loved*: the floor measures build ambition, not resonance.
- **Hit 4–5 criteria:** **6** (up one) — breathline (4), tidemark (4), commons (4), mirrorfold (4), **limn (4 — the strongest floor claim of the window: a genuine grep-0 #1 [`createFaceTracker`, first gaze conductor] + ≥3 subsystems [face tracker + dry/wet bandpass-highshelf-peaking-lowpass graph + WebGL2 spectrum lens] + named ref [Frontiers gaze paper] + #5 recent research, all honest)**, and **hearth (5 — still the lab's SOLE 5/5).** The ceiling is genuinely high and rising. That is exactly why the flat vote is the story: the problem is not ambition, it is that a rising ceiling nobody has walked under is a ceiling in an empty room.

## Standouts (positive)
- **17552-limn** — the standout of the window on craft and honesty. It is the freshest single idea the lab has shipped in weeks — *where you look changes what you hear*, a one-line hook a child could repeat — built on a genuine grep-0 technique and a paper 6 days old, and it is the ONLY one of the three wip ships that made itself reviewable: the labelled **ghost-gaze auto-demo** sweeps the lens with no camera and no permission, so the idea reads at a muted glance. That is the reviewability discipline the last jury begged for — applied to the wrong piece (a seventh camera-conduct build) but applied *well*. If any 17xxx earns Karel's first tap, it is this one.
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: multi-user co-presence welded to cross-session memory into a room that remembers its listeners, warm on a cold muted glance via pre-seeded sessions. Still the single strongest verified-if-opened deepen target — toward intimacy, not scale, and NOT via another camera.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: the ~0.36 Hz breath recovered from the *signed rubato* of his real take, a measured Hz readout, no faked prior. The best example in the file of ambition that stays honest and legible at once — and, tellingly, it is autonomous: it verifies itself.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17536-mudra** — the pruning-pattern exemplar of this window. Not a weak build (honest 3/5, clean effect graph, real gesture-vocabulary idea), but it is the clearest case of "diverse body-signal, identical verb, and now un-reviewable BY DESIGN": its README states outright *"There is no fake autonomous demo"* — so unlike `mirrorfold` (ghost-demo) and `limn` (ghost-gaze), it cannot be seen at all without a camera pointed at hands. Shipping the *least* reviewable piece of an *unreviewed* run, one cycle after two juries ordered reviewability, is the local minimum the lab is actually sitting in — not the concept, the choice.
- **17104-firstlook** — still the clearest local-minimum example in the file: a lure is not a concept, and it is triply superseded.
- **17312-timeheads** — still the concept-level local minimum of the embodied run: the **symmetric** two-hand reading, superseded within one cycle by `resonantpair`'s asymmetric one, and drew no love on its first clean review. The necessary first step of a run that should have ended four pieces ago.

## Provocations for tomorrow's dream cycle
1. **STOP building the verb. This is the third jury to say it and the first to say it as a floor, not a nudge: do NOT ship a seventh "camera-tracked body conducts one take" piece.** That includes both banked seeds — `cadence` (sway → time-base) and `vane` (head-turn → soundstage) are the *same verb* wearing an audio-forward coat. The input axis (hands → people → gesture → gaze → gait) is fully mined. A seventh entry is not diversity; it is the monoculture with a new sensor.
2. **The next fire's ONLY job is reviewability — the last jury ordered it and the lab skipped it twice.** Three consecutive `wip`, self-unverifiable ships (`mirrorfold`, `mudra`, `limn`) is a process emergency, not a style note. Build ONE of: (a) the `mirrorfold` **solo / auto-demo front door** already ordered (its "1 of 2, waiting" and ghost paths exist — surface them), or (b) an **embodied-run vestibule** — a single muted, no-permission, glance-legible threshold showing all six camera pieces looping at once, so the whole run finally becomes openable on a cold phone. `mudra` especially shipped with no demo and must get one. Ship nothing that needs Karel's webcam to know if it works.
3. **Read the vote before the next build, and if it is still flat on all 17xxx, change LANE — Karel asked for exactly this.** MORNING cycle 1249 records his own open question: *"If none of these lands for you, tell me to change lane, not just the verb."* The silence is now long enough to answer it FOR him: `hall` was the last love, ~6 pieces / ~2 weeks ago. Treat the embodied-conducting hypothesis as **falsified-by-silence** and have a genuinely non-embodied fallback staged — autonomous, self-verifying, glance-legible (the `breathline`/`tunesignal` mould), the kind of piece that can earn a vote without a camera or a second body in the room.
4. **The rested escape is audio-forward — but build it as the reviewability piece, not verb #7.** `three.js` fell to 3× (the ban worked), but `Canvas2D` and `WebGL2` are both at 4× and `warm`+`neutral` palettes are both over. `audio-only` output is 0×, palette-moot — the single move that clears the substrate AND palette bans at once. Just remember it does not fix the monoculture on its own: an audio-forward piece that still says "sway your body and the take responds" is still the verb. Pair audio-forward with a NON-body trigger (autonomous evolution, real-world data, a long-form stateful piece) to fix both at once.
5. **Deepen `hearth` toward intimacy, or `limn` toward legibility — do NOT open a new front.** If the lab must build (not just make-reviewable), the two pieces worth extending are the 5/5 (`hearth` — co-presence + memory, toward two intimate listeners, no camera) and the one fresh idea that reads (`limn` — its ghost-gaze demo is the template; make the gaze mapping so obvious a first-timer gets it in 3 seconds). Both are deepenings of proven concepts, not a seventh sensor on the same verb.

## Karel-facing line
The lab is building beautifully but at a wall: six pieces running now it's the same move — a camera watches you, one of your takes answers — none has a vote, and the last three can't even test themselves without your webcam; so today the honest ask isn't a seventh, it's you opening ONE (try `limn` — your eyes conduct it, and it runs before you turn the camera on) and telling me whether this whole lane is worth another night.
