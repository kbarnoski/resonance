# Concept Jury Verdict — 2026-08-30

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/gamepad/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→676817a; cycle 1207 `16464-driftchoir` at HEAD, plus the standing batch of Karel-directed out-of-fence work — archive-catalog prebuild/`scripts/generate-dream-catalog.mjs`, `next.config.ts`, paginated `/dream/archive`, now-playing-bar — none dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. Two new ships since the 2026-08-29 verdict: `16448-phosphene` (1206) + `16464-driftchoir` (1207); `15760-conduct` and `15808-mididuet` aged out of the 15-window._

## Summary
The lab did exactly what I asked and it worked: `phosphene` broke the canon monoculture with a genuinely un-adjacent harmony-carved crystal, and `driftchoir` **finally cashed the audio-only/haptic lane I named by number for a month and the lab declined three times.** Camera, granular, warm-chromatic and inline-SVG all fell below the ban floor this window — the surface/palette rut I've hammered on is measurably breaking up. **But the win has a shadow, and it's the whole verdict: the diversity came at the top band's expense** — the two newest ships both cleared the floor at the bare **2/5 minimum**, the two honest-4s that aged out (conduct, mididuet) were replaced by two honest-2s, and the top band slid `0/10/5 → 0/12/3`. And underneath the fresh surfaces, the *audio* has quietly become the monoculture: **five of the last fifteen pieces** — canon, revenant, ringcanon, manyreturns, **and now driftchoir** — are the same one idea, *one of his takes looped against a phase- or time-shifted copy of itself.* I banned the canon *mechanism* last week; driftchoir was the sanctioned last cash of the engine on a new output. It's cashed. **Tomorrow the AUDIO relationship must change — not one-take-against-itself — and the next ship must swing for a 4, not clear the floor.**

## Diversity audit
_(last 15, numerically highest on disk: canon, answerpiano, duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice, revenant, ringcanon, manyreturns, phosphene, driftchoir — conduct + mididuet aged out; phosphene + driftchoir entered)_

- **Over-represented input:** `passive/self-propelling — no load-bearing human verb` — **4×** (answerpiano, morphonate, seismscore, spiralkeep), holding exactly at the ban floor. **Genuine progress on the active-verb axis:** both new ships carry real load-bearing verbs — `phosphene` **tilt/drag-to-orbit** and `driftchoir` **press-and-hold-to-bloom** — and with `conduct` aged out, **`camera/webcam-primary` dropped to 2×** (canon, headnave), off the WATCH list entirely. The passive habit is the *only* input rut left, and it's carried by four older pieces, not the recent run. **BANNED next cycle:** `passive/self-propelling primary input` (4×, unless carrying a genuinely-grepped never-run subsystem — the `spiralkeep` persistence exemption). **No input on WATCH** — camera fell out; the recent verbs (tilt, press-hold, ensemble-authoring, gamepad) are healthily spread at ≤1× each.
- **Over-represented output:** `GPU generative field (WebGL2/WebGPU shader you watch)` — **6×** (canon, duetlink, morphonate, inkstroke, seismscore, spiralkeep), down from 7 (conduct aged out) but still the single most-used surface by a wide margin, still banned. **`inline-SVG/DOM primary surface` fell to 3×** (answerpiano, roomtone, loomvoice — mididuet's exit pulled it under the floor); WATCH, not banned. The two new ships both landed on **rested** surfaces: `phosphene` on a **three.js orbited SOLID** (an object you turn, explicitly not a room and not a watch-only field) and `driftchoir` on **audio-only / no-primary-visual** — *the still-0× lane, finally 1×.* `Canvas2D` holds at **2×** (ringcanon, manyreturns); `three.js inhabited room` at **2×** (headnave, revenant) — phosphene is a solid, did NOT add to the room count. **BANNED next cycle:** `GPU generative-field-as-the-visual` (6×). **WATCH:** `inline-SVG/DOM` (3×, one ship from re-banning), `Canvas2D` (2×), `three.js inhabited room` (2×). Genuinely 0× and still owed: **projection / installation** (Tauri/kiosk — Karel's stated priority).
- **Over-represented technique:** **the headline, and it's the AUDIO now, not the visual.** `decoupled-time-base canon (one recording read at two-or-more time-bases as counterpoint against itself)` — still **4×** (canon, revenant, ringcanon, manyreturns), still over the ban floor, still banned as of last week. But look wider: `incommensurate-loop phase-drift ensemble` is now **2×** (manyreturns, **driftchoir** — the *same reused engine*), and the two are the same meta-idea. **Counted as one family — "loops of one take drifting/answering against a shifted copy of itself" — that is 5 of the last 15 pieces** (canon, revenant, ringcanon, manyreturns, driftchoir). A third of the window is one audio idea. Meanwhile `granular grain-triggering` **fell to 3×** (canon, inkstroke, seismscore — conduct + mididuet aged out), off the ban floor to WATCH, and `band-split crossover` holds at **3×** (canon, duetlink, revenant). Freshly-cashed and healthy at 1× — do NOT re-ban into extinction: convolution cross-synthesis (roomtone), whole-take polyphony (loomvoice), physical-model modal resonance (ringcanon), harmony-carved instanced-geometry (phosphene). **BANNED next cycle:** `decoupled-time-base canon mechanism` (4×) **+ the whole `loops-of-one-take-against-itself` family, incommensurate-ensemble included** (5× as a family — the engine is spent; ship no sixth). **WATCH:** `granular` (3×), `band-split crossover` (3×), `Basinski per-voice aging` (2×).
- **Over-represented vibe:** `cool-severe ink-on-bone/black` — **6×**, still the true house style (duetlink cold-achromatic, morphonate cyan/ink, inkstroke indigo/bone, headnave dark-severe, seismscore Ikeda B-W-R, spiralkeep cyan-teal/bone). **`warm-chromatic` collapsed to 2×** (canon, answerpiano — conduct + mididuet were the warm tail and aged out); off the floor to WATCH, the aging-out I've tracked for weeks is basically done. **New tell, and it's the one from last week spreading:** `monochrome-base-plus-one-signal` is now **3×** — graphite+chartreuse (ringcanon), ashlight+bone (manyreturns), **silver-mercury+amethyst/teal (phosphene)** — and it jumped surfaces, from the two Canvas2D ships onto three.js. The "desaturated ground + one saturated accent" register is quietly becoming the new default across every surface. The three genuinely fresh 1× registers hold (roomtone high-key-magenta, loomvoice polychrome, revenant earthy). **BANNED next cycle:** `cool-severe-ink-on-bone/black` (6×). **WATCH:** `monochrome-base-plus-one-signal` (3×, jumped surfaces — one ship from a ban), `warm-chromatic` (2×). **Encouraged:** the three 1× fresh registers — pull one *forward* to 2× deliberately, or find a sixth register, but do NOT reach for desaturated-plus-one-accent a fourth time.
- **BANNED for next cycle:** `passive/self-propelling primary input` (4×) + `GPU generative-field output` (6×) + `decoupled-time-base canon mechanism` (4×) **+ the `loops-of-one-take-against-itself` audio family, incommensurate-ensemble included** (5× as a family — NEW, the audio engine is now the rut) + `cool-severe-ink-on-bone/black palette` (6×). **WATCH (not yet banned):** `inline-SVG/DOM` (3×), `Canvas2D` (2×), `three.js inhabited room` (2×), `granular` (3×), `band-split crossover` (3×), `Basinski aging` (2×), `monochrome-base-plus-one-signal palette` (3×), `warm-chromatic` (2×). **Not banned (rule 10):** catalog playback as the audio source. **Explicitly owed / still 0×:** projection/installation, real-world-data sonification, cross-modal translation; and a genuinely *different audio relationship* — multi-take cross-synthesis (roomtone/loomvoice are the seeds), spectral freeze (the long-banked `fold`), additive resynthesis, real-time source separation.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Fifteenth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 12** (answerpiano 3, duetlink 2, morphonate 3, inkstroke 3, headnave 3, seismscore 3, roomtone 3, loomvoice 3, revenant 3, ringcanon 3, **phosphene 2, driftchoir 2**). The honest middle, and it **grew by two** — because *both new ships landed here at the bottom of the band.* `phosphene` cleared **#2** (≥3 subsystems: catalog loader + `loadTrackAnalysis` chord-walker + three.js shader lattice + facet-cage + onset detector + tilt steering) **+ #3** (Klüver form-constants, 1926), honestly declined #1 (instanced-geometry ~heavily trodden). `driftchoir` cleared **#2** (catalog loader + incommensurate-loop scheduler + Basinski aging + phase-coincidence detector + dual haptic/audio cue) **+ #3** (Eno/Reich/Basinski), honestly declined #1 (haptic, audio-only, and the ensemble engine all have priors). Both are clean, honest **disciplined-2s** — the *floor minimum*. `duetlink` remains the lone 2 that reads as thin rather than disciplined.
- **Hit 4–5 criteria — 3 (canon 5, spiralkeep 4, manyreturns 4).** Distribution **`0 / 12 / 3`** — **the top band shrank** (from `0/10/5`), and this is the number the whole verdict turns on. It did not shrink because anything got worse; it shrank because the two honest-**4s** that aged out (conduct, mididuet) were replaced by two honest-**2s** (phosphene, driftchoir). Diversity was bought with ambition, ship-for-ship. That was a fair trade *this* window — the audio-only debt was worth a floor-clearing ship, and phosphene's diversity break was worth it too. But it is not a trade to repeat: **two consecutive floor-minimum ships is a pattern, not a trade, and the next one needs to reach for a 4.** The three top-band survivors are the template: `canon` (the lone verified 5), `spiralkeep` and `manyreturns` (honest 4s that got there by *extending a real thing until it deepened*, not by minting a fresh disciplined-2 in a new corner).

## Standouts (positive)
- **16448-phosphene** — the diversity win of the window, and the piece that proves the lab can walk away from canon cleanly. His harmony *carves* a single crystalline light-body live: the chord sets the kaleidoscopic fold order (facets visibly re-cut on a chord change), minor/major swings the hue amethyst↔teal, tension shears the spiral, onsets launch a bloom-wavefront through the shells. It's an *object you turn in your hands*, not a room and not a watch-only field — dodging the 6× GPU ban and the three.js-room watch at once — and it returned to the stated VISIONARY primary direction the canon strand had neglected for a fortnight. An honest 2, but the *right* 2: the un-adjacent break the last verdict demanded.
- **16464-driftchoir** — the debt, paid. The audio-only/haptic ghost choir I named by number for weeks and the lab `rm`'d unshipped three times, finally shipped — and shipped in the *robust* reading: the desktop coincidence-swell (the two aligning voices bloom gain + re-open lowpass on *his own sound*) is the exact fix that lets the alignment read on a 06:30 desktop glance without a phone, the resurrect-condition STATE named. It is the lab's first honest test of the screen bias. **Its worth is now Karel's ears' to confirm** (see provocation #5) — but as a discipline-of-restraint ship it is exemplary: no screen to hide behind, no synth anywhere, the alignment *heard and felt* not watched.
- **16368-manyreturns** — still the strongest structural bid at the ceiling the lab has made: the self-building incommensurate-loop choir, the wear-ring memory trace that makes minute-5 genuinely ≠ minute-1, the honest ambition-#1 claim. It remains an honest 4 and the standing proof that the ensemble engine *works* — which is precisely why the engine is now spent and must be retired (see provocations #1).
- **15824-canon** — the lone *verified* 5/5, now the fully-extended root of a closed four-piece lineage (revenant → ringcanon → manyreturns). Still the most valuable object in the lab and the proof a decoupled-time-base instrument reads as a real 5. Its lineage is finished; honor it by building something else.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15920-duetlink** (2/5) — the window's local-minimum exemplar and the only 2 that reads as thin rather than disciplined: strip the "first multi-user" banner (the lab has ~24 multi-user priors) and it's a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors. Competent plumbing, not a concept — and its crossover feeds the 3× band-split creep still worth watching.
- **16064-seismscore** (3/5) — the triple-default in one object: passive input + GPU generative field + granular grain-triggering, Ikeda B-W-R. Technically clean, but it's the exact stack the bans exist to climb off; a competent re-run of the house style, not a new idea.
- **15872-answerpiano** (3/5) — passive/self-propelling with no load-bearing human verb; clears the floor on paper but adds no axis its neighbours didn't, and lands on the most over-represented input class. Residue of the passive-input habit that is now the lab's only remaining input rut.

## Provocations for tomorrow's dream cycle
1. **The AUDIO engine is now the monoculture — retire "loops of one take against itself" entirely.** canon → revenant → ringcanon → manyreturns → driftchoir is **5 of the last 15** built on the same one idea: his take looped/phased/answered against a shifted copy of itself. driftchoir was the sanctioned final cash of that engine on a new output modality — **it's cashed, and it closes the account.** Ship NO sixth. The next audio move must be a *genuinely different relationship*: two DIFFERENT takes cross-synthesized (extend `roomtone`'s convolution or `loomvoice`'s polyphony), spectral freeze / STFT cross-synthesis (the long-banked `fold`), additive resynthesis, or real-time source separation. Change the DSP, not the surface — a fresh visual on the same drifting-loop engine is the relocated rut, and I'll score it a 2 on arrival.
2. **Swing for a 4 — two consecutive floor-minimum ships is enough.** phosphene and driftchoir were both honest **2/5**, and the top band slid `5 → 3` because of it. The trade bought real diversity and was worth it once; a third floor-clearing disciplined-2 makes it a habit. The next ship should aim to *claim a real #1 or a genuine multi-cycle deepen* — the way spiralkeep and manyreturns reached 4 by extending a real thing until it deepened, not by minting a fresh 2 in a new corner. Concretely: take a live 4 (`spiralkeep`'s excitable-medium persistence, `manyreturns`' ensemble) and push it to a *verified* 5, OR start a genuinely 3-subsystem ≥#1 build — but stop clearing the floor and calling it a cycle.
3. **Cash a 0× CATEGORY, not just a 0× surface.** phosphene and driftchoir diversified *output beautifully* but both stayed *music about music.* The genuinely un-cashed **categories** haven't appeared in 15+ cycles: **projection/installation** (Tauri/kiosk — Karel's own stated priority #5), **real-world-data sonification** (an external API as composition input — music *about* something other than music, his catalog as the instrument), **cross-modal translation** (an unexpected sensor → an unexpected axis of his takes). A rough skeleton of a genuinely new category beats a polished piece in a category the lab has run 200 times.
4. **The palette tell jumped surfaces — hold the line before it calcifies a fourth time.** `monochrome-base-plus-one-signal` is now **3×** (graphite+chartreuse, ashlight+bone, silver+amethyst) and it spread *off* Canvas2D *onto* three.js with phosphene — the exact pair-calcification I flagged last week, now generalizing across surfaces. Do NOT reach for "desaturated ground + one saturated accent" a fourth time. The win is the three fresh 1× registers (high-key-magenta `roomtone`, polychrome `loomvoice`, earthy `revenant`) — pull one *forward* to 2× on purpose, or find a sixth register wrong next to all of them (a metallic/leaf register, a duotone, a single unused saturated hue as the *ground* not the accent).
5. **driftchoir needs Karel's EARS before the lab decides the audio-only lane's fate.** It's the one ship in the window headless can't judge, and MORNING.md puts the open question directly to Karel: *does an audio-only/haptic piece land, or do you want every piece to carry a strong visual?* His answer reshapes how much the lab invests in the screen-less lane. Before shipping *another* audio-only piece, get a real listen: does the choir breathe as an evolving texture vs. a wash, and does the desktop coincidence-swell carry it without a phone? Don't compound the bet until the first one is heard.

## Karel-facing line
Real diversity win — the owed audio-only `driftchoir` finally shipped and `phosphene`'s harmony-carved crystal broke the visual rut, and camera/granular/warm all fell off the ban floor — but the diversity was bought at the top band's expense (two floor-minimum ships, top band slid 5→3), and underneath the fresh surfaces **five of the last fifteen pieces are the same one audio idea (his take looped against itself)**: tomorrow needs a genuinely different DSP and a real swing at a 4, not a sixth drifting-loop piece.
