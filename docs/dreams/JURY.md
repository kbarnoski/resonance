# Concept Jury Verdict — 2026-08-31

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/gamepad/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→5579fa2; cycle 1209 `16560-spectralbraid` at HEAD, plus the standing Karel-directed out-of-fence batch — archive-catalog prebuild/`scripts/generate-dream-catalog.mjs`, `next.config.ts`, paginated `/dream/archive`, now-playing-bar — none dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. Two new ships since the 2026-08-30 verdict: `16496-terrafret` (1208) + `16560-spectralbraid` (1209); `15824-canon` and `15872-answerpiano` aged out of the 15-window. RESEARCH.md is fresh (dated entries through 2026-08-31)._

## Summary
Good week, and an honest one. The two newest ships answered **three of yesterday's five provocations head-on**: `spectralbraid` broke the audio monoculture I banned — two *different* takes cross-synthesized cell-by-cell, never a self-loop — `terrafret` cashed a genuinely 0× **category** (real-world-data sonification: the living Earth playing his piano, music finally *about* something other than music), and both **claimed a real ambition #1** instead of clearing the floor at a disciplined-2 the way phosphene/driftchoir did. That is real movement on exactly the axes I hammered. **But the diversity relocated, again** — both "diverse" ships landed on **Canvas2D**, which jumps from 2× to **4×** and onto the ban floor; the escape from the audio rut and the palette rut came at the cost of the surface. And the number the whole verdict turns on: **the window has lost its only verified 5.** `canon` aged out, the top band is down to **two honest-4s and zero 5s** (`0/13/2`), and after three days of me asking for a swing to 4 the answer has been 2, 2, 3, 3 — better each time, real #1 claims now, but still landing at 3. **Tomorrow: get OFF Canvas2D, ship the fully-built projection/installation piece sitting in the bank, and finally land a verified 4.**

## Diversity audit
_(last 15, numerically highest on disk: duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice, revenant, ringcanon, manyreturns, phosphene, driftchoir, terrafret, spectralbraid — canon + answerpiano aged out; terrafret + spectralbraid entered)_

- **Over-represented input:** **none at the ban floor — a genuine, structural win.** With `canon` (camera) and `answerpiano` (passive) both aged out, the two long-standing input ruts collapsed at once: `passive/self-propelling` fell to **3×** (morphonate, seismscore, spiralkeep) and `camera/webcam-primary` fell to **1×** (headnave alone). The recent run is a healthy spread of load-bearing verbs — gamepad (ringcanon), ensemble-authoring (manyreturns), tilt-orbit (phosphene), press-hold (driftchoir), live-data-stream (terrafret), pointer-paint (spectralbraid) — all at ≤1×. **BANNED next cycle: nothing on input.** **WATCH:** `passive/self-propelling` (3×, the last input habit, and the `spiralkeep` persistence exemption still applies to a genuinely-never-run subsystem).
- **Over-represented output:** **the headline this window.** `GPU generative field (WebGL2/WebGPU shader you watch)` holds at **5×** (duetlink, morphonate, inkstroke, seismscore, spiralkeep — canon aged out from yesterday's 6), still the single most-used surface, still banned. **But `Canvas2D` jumped 2×→4×** (ringcanon, manyreturns, **terrafret, spectralbraid**) — *both* new ships landed here. This is the exact relocation pattern the audit exists to catch: the diversity move escaped the audio and palette ruts and quietly re-concentrated the surface. `Canvas2D` is now ON the ban floor. `inline-SVG/DOM` fell to **2×** (roomtone, loomvoice — answerpiano's exit pulled it under), `three.js inhabited room` holds at **2×** (headnave, revenant), `three.js orbited solid` **1×** (phosphene), `audio-only/no-primary-visual` **1×** (driftchoir). **BANNED next cycle:** `GPU generative-field-as-the-visual` (5×) **+ `Canvas2D` (4×, NEW — the relocated rut).** **WATCH:** `three.js inhabited room` (2×), `inline-SVG/DOM` (2×). **Genuinely 0× and owed for 15+ cycles: projection / installation** (Tauri/kiosk — Karel's own stated priority #5).
- **Over-represented technique:** `the loops-of-one-take-against-itself family` (decoupled-time-base canon + incommensurate-loop phase-drift ensemble) — **4×** (revenant, ringcanon, manyreturns, driftchoir; canon aged out from yesterday's 5). Still over the floor, still banned — **but declining and correctly starved:** neither new ship touched it. `spectralbraid` is two *different* takes cross-synthesized (explicitly off the family) and `terrafret` is data-triggered phrase-mapping. **The retirement is holding — keep it closed; the family will fall to 3× and off the floor on its own as revenant/ringcanon age out.** Within it, `decoupled-time-base canon mechanism` is now **2×** (revenant, ringcanon) and `incommensurate ensemble` **2×** (manyreturns, driftchoir). `granular grain-triggering` fell to **2×** (inkstroke, seismscore), `band-split crossover` to **2×** (duetlink, revenant) — both off the floor. Freshly-cashed and healthy at 1× — do NOT re-ban into extinction: two-take spectral cross-synthesis (spectralbraid), external-data phrase-mapping (terrafret), convolution cross-synthesis (roomtone), whole-take polyphony (loomvoice), physical-model modal resonance (ringcanon), harmony-carved geometry (phosphene). **BANNED next cycle:** `loops-of-one-take-against-itself family` (4×, keep the account closed — ship no sixth). **WATCH:** `granular` (2×), `band-split crossover` (2×).
- **Over-represented vibe:** `cool-severe ink-on-bone/black` — **6×**, unchanged and now the *most stubborn* tag in the audit (duetlink cold-achromatic, morphonate cyan/ink, inkstroke indigo/bone, headnave dark-severe, seismscore Ikeda B-W-R, spiralkeep cyan-teal/bone). All six survived the window; nothing aged out of it. Still the true house style, still banned. `monochrome-base-plus-one-signal` holds at **3×** (graphite+chartreuse ringcanon, ashlight+bone manyreturns, silver+amethyst phosphene) — WATCH, one ship from a ban, and correctly avoided by both new ships. **New tell: two-signal `DUOTONE` is now 2×, and it's the two newest ships** — blue↔magenta (terrafret), copper↔verdigris (spectralbraid). Good that they dodged mono-plus-one; watch that duotone doesn't become the *next* default the same way. `warm-chromatic` collapsed to **0×** (canon + answerpiano were the last two, both aged out) — the fade-out I tracked for a month is complete. The three fresh 1× registers hold (roomtone high-key-magenta, loomvoice polychrome, revenant earthy). **BANNED next cycle:** `cool-severe-ink-on-bone/black` (6×). **WATCH:** `monochrome-base-plus-one-signal` (3×, one from ban), `two-signal duotone` (2×, emerging on the newest ships).
- **BANNED for next cycle:** `GPU generative-field output` (5×) **+ `Canvas2D output` (4×, NEW — the relocated diversity rut)** + `loops-of-one-take-against-itself technique family` (4×, keep closed) + `cool-severe-ink-on-bone/black palette` (6×). **WATCH (not yet banned):** `passive/self-propelling input` (3×), `three.js inhabited room` (2×), `inline-SVG/DOM` (2×), `granular` (2×), `band-split crossover` (2×), `monochrome-base-plus-one palette` (3×), `two-signal duotone` (2×). **Not banned (rule 10):** catalog playback as the audio source. **Explicitly owed / still 0× for 15+ cycles:** **projection/installation** (Karel's stated priority — and `16528-vigil` is fully built in the bank), **cross-modal translation** (`16512-lettertongue` also banked, fully built).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Sixteenth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 13** (morphonate 3, duetlink 2, inkstroke 3, headnave 3, seismscore 3, roomtone 3, loomvoice 3, revenant 3, ringcanon 3, phosphene 2, driftchoir 2, **terrafret 3, spectralbraid 3**). The honest middle, and it grew by two — but this time it grew the *right* way: both new entries came in at **3 with an honest #1 claim**, not at the bare 2 that phosphene/driftchoir cleared. `terrafret` cleared **#1** (first real external-API sonification — a live USGS feed as the primary compositional input greps to zero) **+ #2** (≥3 subsystems: catalog loader + USGS poller/dedupe/parser + event→music mapper + Canvas2D map) **+ #4** (jury-named 0× category). `spectralbraid` cleared **#1** (first shipped two-take spectral cross-synthesis + first painterly time-frequency authoring surface, both grep-0) **+ #2** (5 subsystems: two-take loader + dual offline STFT + painted A/B mask + streaming overlap-add resynth + Canvas2D spectrogram) **+ #3** (IRCAM ASAP "Spectral Crossing"). `duetlink` remains the lone 2 that reads as thin rather than disciplined.
- **Hit 4–5 criteria — 2 (spiralkeep 4, manyreturns 4).** Distribution **`0 / 13 / 2`** — **the top band shrank again, from `0/12/3` to `0/13/2`, and this is the number the whole verdict turns on.** It shrank for one reason: **`canon`, the lab's only verified 5/5, aged out of the window.** There is no longer a single 5 in the audit, and the two survivors are honest-4s (`spiralkeep`'s excitable-medium persistence, `manyreturns`' self-building ensemble) that got there by *extending a real thing until it deepened.* The trajectory is the story: I have asked for a swing to a verified 4 for three days, and the ship-for-ship answer has been **2 → 2 → 3 → 3**. That IS improvement — the two new 3s carry real #1 claims, a clear step up from the two floor-minimum 2s before them — but it has still never reached a **4**, and meanwhile the top band keeps thinning as older 4s and the lone 5 age out. **The middle is climbing; the ceiling is falling. Next ship must land a verified 4, not a fourth honest-3.**

## Standouts (positive)
- **16560-spectralbraid** — the ship that broke the audio monoculture cleanly, and the direct answer to last verdict's #1. Not one take against itself — **two genuinely different takes cross-synthesized**, and you *paint* which performance shows through in every time×frequency cell, hearing them braided into a performance that never happened. It's the lab's first painterly time-frequency authoring surface and its first shipped two-take spectral crossing (both grep-0), and it finally cashes the long-banked `fold` the jury has named for months. An honest 3 with a real #1 — the *right* reach, not a relocated rut. Its one shadow is the surface (Canvas2D, now 4×) and that it still wants Karel's ears (see provocation #5).
- **16496-terrafret** — the piece that got the lab out of "music about music" at last. A live USGS earthquake feed decides what you hear: every quake in the last hour becomes an enveloped phrase of his real piano, magnitude→transpose→register, depth→lowpass, longitude→pan, the map slowly drawing the planet's last hour. It's the first real external-API sonification (grep-0), a jury-owed 0× *category*, and it's robust-everywhere via a bundled snapshot so it's audible at 06:30 regardless of network. Music *about the planet* — the "huh, didn't know we could do that" Karel prizes.
- **16368-manyreturns** — still the strongest structural bid at the ceiling the lab has: the self-building incommensurate-loop choir with the wear-ring memory trace that makes minute-5 genuinely ≠ minute-1. An honest 4 and now, with canon aged out, **one of only two 4+ pieces left in the window** — a live deepen target for the verified-5 push, provided the deepen isn't a re-run of the now-retired loop engine.
- **16144-spiralkeep** — the other surviving 4: excitable-medium persistence with a real accumulated state. The second live template for provocation #2 — extend it until it verifies as a 5.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15920-duetlink** (2/5) — the window's local-minimum exemplar and the only 2 that reads as thin rather than disciplined: a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors under a "first multi-user" banner the lab has ~24 priors for. Competent plumbing, not a concept — and its crossover feeds the band-split creep and its shader feeds the 5× GPU-field ban.
- **16064-seismscore** (3/5) — the triple-default in one object: passive input + GPU generative field + granular grain-triggering, Ikeda B-W-R. Technically clean, but it's the exact stack the bans exist to climb off — a competent re-run of the house style, not a new idea.
- **16000-morphonate** (3/5) — passive/self-propelling input on a GPU generative field in cyan/ink. Clears the floor on paper but lands on three of the four banned tag classes at once (GPU-field output, cool-ink palette, and the passive-input habit); residue of the house style, not a departure from it.

## Provocations for tomorrow's dream cycle
1. **Canvas2D is the new relocated rut — get OFF it.** Both diversity ships escaped the audio monoculture and the palette calcification beautifully, then both landed on Canvas2D, pushing it 2×→**4×** and onto the ban floor. This is the audit's whole point: diversity on one axis re-concentrating on another. Next ship must NOT be Canvas2D. Rested and owed surfaces: **projection/installation** (0× for 15+ cycles, and see #3), **audio-only** (1×, driftchoir), **three.js solid** (1×, phosphene). A fresh visual on Canvas2D is the relocated rut and I'll flag it on arrival.
2. **Land a verified 4 — the ceiling is now the whole problem.** `canon` aged out; the top band is **two honest-4s and zero 5s**, and the three-day trajectory (2→2→3→3) has climbed the middle but never reached the ceiling. The two new #1 claims were the right move and I credit them — but a real #1 on paper is not yet a verified 4 in the object. Next ship must either **deepen a live 4 to a verified 5** (`spiralkeep`'s persistence or `manyreturns`' ensemble — but NOT by re-running the retired loop engine) OR **push spectralbraid itself toward the 4–5** (longer/full-take analysis past the ~17s cap, phase-vocoder tempo-alignment before crossing, saveable mask "scores"). Stop shipping honest-3s in new corners; extend one real thing until it deepens.
3. **Cash the last 0× category — and it's sitting fully built in the bank.** terrafret cashed real-world-data; the two categories still at 0× for 15+ cycles are **projection/installation** (Karel's own stated priority #5) and **cross-modal translation**. Both are already fully built and banked: `16528-vigil` (the all-night projection/installation kiosk) and `16512-lettertongue` (language→his piano). Shipping `vigil` is a de-risked way to hit a 0× category AND get off Canvas2D in one move — it is the single highest-leverage ship available tomorrow. Do it before minting anything new.
4. **A new palette tell is forming — two-signal duotone is 2×, both the newest ships.** They correctly dodged the mono-plus-one register (still 3×, one from a ban), but reached for a two-metal/two-hue duotone (blue↔magenta, copper↔verdigris) instead. Don't let duotone calcify into the next default the way mono-plus-one did. The win is still the three fresh 1× registers (high-key-magenta roomtone, polychrome loomvoice, earthy revenant) — pull one *forward* to 2× on purpose, or find a genuinely sixth register (a single saturated hue as the *ground* not the accent, a leaf/metallic palette, a true polychrome).
5. **Two consecutive ships now want Karel's ears — get a listen before compounding the bet.** `driftchoir` (audio-only, carried from yesterday) and now `spectralbraid` (painterly cross-synthesis) are both pieces a headless jury cannot judge: does the braid read as ONE coherent instrument or two takes fighting? Does the choir breathe or wash? Before deepening spectralbraid toward the 4–5 in #2, get a real listen — don't stack a third un-hearable audio bet on top of two unverified ones.

## Karel-facing line
Strong week — the two newest ships answered three of five provocations at once (broke the audio rut with two-take spectral cross-synthesis, cashed the real-world-data category, both claimed a real #1) — but the diversity relocated onto Canvas2D (now 4× and banned), `canon` aged out so the window has zero verified 5s left, and the top band is down to two honest-4s: tomorrow, get off Canvas2D, ship the fully-built projection/installation piece (`vigil`), and finally land a verified 4.
