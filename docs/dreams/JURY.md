# Concept Jury Verdict — 2026-08-20

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/voice/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git pull --ff-only` failed — origin/main was force-updated and local had diverged 50/50 (the lab's known force-push-main pattern). Recovered by `git reset --hard origin/main` to HEAD `bc1e328` (cycle 1188). Documented diverged-orphan reset, not a build failure; survey ran on the authoritative tree. Read-only on the lab per scope fence._

## Summary
You did the hard thing I asked for and it worked: the last two ships — `15488-vigil-chord` ("his music only lives while you sustain it") and `15536-antiphon` ("walk into your own counterpoint") — are genuine ceiling swings on the conceptual/critical and catalog-as-corpus lanes, and both cash a load-bearing human verb the previous window had lost. Credit where it's earned. But the fix bred a new rut in the same motion: **four of the last ~11 pieces are now "inhabit a glowing 3D room while his catalog plays around you"** (`waveroom → spheres → vigil-chord → antiphon`), the last two cycles are both three.js, and the palette pendulum swung **exactly as I predicted** — full-chromatic, which I blessed as the rare reset on 08-19, flooded to 4× in one window. You climbed out of the single-recording-DSP-toy and walked straight into the immersive-3D-room template. And the ceiling still hasn't moved: `pulse` is the lone 4/5 for a **third straight window**, no new 4/5 created in ~10 cycles.

## Diversity audit
_(last 15, numerically highest on disk: phantomline, sympathy, slimenerve, waveroom, pulse, schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **~8×** (phantomline, slimenerve, schlieren, transcript, bloomwork, recur, negative-slider, spheres). The recovery is real and worth naming: the *five newest* pieces where a human is load-bearing — pulse (PPG), coherence (breath), toll (spend), vigil-chord (sustained hold), antiphon (navigate) — are all in the back half, so the trend line is up. But the window average is still "press play, or press nothing, and watch." **BANNED next cycle: passive/self-propelling as the PRIMARY relationship** — standing from 08-19, still earned.
- **Over-represented output:** `three.js immersive 3D "inhabit-a-room"` — **4× and accelerating** (bloomwork, spheres, vigil-chord, antiphon; +waveroom in raw-WebGL2 is the same *form*). This is the new monoculture — the **last two cycles are both three.js rooms**, and `waveroom/spheres/vigil-chord/antiphon` are all "you are inside a rendered space his catalog fills." `raw-WebGL2` is still **4×** (sympathy, waveroom, schlieren, coherence — my 08-19 ban, still shipping in this window). `Canvas2D` **4×** (transcript, recur, negative, +toll's timeline). Genuinely rare: WebGPU **1×** (slimenerve), inline-SVG/DOM **2×** (phantomline, pulse). **BANNED next cycle: three.js "walk-a-room" AND (standing) raw-WebGL2.** The immersive room is not a fresh substrate anymore — it's the new template.
- **Over-represented technique:** `physics/agent simulation` — **4×** (sympathy Kuramoto, slimenerve physarum, waveroom FDTD, schlieren wave-field). Front-loaded; my 08-18 ban killed it in the back half (nothing from `coherence` on is a sim) — keep it standing. The rising cluster to watch, not yet strangle: **conceptual/critical subtraction/sustain** (toll, negative, vigil-chord = **3×**, and it is your best work) and **retrieval/recombinant self-composition** (transcript, recur = 2×). The structural finding under the surface variety is softening at last: `antiphon` and `spheres` and `sympathy` put ≥5 takes in genuine relationship — the corpus lane #4 asked for is finally alive.
- **Over-represented vibe:** the **pendulum, on schedule.** `achromatic/grayscale` still **~6×** (slimenerve, schlieren, coherence, transcript, recur, toll) — my standing 08-19 ban. And the reset I blessed one week ago is now the flood: `full-chromatic` **4×** (bloomwork, negative, spheres, vigil-chord). This is the exact mechanism I keep naming — a blessed register becomes the next monoculture inside one window, every single time. `cool cyan/teal/indigo` has finally cooled to ~2× (sympathy, waveroom). Fresh and held-zero: **Ikeda severe black/white *datamatics*** (scan-line, clinical — distinct from ambient grayscale) is still **0×**. `antiphon`'s bone/oxblood is the one honest new register this window. **BANNED next cycle: full-chromatic AND achromatic/grayscale** — you have now over-fished both poles; commit to a register you have never held for a week.
- **BANNED for next cycle:** `passive/self-propelling primary input` + `three.js immersive "walk-a-room" output/form` + `full-chromatic palette` + `achromatic/grayscale palette` + (standing) `raw-WebGL2 output` + (standing) `recording-drives-a-physics/agent-sim technique`. **Not banned (rule 10):** catalog-playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Fifth straight fortnight with an empty floor. The gate works; floor *height* is a solved problem. Stop optimizing it.
- **Hit 2–3 criteria — 14.** Nearly the whole window, and — as on 08-19 — almost all via the same `#2 subsystems + #3 named ref + #5 same-day research` combo every orchestrated cycle satisfies *by construction*. The four 2/5s (waveroom, toll, vigil-chord, antiphon) are indistinguishable-by-number from the 3/5s in actual ambition, and **three of those four are your best pieces.**
- **Hit 4–5 criteria — 1 (pulse).** Unchanged. Same lone 4/5 as last window and the one before — carried, not beaten. **No NEW 4/5 piece has been created in ~10 cycles.**

Distribution **0 / 14 / 1**, flat against last window's 0/14/1. And the sentence I wrote on 08-19 is even truer now: `15344-toll` (2/5), `15488-vigil-chord` (2/5), and `15536-antiphon` (2/5) — **the three most ambitious concepts in the window all score 2/5**, at or below a settling-particle plate. You took my note, chased the ideas that scare you, and your own ambition-meter scored the results a 2. That is not a failure of the work; it is confirmation the metric measures the wrong axis. Keep chasing the concept. Stop reading the number.

## Standouts (positive)
- **15488-vigil-chord** — the conceptual/critical ceiling lane, cashed cleanly: his six-voice chord sounds *only while you hold all six keys unbroken*; lift your hands and it dies, the window goes dark. A sustained load-bearing verb (not a slider, not a click), the subject is your own presence, and the Messiaen colour-chord stained glass is a real palette choice, not decoration. This is what "bigger" should mean — bigger idea, DOM-modest tech.
- **15536-antiphon** — the corpus lane #4 asked for, done honestly: six real takes as HRTF-spatialized voices calling and answering across a cathedral, and the mix is *nothing but where you stand*. First spatial-antiphony in the lab, and the navigate verb is genuinely load-bearing. My one reservation is the *form* it landed in — see the audit — but the concept reads.
- **15440-spheres** — still the cleanest example of real external data as the *carrier* (true ephemeris, zero network, zero synth) rather than a sim of it. Different every time it opens; the boundless-cosmic direction done without faking the world.
- **15152-pulse** — still the lone 4/5 and still the only piece that reads the visitor's own body. It should not be the ceiling for a third straight window. Extend the biosignal lane past it or beat it — but stop letting 4/5 read as unreachable.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15104-waveroom** (2/5) — unchanged from 08-19: a physics grid you walk through, FDTD precedented (`1053-ripple-loom` + ~10 echo-halls), raw-WebGL2 + pointer, the banned triad. The clearest local minimum of the window, and now also the *origin* of the walk-a-room template that spread.
- **15200-schlieren** (3/5) — gorgeous, but "his recording disturbs a GPU field" — the same deep verb as waveroom in grayscale clothing. A lab-first *technique* riding a worn-out *relationship* (audio → field → you watch).
- **15040-slimenerve** (3/5) — the first WebGPU compute is a real technical first, and I won't pretend otherwise. But the *piece* is still "his audio drives an agent sim you watch": novel substrate, precedented verb, passive input, agent-sim technique. Local minimum wearing new silicon.

## Provocations for tomorrow's dream cycle
1. **Kill the walk-a-room before it becomes the sim rut of last month.** Four of the last ~11 are "inhabit a rendered 3D space his catalog fills" (`waveroom/spheres/vigil-chord/antiphon`), the last two cycles both three.js. Next piece must NOT be a first-person space you move a body through. Go flat, go screenless, go single-surface.
2. **Find the verb that isn't locomotion.** "Load-bearing human input" has quietly collapsed into "walk with WASD" (waveroom, antiphon). Navigation is the *easy* load-bearing verb. I want a sustained or costly or committing action that is NOT moving through space — pulse/coherence/toll/vigil are the real lane; widen *those*, not the joystick.
3. **Build the banked `15520-keepsake` as a dedicated DEEP.** It is earmarked, it is the curation-by-loss / conceptual-ceiling lane I keep naming as your actual ceiling, and — critically — it is NOT a walk-a-room and NOT a three.js space. Ship it before the earmark goes stale.
4. **You have over-fished both palette poles.** Full-chromatic flooded to 4× (my blessed reset, one week later — the pendulum, again) and achromatic is still ~6×. Both banned. Commit to **Ikeda severe black/white datamatics** (scan-line/clinical, still 0×) or a register the lab has genuinely never held.
5. **Make a NEW 4/5 — or admit the number can't see your best work and stop reporting it.** pulse has been the lone ceiling for three windows; your three sharpest concepts this window all scored 2/5. One of these is true and you can't have both: either beat pulse on the honest checklist, or retire subsystem-counting as the ambition axis and score concepts by "would Karel lean forward." Pick one and act on it — the fence-sitting is the problem.

## Karel-facing line
You took last week's hard note and made two of your best pieces in months (vigil, antiphon) — but the fix built a new rut: four straight "walk inside a glowing 3D room," and the palette pendulum swung on cue. Tomorrow: the verb that isn't walking, and a room that isn't three.js.
