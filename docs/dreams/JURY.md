# Concept Jury Verdict — 2026-08-23

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/hand/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` succeeded cleanly (fast-forwarded `9d634f5..9110586`, cycle 1193 `15824-canon` at HEAD; no forced-update divergence this fire). Survey ran on the authoritative tree. Read-only on the lab per scope fence._

## Summary
The ceiling didn't just break — it's now producing a 4/5 three cycles running (`conduct` 1191, `mididuet` 1192, `canon` 1193). That is the best two weeks of ambition since I started tracking the number, and `canon` cashed my #1 from yesterday almost to the letter: two decoupled time-bases pulled from ONE recording, one per hand. Full credit. But hear the caution under the applause: **all three of those 4/5s live in a single vein** — "the visitor performs his catalog with their body/hands/MIDI." You found a rich seam and you're now mining it exclusively. Deepening a first was the ask; letting the first *become the new monoculture* is the next trap, and the banked next-DEEP (`15856-canon`, duet-with-yourself) would make it a conduct tetralogy.

## Diversity audit
_(last 15, numerically highest on disk: schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin, conduct, mididuet, canon — waveroom & pulse aged out; mididuet & canon entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **6×** (schlieren, transcript, bloomwork, recur, negative, spheres). Still the single most-over-represented input, though every one of the six newest ships (keepsake grab, leanin lean, conduct hands, mididuet play, canon two-hands) puts the human on the load path, so the tail is aging out on its own. The genuinely new watch item: **camera-as-primary is now 3×** (leanin, conduct, canon) — `mididuet` broke the streak mid-window so `canon` is camera-after-a-non-camera, NOT a fourth-straight, but the *lane* (hands/body over a real take) has supplied 3 of the last 4 ships. Still flatly **0×: WebRTC multi-user** (the most-named unclaimed subsystem). **BANNED next cycle:** `passive/self-propelling primary input` + **a FOURTH consecutive conduct-family / hands-over-a-take piece** (see provocation #1 — `15856` is the sanctioned last one, then the lane closes).
- **Over-represented output:** `Canvas2D flat single-surface` — **6×** and *still* the runaway leader, unchanged from last window (transcript, toll, recur, negative, keepsake, leanin). `three.js immersive room` **4×** (bloomwork, spheres, vigil-chord, antiphon), standing ban. Genuinely rare and the ones to build on: **WebGPU-compute 2× (conduct, canon — now compounding, exactly right), inline-SVG/DOM 1× (mididuet — clean and rested), raw-WebGL2 2× (schlieren, coherence — cooling).** **BANNED next cycle:** `Canvas2D flat-single-surface output` + (standing) `three.js inhabit-a-room`.
- **Over-represented technique:** `conceptual/critical subtraction & consume-once` — **4×** (toll, negative, vigil-chord, keepsake), still over-represented in the window but *correctly unshipped for four cycles* — it will clear on its own; keep the ban until it does. `concatenative corpus-conversation` is a healthy **3×** (transcript, recur, mididuet) and stays blessed — it is the "his music in genuine relationship" lane. `conducting → time-base` is **2×** (conduct, canon) and about to be 3× if `15856` ships. **BANNED next cycle:** `subtraction/consume/negative-space technique`.
- **Over-represented vibe:** the honest new finding — **`warm chromatic ember→gold→violet` is now 3×** (leanin, conduct, canon) and hardening into a palette rut *inside the exact lane I praised last window*. I credited it at 1× as "intentional"; at 3× it's a signature turning into a habit. `conceptual/critical` as a *mood* is still **4×** (toll, negative, vigil, keepsake, aging out). `achromatic/grayscale` has fallen to **2×** (schlieren, coherence) — **the standing grayscale ban can LIFT.** **BANNED next cycle:** `warm-chromatic-ember palette` (the new rut) + `conceptual/critical-subtraction mood`. **LIFTED:** `achromatic/grayscale` (down to 2×).
- **BANNED for next cycle:** `passive/self-propelling primary input` + `a fourth-straight conduct-family/hands-over-a-take piece` + `Canvas2D flat-single-surface output` + (standing) `three.js inhabit-a-room` + `subtraction/consume/negative-space technique + mood` + `warm-chromatic-ember palette`. **LIFTED:** `achromatic/grayscale` (fallen to 2×) and `raw-WebGL2` is cooling (2×, watch not ban). **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Eighth straight fortnight with an empty floor. The floor is solved; stop optimizing it.
- **Hit 2–3 criteria — 12** (schlieren, coherence, transcript, bloomwork, toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin). `recur` remains the sharpest of this band on the honest axis (same-week dated research → a real self-composing engine with recurrence arcs). `leanin` (2/5) and `transcript` (2/5) are the weakest — Canvas2D output, a single scalar over one take, no research or external-reference hook.
- **Hit 4–5 criteria — 3 (conduct, mididuet, canon).** Distribution **0 / 12 / 3**, up from `0/13/2`. `canon` clears via a **genuine first** (two decoupled time-bases from one buffer, one per hand via MediaPipe handedness — 0× before this cycle) **+ #2 (real-catalog loader + two-hand handedness + dual-voice crossover graph + WebGPU dual-grain-cloud + Canvas2D fallback ≥3) + #3 (arXiv:2604.27957 + BachDuet + the July RT-MSS cluster) + #4 (explicit cycle-2 of conduct) + #5 (same-cycle RESEARCH finding)** — the clean 5/5 candidate of the window. `mididuet` clears via **Web MIDI (a subsystem the lab had NEVER run) + co-performer-as-input (0×) + ReaLJam/CataRT reference + fresh hook.** Both are above the plate via never-run subsystems, not by-construction recombination — which is exactly the bar I set.

The metric moved for the right reason twice more. **But 3-for-3 in one lineage is the thing to name.** A 4/5 still requires a subsystem the lab has never run; the two still sitting at 0× are **WebRTC multi-user** and **MIDI/OSC *out*** (mididuet took MIDI *in*). Those are the next honest 4/5s that are NOT more hands.

## Standouts (positive)
- **15824-canon** — the deepen delivered, and it earns the headline. It took `conduct`'s single global time-base and split his ONE take into a bass voice and a treble voice via a Linkwitz-Riley-style crossover, then handed each voice's `playbackRate` to a different hand by MediaPipe handedness — so his piano plays in genuine counterpoint against itself, the pad dragging while the melody runs ahead, with a live "near unison / drifting / wide canon" readout. It reused the WebGPU compute grain engine by *index-splitting* the 20k grains into two clouds — the engine compounded instead of resetting (my #3, verbatim). That is what a cycle-2 is supposed to look like.
- **15808-mididuet** — the first time the visitor is a *co-performer* instead of a conductor/filter/curator outside playback. Web MIDI in (lab-first subsystem), his real recorded phrases retrieved concatenatively to answer your line, and — the surprise — the anticipated completion drawn as a ghost constellation on his staff *before* it sounds (ReaLJam's "anticipation made visible," reframed rule-10-clean so the "AI partner" is his own catalog). Clean inline-SVG output, the rested 1× surface. A genuine second vein, not a reskin of conduct.
- **15760-conduct** — still the origin of the break and still the template the two above build on: conducting-hand HEIGHT → the actual time-base of his take, over a WebGPU grain-cloud your hands physically push.
- **15392-recur** — still the model of the mandate on the *research-freshness* axis: a <30-day dated finding (Hawley, arXiv:2608.04378, 2026-08-05) → a self-composing engine with recurrence arcs back to first hearings. `canon` beat it on subsystem-novelty; `recur` still beats every 4/5 on the literal `today's-research → today's-build` chain.
- **15440-spheres** — the cleanest "real external data as the *carrier*, not a sim of it": true planetary ephemeris layering his takes by live aspect geometry, different every open.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15312-transcript** (2/5) — self-describes as "a deep extension of `14656 · answerback`"; incremental harmonic scoring, no external named reference, no recent-research hook, Canvas2D typographic output. The clearest local-minimum-by-extension of the window.
- **15696-leanin** (2/5) — lovely *concept* (his take only fully present when you physically lean in), but the execution lands on every over-represented axis: Canvas2D flat output (6×), face-detection copied wholesale from `1103-strange-face` (not a technique-first), a single proximity scalar over one envelope over one take, and warm-ember palette (now the 3× rut). Next to `conduct`/`canon` it reads as the safe Canvas2D draft of "camera-as-instrument."
- **15344-toll** (2/5) — where the (now-paused) subtraction lane began repeating itself. `toll` (spend-to-hear-once) and `keepsake` (keep-only-what-you-grab) are two framings of one consume-once verb; `keepsake` is the sharper. `toll` is the earlier draft.
- **15424-negative** (2/5) — a beautiful sentence ("his notes are the dark holes; you hear the silences between") and real references (Cage 4′33″), but conceptually it's a fourth subtraction piece and technically a single inverted-gain envelope over Canvas2D. Concept-strong, subsystem-thin.

## Provocations for tomorrow's dream cycle
1. **Ship `15856-canon` (duet-with-yourself) as the LAST conduct-family piece — then close the lane, on purpose.** It's the sanctioned cycle-3 deepen (record a conducting pass → replay it as a ghost second conductor) and it's already built and banked, so ship it. But say in STATE.md that it *closes the tetralogy*. Three of the last four ships are hands-over-a-take; a fifth without an exit plan is how "deepen your first 4/5" curdles into the new rut I spent four windows warning you off of. Deepen, then leave.
2. **WebRTC multi-user is STILL 0× — and it is the natural fusion of the vein you're already in.** Two people conducting ONE of his takes together (each drives a voice; the mix is the shared gesture) is `conduct` + the single most-named unclaimed subsystem, and it would be a genuine 4/5 that is NOT a fourth pair of solo hands. This is the frontier that lets you keep the momentum of the conduct lane WITHOUT staying in it.
3. **Get off Canvas2D — it is *still* 6× and unmoved for two verdicts. Build on the WebGPU-compute engine you now own (2×) or the inline-SVG surface (1×, `mididuet`).** You have two rested, distinctive output substrates in-house. There is no excuse left for a flat canvas as the primary surface.
4. **Break the warm-ember palette — it went 1× → 3× in one window, inside the lane I praised.** `leanin`, `conduct`, and `canon` are all oxblood→gold→violet. It reads as a signature now, and a signature repeated a fourth time is a habit. Grayscale is LIFTED (down to 2×) — a severe achromatic or a cold register would be maximum cross-cycle contrast.
5. **Name a <30-day dated finding as the build hook, the way `recur` did.** `canon`'s anchor (arXiv:2607.12872, July, ~4–6 weeks) was closer to the bar than `conduct`'s April caveat — but "closer" is not "inside." RESEARCH.md's newest entry is today; make tomorrow's STATE.md cite a genuinely <30-day finding as the hook, not a current-year-adjacent one you have to caveat.

## Karel-facing line
Three 4/5s in a row is your best fortnight ever — `canon` splits your take into two hands beautifully — but you've now mined one vein (hands over your catalog) four cycles deep; ship the banked duet-with-yourself to close it cleanly, then the real frontier is two people conducting one take together over WebRTC (still 0×), on anything but a flat canvas.
