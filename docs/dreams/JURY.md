# Concept Jury Verdict — 2026-09-19

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.** The **2026-09-16 PRIORITY DIRECTIVE (embodied camera conducting)** is live and governs the next several BUILD winners; this verdict is written inside it, not against it.
>
> _meta: `git fetch && checkout main && pull --ff-only` — already up to date at origin/main, no rewrite this fire. Read-only on `src/app/dream/**` — no prototype touched. **The window moved by TWO and the story turned:** `16944-nodalfigure` and `16960-tensethread` (the last of the chord-as-subject cluster) roll off the bottom; `17408-bodycast` and `17504-mirrorfold` roll on the top. Numeric last-15 review set: vaultloom · inkpressure · voiceloom · firstlook · breathline · tidemark · hearth · hall · tunesignal · vestibule · commons · timeheads · resonantpair · **bodycast · mirrorfold**. RESEARCH.md is fresh — the c1247 dive is dated **yesterday, 2026-09-18** (SoundMHPE, arXiv 2609.04902, 2026-09-04) and is chained visibly into mirrorfold; freshness honesty is being logged correctly. Votes were last verified by the c1245 fire at **48 loved, flat, none on any 17xxx** — the jury reads no live server (read-only) and takes that as the standing figure; both new ships are effectively un-reviewed. **The one fact that governs this verdict:** the last jury said the sharpest uncashed bet was two real PEOPLE, not two hands — and the lab cashed it, correctly, without first stacking a redundant third two-HAND piece. It obeyed the letter AND the spirit. The new risk is one level up._

## Summary
Give the lab its due: it read the last verdict and did the hard, correct thing. It did **not** build a third two-hand duet. It diversified the body-signal exactly as provocation #1 asked — `bodycast` moved to full-body spatial mixing — and then it cashed the sharpest uncashed bet in the file, provocation #2, head-on: `mirrorfold` is the lab's **first two-PEOPLE piece**, and it invents a genuinely new relationship (mirror each other → one unison voice; break symmetry → a canon spread across two bodies) instead of porting the tired negotiate-to-a-lock mechanic to two people. That is the best jury-response the file has recorded. But a critic has to name the two shadows over it. First: `mirrorfold` shipped `status: wip` and **cannot verify itself** — a two-person webcam is impossible in the cloud, so the entire two-people hypothesis now rests on a piece that literally needs Karel's 30-second test to know whether it works at all. Second, and larger: "point a camera at a body and conduct one take" is now **four pieces deep** (timeheads · resonantpair · bodycast · mirrorfold — five counting the loved `canon` just outside the window). The two-hand monoculture the last jury killed did not die; it moved up an abstraction level into an *embodied-conducting* monoculture, and the directive is being worn as cover for it. The lab is aimed well and building cleanly — but the review loop is still shut (flat at 48, nothing on 17xxx for ~2 weeks), and it is now betting its freshest idea on an unverifiable ship inside a category that hasn't yet drawn a single vote.

## Diversity audit
_(governs the next BUILD; camera/body-tracking INPUT is EXEMPT while the 2026-09-16 directive stands — diversify on body-signal, technique, substrate, palette instead)_
- **Over-represented input:** `autonomous / no-live-body` **5×** (inkpressure, voiceloom, firstlook, breathline, tidemark) — still over the ≥4 line but **moot**: the directive forces embodied and this cluster is aging out on its own as the window advances. `camera / body-tracking` is also **5×** (hall, timeheads, resonantpair, bodycast, mirrorfold) — over the line but **EXEMPT** under the directive, and *supposed* to repeat. Rested and healthy elsewhere: `multi-user co-presence` **2×** (hearth, commons), `tilt/gyro` **2×** (vaultloom, tunesignal), `touch/curation` **1×** (vestibule). **The input axis is not the watch — camera is doing all the diversifying work, exactly as designed. The watch is the TECHNIQUE the camera is pointed at.**
- **Over-represented output:** `Canvas2D` **5×** (voiceloom, breathline, tidemark, tunesignal, vestibule) — unchanged plurality, still firmly at ban-tier. And a NEW one crossed the line: `three.js` **4×** (vaultloom, timeheads, bodycast, mirrorfold), including **2× consecutive** (bodycast→mirrorfold) — STATE already self-flagged "next output must NOT be three.js," and the jury confirms it. Rested: `WebGL2` **2×** (hall, resonantpair), `SVG` **2×** (inkpressure, firstlook), `WebGPU` **2×** (hearth, commons — the freshest GPU shelf, and `17472-twaincircle`'s banked compute-weave substrate is the one to reach for), `audio-only` **0×** (genuinely, conspicuously rested).
- **Over-represented technique:** on a strict single-tag count, **still nothing at 4×** — the ≥4 ban does not formally bite. But the honest discretionary read is the whole verdict: **embodied camera-conducting of one take is now 4× in-window** (timeheads, resonantpair, bodycast, mirrorfold) and **5-deep counting the loved `canon`** just outside it — the tightest concept run the file has ever held, and it has replaced the two-hand run rather than ended it. Sub-clusters: `granular` **2×** (timeheads, resonantpair), `memory / accretion` **2×** (tidemark, hearth), `co-presence / N-body` **2×** (hearth, commons), `lure / front-door` **2×** (firstlook, vestibule). Credit where due — the *body-signal* genuinely diversified (two-hand → full-body → two-people); the *verb* did not.
- **Over-represented vibe:** the palette re-camped again. `neutral` **5×** (hall, tunesignal, vestibule, commons, timeheads) — over the line the last jury drew, and it stayed there. `warm` **5×** (breathline, tidemark, hearth, resonantpair, bodycast) — crossed to 5× as bodycast's amber-stage-light landed. `cool-luminous` **fell to 3×** (vaultloom, voiceloom, mirrorfold's indigo/lilac/amethyst violet-split) as the two chord pieces rolled off, with `ink/charcoal-on-cool` **2×** (inkpressure, firstlook) beside it. So the two ends that are *over* are now **warm and neutral**, not cool — the cool family is the least-camped for the first time in weeks. But "least-camped" is not "rested": there is still no genuinely empty temperature. The one truly fresh aesthetic move remains **audio-forward** (palette-moot; `audio-only` is 0× on output).
- **BANNED for next cycle:** `Canvas2D` output (5×) · `three.js` output (4×, 2× consecutive — reach for `WebGPU` / `raw-WebGL2` / `audio-only`) · `neutral` palette (5×) · `warm` palette (5×) — **the honest palette move is audio-forward (palette-moot) or the least-camped cool family, NOT a reflex warm/neutral wash.** `autonomous` input is over but moot (directive forces embodied); `camera` input is over but EXEMPT. No ≥4 technique ban formally, but **DISCRETIONARY HARD STOP: no FIFTH "conduct one take with a camera-tracked body" piece.** The category is 4-deep (5 with canon); diversify the **verb**, per the directive itself.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. Unchanged: a lure dressed as a build, superseded by `vestibule` (the real front door) and by the arrival of the signal it existed to manufacture. Still the local-minimum ship of the window.
- **Hit 2–3 criteria:** **9** — vaultloom (3), inkpressure (3), voiceloom (3), hall (3), tunesignal (3), vestibule (2), timeheads (3), resonantpair (3), **bodycast (3 — ≥3 subsystems: MediaPipe pose + 4-voice multiband HRTF spatial graph + three.js beams + per-voice level read; named ref Sounding Bodies / Videoplace; same-day research)**. Honest, un-inflated floor work — bodycast correctly declined a faked grep-0 #1 and cleared on #2+#3+#5. Note again: `hall` sits at 3 yet is the piece Karel *loved* — the floor measures build ambition, not resonance.
- **Hit 4–5 criteria:** **5** (up one — mirrorfold arrived, nodalfigure/tensethread [both 2] left) — breathline (4), tidemark (4), commons (4), **mirrorfold (4 — first multi-person piece: ≥4 subsystems [`createPoseTracker(2)` two-body + dual phase-locked buffer sources + delay-canon spatial split + per-voice analyser] + named refs [SoundMHPE, Vrengt, Videoplace] + same-day research; declined the grep-0 #1 honestly)**, and **hearth (5 — still the lab's SOLE confirmed 5/5).** The ceiling moved by exactly one honest notch this window. Good — but note *both* pieces that reached the top bench recently (commons, mirrorfold) are multi-body co-presence, and neither has drawn a vote.

## Standouts (positive)
- **17504-mirrorfold** — the standout of the window and the strongest jury-response in the file. It cashed the *exact* named bet (two real PEOPLE, not two hands) and refused the lazy version of it: instead of "negotiate two people into a lock," the split into a two-voice canon **is a musical state you play**, the vertical gap setting the interval. That the SPLIT is expressive rather than a failure is a real idea. Held short of unqualified triumph for one reason only, and it is not the build (which is clean — tsc 0, eslint 0, every hard gate passed): it is `status: wip` and **self-unverifiable** — the two-live-people synchrony feel and thresholds cannot be tested headless. It is the right bet placed on a piece that can't tell you if it landed.
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: multi-user co-presence welded to cross-session memory into a room that remembers its listeners, warm on a cold muted glance via pre-seeded sessions. With Karel endorsing the co-presence lineage and mirrorfold now extending it, still the single strongest verified-if-opened deepen target — toward intimacy, not scale.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: the ~0.36 Hz breath recovered from the *signed rubato* of his real take, a measured Hz readout, not a faked prior. The best example in the window of ambition that stays honest and legible at once.
- **17232-tunesignal** — still the most muted-phone-glance-right build: whole-viewport 1-bit static resolving into a radial figure of his music as you tilt, ears clearing at the same instant. Purpose-built for the shut review loop this verdict keeps circling.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it is triply superseded: by `vestibule`, and by the arrival of the very signal it existed to manufacture. Still the clearest local-minimum example in the file.
- **17312-timeheads** — *not* a weak build, but the clearest concept-level local minimum of the embodied run: the **symmetric** two-hand reading, superseded within one cycle by resonantpair's asymmetric one, and drew **no love** on its first clean review. It was the necessary first step of a run that has now been correctly moved past — keeping a second symmetric-ish hand-piece in the pipeline would be the redundancy the lab has (rightly) declined.
- **17408-bodycast** — an honest, clean 3/5 that did its diversifying job (body-signal → full body). Flagged here not as a bad build but as the piece most at risk of being the *fourth* entry in a category no vote has yet touched: if the embodied-conducting run stays un-reviewed, bodycast is where "diverse body-signal, identical verb" becomes visible. It earns its slot; the *pattern* it sits in is the pruning target, not the piece.

## Provocations for tomorrow's dream cycle
1. **You cashed the two-people bet — now HOLD and let it be reviewed. Do not stack a second multi-person sibling.** The lab held correctly after commons and again after timeheads; do it a third time here. `mirrorfold` is `status: wip` and its whole thesis is unproven until Karel runs the 30-second two-person test its own MORNING note asks for. Building `mirrorfold` cycle-2 (fold in twaincircle's asymmetric roles / port the WebGPU weave / a real pitch-interval canon) on top of an *unverified* wip would repeat the timeheads mistake at a higher stakes.
2. **The next fire's first job is to make mirrorfold reviewable, not to add to it.** Its biggest weakness is self-unverifiability. Either (a) build a `firstlook`-style **auto-demo of the two-body fold** (the ghost-demo path exists — surface it as a muted, no-permission looping lure that shows mirror→unison / break→canon at a glance), or (b) ship a **solo-testable** entry point (the "1 of 2, waiting for a second" mode is already there — make *that* the front door) so Karel can partly validate it alone. Do NOT build a fifth conduct-a-take piece to distract from a fourth that hasn't been seen.
3. **Read the vote before doubling down — the run is 4-deep and un-validated.** timeheads drew no love; bodycast, resonantpair and mirrorfold are effectively un-reviewed at 48-flat. If the 06:30 review is *still* flat on all 17xxx, treat "embodied camera-conducting of one take" as an **unfalsified hypothesis being built past its evidence**, not as a proven vein — and have a fallback ready that is NOT another camera-conduct-a-take piece.
4. **The verb is the monoculture now — rest it.** The directive mandates embodied *input*, not this exact *concept*. Every recent piece is "camera → body → conduct one of Karel's takes." Genuinely fresh embodied verbs the lab has never shipped: **gait / step → time-base or tempo**, **face / head-lean / gaze → dynamics or focus** (distinct power, 0×), **gesture-vocabulary** (`createGestureTracker` canned gestures — near-untouched, only `3880-conjure`). Or the true next step the two-people cash unlocks: **networked REMOTE two-person** via peerSync/WebRTC — two people in *different rooms* sharing one take. That is the real `duetlink`, and it's 0×.
5. **Substrate and palette are both boxed in — the escape is audio-forward.** `three.js` just hit 4× with 2× consecutive (bodycast→mirrorfold); `Canvas2D` is 5×; `neutral` and `warm` are both at 5×. The single move that clears the substrate ban, the palette ban, AND finally tests the lab's screen-bias in one shot is **audio-forward / audio-only** (0× output, palette-moot). If you must render, reach for the rested `WebGPU` shelf (twaincircle's banked compute-weave), not a fifth three.js or a sixth Canvas2D.

## Karel-facing line
The lab did exactly right — it didn't build a third two-hand piece, it cashed your two-real-people bet with `mirrorfold` (mirror each other → one voice, break apart → a canon across your bodies) — but it can't test itself without your 30-second two-person webcam check, and "point a camera and conduct one take" is now four pieces deep with zero votes, so the one thing that would help most today is opening one and tapping.
