# Concept Jury Verdict — 2026-09-21

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.** The **2026-09-16 embodied-camera directive** is still on the books — but read this verdict's Summary before invoking it: for the first time in ~2 weeks the lab shipped a piece that ISN'T under it, and that piece is the news.
>
> _meta: `git fetch && checkout main && pull --ff-only origin main` — fast-forwarded cleanly to `origin/main` (c1251 tremor SHIP + `tramokyo:`/`sbl-` commits, all outside the dream fence); read-only on `src/app/dream/**`, no prototype touched. **The window moved by TWO and the story finally turned:** `17088-voiceloom` and `17104-firstlook` roll off the bottom; `17584-stillpoint` and `17616-tremor` roll on the top. Numeric last-15 review set: breathline · tidemark · hearth · hall · tunesignal · vestibule · commons · timeheads · resonantpair · bodycast · mirrorfold · mudra · limn · **stillpoint · tremor**. RESEARCH.md is fresh and honest — the top entry is dated **today, 2026-09-21** (ICAD data-sonification / live-feed turn) and chains visibly into `tremor`; the chain is intact every cycle. Votes: STATE cycle 1251 records the vote as still **flat on all 17xxx — `17200-hall` is the last piece Karel loved, ~2 weeks / ~6 pieces ago.** The jury reads no live server and takes that standing figure forward. **The one fact that governs this verdict:** three juries in a row drew a hard stop at the camera-conduct verb and ordered a lane change. This window the lab did BOTH the wrong thing and the right thing, ~12 hours apart — it shipped `stillpoint`, the seventh camera-conduct piece the last jury banned as a floor, in the morning; then in the evening it shipped `tremor`, a genuinely non-embodied, self-verifying real-world-data piece — the exact lane change ordered. The engine finally turned the wheel. Do not let it turn back. **[Addendum, landed during this run:] cycle 1252 shipped `17664-strand` (long-form motif-memory recomposition of "Bath" — non-embodied, self-verifying via an SVG memory-ribbon, zero camera) while this verdict was being written — that is exactly Provocation #1 below, executed. The non-embodied lane now has two shots, not one; the review set above still ends at `tremor` (strand rolls onto the next window). The verdict stands and is vindicated: keep going, no eighth camera piece.**_

## Summary
The wall got a door in it this window, and it is worth opening before anything else: **`17616-tremor` is the first non-embodied ship in ~2 weeks and the first piece in six to make itself reviewable by construction** — every earthquake on Earth right now voices one transformed fragment of Karel's real take, it runs autonomously after one tap, and it plays the full mapping *with zero network or permission* via a bundled sample-quake fallback. That is the reviewability discipline three juries begged for, finally built into a piece instead of promised. Credit it loudly. But honesty cuts both ways, and the same window also produced `17584-stillpoint` — which is, by the plain letter of the last jury's non-negotiable floor ("no SEVENTH camera-tracked-body-conducts-one-take piece, not any body-signal"), exactly the seventh. Its own build log concedes this in writing and keeps it as "most defensible." It is a beautiful inversion (the instrument is *stillness*), but inverting the verb is still the verb — the jury said so in advance, by name. So the window is a split decision the lab made about itself: it defied the floor in the morning and honored it by nightfall. The right read is not to relitigate `stillpoint` (immutable, shipped, genuinely lovely) but to name which of the two ships is the future. It is `tremor`. The build engine remains the best this file has measured; for the first time in a month the review loop has something it can actually close — a piece that verifies itself — but only Karel's tap turns "shipped" into "landed," and the vote is still flat. The task now is not a new idea. It is depth in the lane that just opened, and no eighth camera piece.

## Diversity audit
_(governs the next BUILD; the 2026-09-16 directive exempts camera INPUT — but the jury's read is the whole verdict: the input axis is exhausted as a diversifier and the verb is done)_
- **Over-represented input:** `camera / body-tracking` **8×** (hall, timeheads, resonantpair, bodycast, mirrorfold, mudra, limn, stillpoint) — even more concentrated than last window, and technically EXEMPT under the directive. `autonomous / no-live-body` **3×** and aging honestly (breathline, tidemark, **tremor** — the freshest, real-world-data). Rested: `multi-user co-presence` **2×** (hearth, commons), `tilt/gyro` **1×** (tunesignal), `touch/curation` **1×** (vestibule), `real-world-data` **1×** (tremor — brand new, the only rested INPUT that is also a rested VERB). **The input axis is not the watch — the watch is what the camera is pointed AT, and it moved exactly once, off-camera, in tremor.**
- **Over-represented output:** `WebGL2` **4×** (hall, tunesignal, resonantpair, limn) — at ban-tier. `Canvas2D` **fell to 3×** (breathline, tidemark, vestibule — the last jury's ban worked, credit it). `three.js` **3×** (timeheads, bodycast, mirrorfold), `WebGPU` **3×** (hearth, commons, mudra — the healthiest GPU shelf). Freshest and still-rested: `audio-forward` **rose to 2×** (stillpoint, tremor) but is still the least-camped output the lab owns, `SVG` **1×** (tremor's world map). Audio-forward + minimal-SVG is the shelf that just proved it reads on a cold phone.
- **Over-represented technique:** on a strict single-tag count, **still nothing at 4×** — the formal ≥4 ban does not bite. But the honest discretionary read is the entire verdict, and it got WORSE: **"conduct one of Karel's takes with a camera-tracked body-signal" is now 7×** (timeheads, resonantpair, bodycast, mirrorfold, mudra, limn, **stillpoint**) — the seventh the last jury forbade. The one genuinely new technique in the window is tremor's **event-driven sonification** (external live data → real recording), 1×, the first non-body conducting relationship the lab has ever shipped. Sub-clusters unchanged: `granular scrub` 2× (timeheads, resonantpair), `memory / accretion` 2× (tidemark, hearth), `co-presence / N-body` 2× (hearth, commons).
- **Over-represented vibe:** `warm` **5×** (breathline, tidemark, hearth, resonantpair, bodycast) — still the worst-camped temperature, still over. `cool-luminous` **crossed to 4×** (mirrorfold, mudra, limn, tremor — the recent run leaned here to escape warm, and is now camped itself). `neutral` **4×** (hall, tunesignal, commons, timeheads) — also over. `dark/near-black` 1× (stillpoint's violet-white still-point), `mixed` 1× (vestibule). The one genuinely rested aesthetic remains **audio-forward** (palette-moot).
- **BANNED for next cycle:** `WebGL2` output (4×) · `warm` palette (5×) · `neutral` palette (4×) · `cool-luminous` palette (4×, newly over — do NOT reflex to it as the "escape" from warm, the last two runs already did) — **the honest move is audio-forward (palette-moot) / minimal-SVG on ink, or the rested `three.js`/`WebGPU` shelves.** `autonomous` input is fine (3× and it's the RIGHT lane). `camera` input is over but formally EXEMPT — and **DISCRETIONARY HARD STOP, fourth time drawn, now with a floor already broken once: no EIGHTH "camera-tracked body conducts one take" piece. `stillpoint` was the seventh the last jury banned; there is no defense left for an eighth.** Change the verb stayed unheeded on the body axis; `tremor` changed the LANE. Stay in it.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **0** — for the first time in the file's memory, no local-minimum ship in the window. `firstlook` (the sole 0–1 last time) aged off the bottom, and neither newcomer replaced it: `stillpoint` (3) and `tremor` (3) both clear the floor honestly. The floor has genuinely risen.
- **Hit 2–3 criteria:** **9** — hall (3), tunesignal (3), vestibule (2), timeheads (3), resonantpair (3), bodycast (3), mudra (3), **stillpoint (3 — #1 first audio-forward output honestly claimed, #2 real-catalog loader + pose motion-energy engine + clean⇄granular convolution crossfade + Canvas2D halo = 4, #3 Radigue/La Monte Young + the groove-synchrony paper inverted; no faked grep-0)**, **tremor (3 — #2 catalog loader + live-feed fetch/parse/rolling-scheduler + 5-dimensional event→DSP mapping + SVG world map = 4, #3 ICAD data-sonification / auditory-display lineage, #5 today's dive; honestly declined a #1 since sonification pre-exists in the lab, the fresh value is the LIVE-external-feed conducting a real take)**. Un-inflated floor work throughout.
- **Hit 4–5 criteria:** **6** — breathline (4), tidemark (4), commons (4), mirrorfold (4), limn (4 — grep-0 `createFaceTracker` first gaze conductor), and **hearth (5 — still the lab's SOLE 5/5).** The ceiling is high and steady. The point last jury made still stands, with one crack of light: a rising ceiling in an empty room is still empty — but `tremor` is the first piece built specifically to be walked under without Karel in it.

## Standouts (positive)
- **17616-tremor** — the standout of the window, and not on craft alone (it is an honest 3/5): it is the single most *strategically* important ship in a month because it is the one that finally did what three juries ordered. "Every earthquake happening on Earth right now plays one of your piano takes" is a one-line hook a stranger repeats, the surprise is genuine (the external world as the score, his recording as the sounding body), and — decisively — it is **self-verifying**: the offline sample-quake path demonstrates the entire mapping with zero network and zero permission, so it reads at a cold-phone glance. This is the reviewability template the whole lab should now copy. If any 17xxx earns Karel's first tap since `hall`, it should be this one — it is the only one that doesn't need his webcam to prove it works.
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: multi-user co-presence welded to cross-session memory into a room that remembers its listeners, warm on a cold muted glance. Still the single strongest deepen-if-building target — toward intimacy (two listeners), NOT scale, and emphatically NOT via another camera.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: ~0.36 Hz breath recovered from the signed rubato of his real take, a measured Hz readout, no faked prior — and autonomous, so it verifies itself. Now it has company: it is the *mould* `tremor` was cast from. Two non-embodied, self-verifying, glance-legible pieces is the beginning of an actual lane.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17584-stillpoint** — the pruning-pattern exemplar of this window, and a hard one to write because the BUILD is lovely (an equal-power stillness→presence crossfade, a genuinely poetic inversion, honest 3/5). But concept-level it is the clearest local minimum of *choice* the lab has sat in: it is, by the previous jury's explicit and pre-registered floor, the **seventh** "camera-tracked body conducts one take" piece — the one thing three juries said not to build — and its own build log admits it "by the letter" while keeping it. Inverting the verb (stillness instead of gesture) is the reading of "change the verb" the body axis had left, but the jury had already closed that door in advance. It is also `wip` and self-unverifiable in the cloud. Beautiful piece; the wrong piece to have built that morning.
- **17536-mudra** — still the least-reviewable ship of the run: its README states outright there is no autonomous demo, so it cannot be seen at all without a camera on live hands. Un-loved, `wip`, and now two windows deep as the reviewability anti-example. `tremor` just showed how cheap the fix is; nothing should ship like `mudra` again.
- **17264-vestibule** — a front-door / curation lure is not a concept (honest 2/5, the window's lowest), and it is superseded by the arrival of the real reviewability move it was gesturing at. The local-minimum *shape* now that `firstlook` has aged off.

## Provocations for tomorrow's dream cycle
1. **`tremor` is the lane change — now COMMIT to it, do not treat it as a one-off.** One non-embodied ship does not test a lane; a single flat vote on it would prove nothing. The lab already has TWO more non-embodied pieces built demoable-clean and one `rm -rf` from a build — `strand` (long-form autonomous motif-memory recomposition of "Bath", the thin long-form-stateful lane) and `nightside` (binaural HRTF orbit), both banked IDEAS §1251. Ship ONE of them next so the non-embodied lane gets a fair hearing before anyone concludes anything.
2. **There is no defense left for an eighth camera-conduct piece.** `stillpoint` was the seventh the last jury banned as a floor, not a nudge. If the 06:30 vote is still flat on all 17xxx, that is not ambiguity — it is the camera-conduct hypothesis confirmed falsified-by-silence (last love `hall`, ~2 weeks ago). Stay off the body axis entirely. Both banked seeds `cadence` and `vane` remain the same verb in a coat — leave them banked.
3. **Deepen real-world-data concretely — a second feed is a cheaper, higher-confidence build than any new sensor.** `tremor`'s own next-cycle notes name the moves: aurora / Kp-index, ISS passes, tide / moon-phase — "music about the sky and the sea," his catalog as the sounding body for a different slice of the living planet. Or cross-fade a second take so shallow vs deep-focus quakes draw from different recordings (a two-instrument planet). This is depth in a lane that reads, not a new front.
4. **Reviewability is now SOLVED by example — make it the floor for every future ship.** `tremor` plays its whole mapping with zero network and zero permission via the sample fallback. That is the standard now: an autonomous / data-driven piece that cannot demonstrate itself on a cold muted phone should not ship. Three consecutive `wip` self-unverifiable ships was the process emergency of the last two windows; `tremor` shows it was always avoidable. Never rebuild that stack.
5. **Palette + output: do NOT reflex to cool-luminous as the "escape."** It crossed to 4× this window precisely because the last two runs used it to flee warm — it is camped now too. `WebGL2` output is banned (4×). The genuinely rested shelves are **audio-forward / minimal-SVG on ink** (which `tremor` just proved reads) and, for palette, dark/near-black or palette-moot. If building visual, reach for the rested `three.js`/`WebGPU` — not a third cool wash.

## Karel-facing line
After three juries and two weeks of silence the lab finally changed lanes — `tremor` lets every earthquake on Earth right now play one of your real takes, no webcam, no second person, and it proves itself before you even tap Begin; open it, and if it moves you, the camera-conduct era is over and the world becomes the instrument.
