# Concept Jury Verdict — 2026-09-05

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/data/peer allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (e1e3a05→43a99c9; cycle 1219 `16848-tunevoice` at HEAD). Read-only on `src/app/dream/**` — no prototype touched. **TWO new ships since the 2026-09-04 verdict** (which reviewed through `16800-attune`): `16832-gushbody` (c1218, camera/optical-flow) and `16848-tunevoice` (c1219, voice/mic, audio-forward). The window therefore rotated by **two** slots: `ringcanon` **and** `revenant` aged out; `gushbody` + `tunevoice` entered. RESEARCH.md is fresh (dated 2026-09-05 entry for cycle 1219; the research→build chain is visible). This is the first time in a week the window moved by more than one slot — and it shows in the audit._

## Summary
The lab finally did the thing the last two verdicts begged for: it ran WIDE, the window rotated by **two**, and for the first time in eight days a ban actually **dropped** — Canvas2D 5×→4×, three.js 3×→2× (WATCH relieved). Credit where due: `tunevoice` is the lab's first genuinely **audio-forward** interaction (you *hear* the reward, not watch it), `gushbody` is its first **camera/optical-flow** sensor layer, and both are disciplined honest-first builds. **But** two things are unmoved and one is new: the ceiling is *still* frozen at **zero verified 5s** (now six days), every ship remains a 3, and a fresh rut has crystallized in plain sight — **raw-WebGL2-fragment-field is now 3× in three consecutive nights** (attune → gushbody → tunevoice). The lab escaped Canvas2D by relocating the monoculture one shader-API to the left. Diversity of *surfaces* is improving; diversity of *outcomes* is not — the build machine is a very good perpetual-motion honest-3 generator, and no build has yet been put in front of Karel's eyes or ears to become anything more.

## Diversity audit
_(last 15, numerically highest on disk: manyreturns, phosphene, driftchoir, terrafret, spectralbraid, longvigil, tonguescript, scriptorium, albumvoyage, cadencemap, chordnebula, harmonicswarm, attune, gushbody, tunevoice — `ringcanon` + `revenant` aged out; `gushbody` + `tunevoice` entered)_

- **Over-represented input:** **`keyboard / typing` — still 4× and still ban-floored** (tonguescript, scriptorium, albumvoyage, cadencemap — the manuscript block, byte-identical to yesterday; none aged out). The `catalog-playback + secondary sensor` family is now *healthily diverse* rather than clustered: `pointer/tilt` 2× (chordnebula, harmonicswarm — dropped from 3× as attune re-reads as peer-primary), `camera/optical-flow` 1× (gushbody), `voice/mic` 1× (tunevoice), `multi-user peer` 1× (attune). `autonomous/passive` 2× (manyreturns, longvigil; driftchoir self-builds too). Singletons: live-external-data (terrafret), tilt/orient (phosphene), press-hold (driftchoir), pen/pointer-paint (spectralbraid). **BANNED next cycle: `keyboard/typing` (4×).** No input WATCH this window — the sensor family is spread clean.
- **Over-represented output:** **A ban dropped for the first time in a week — and a new one is forming.** `Canvas2D` **5×→4×** (manyreturns, terrafret, spectralbraid, longvigil — ringcanon aged out) — still banned, but the WIDE rotation finally moved it. `DOM/typographic` holds **4×** (the four manuscripts — frozen). `three.js` **3×→2×** (phosphene, chordnebula — revenant aged out) — **WATCH lifted, now a rested surface.** **NEW / the headline of this audit: `raw WebGL2 fragment/feedback field` is now `3×`** (attune, gushbody, tunevoice — three straight nights) — this is the relocated rut, AT the WATCH ceiling, one ship from a ban. `raw WebGPU compute+render` 1× (harmonicswarm); `audio-only` 1× (driftchoir). **BANNED next cycle:** `Canvas2D` (4×) **and** `DOM/typographic` (4×). **WATCH (AVOID):** `raw WebGL2 fragment field` (3× — the new monoculture; do NOT make it 4×).
- **Over-represented technique:** `prosody / text-score cross-modal mapping` — **still 4×** (the four manuscripts), banned, unmoved. `loops-of-one-take` family (canon + incommensurate phase-drift) **dropped to 2× direct** (manyreturns, driftchoir — ringcanon + revenant, both canon/loops, aged out) **but the four manuscripts still run incommensurate-loop phasing as substrate → ~6×**; keep hard-closed. The singletons to preserve and extend, not re-ban: YIN pitch→consonance reward (tunevoice), Horn–Schunck optical-flow feedback (gushbody), co-presence attunement scalar (attune), volumetric FBM raymarch (chordnebula), GPU particle-life w/ chord attraction (harmonicswarm), spectral cross-synthesis (spectralbraid), stateful memory-accretion (longvigil), live-data→sample sonification (terrafret), harmony-carved geometry (phosphene). **BANNED next cycle:** `prosody/text-score` (4×) + `loops-of-one-take family` (2× direct / ~6× w/ substrate — keep closed).
- **Over-represented vibe:** `near-dark / intimate typographic` — **still 5×** (driftchoir + the four manuscripts), banned, unmoved (all five in-window). **NEW cluster: `warm / ember intimate` 2×** (attune candlelit, gushbody warm-embers) — WATCH, do not make it 3×. `bright luminous cosmic field` holds **2×** (chordnebula, harmonicswarm) — WATCH. `two-signal duotone` 2× (terrafret, spectralbraid). `monochrome-base-plus-one` **3×→2×** (manyreturns ashlight, phosphene silver+jewel — ringcanon's graphite+chartreuse aged out). `cool→warm consonance shift` 1× (tunevoice — teal→amber-gold, the clean new break). **BANNED next cycle:** `near-dark/intimate typographic` (5×). **WATCH:** `warm/ember intimate` (2×), `bright cosmic field` (2×), `two-signal duotone` (2×).
- **BANNED for next cycle:** `keyboard/typing input` (4×) + `Canvas2D output` (4×) + `DOM/typographic output` (4×) + `prosody/text-score technique` (4×) + `loops-of-one-take technique family` (2× direct, ~6× w/ substrate — keep closed) + `near-dark/intimate typographic palette` (5×). **WATCH (at/near ceiling — AVOID):** `raw WebGL2 fragment field output` (3× — the new rut), `warm/ember intimate palette` (2×), `bright cosmic field palette` (2×). **Honest headline: the WIDE cycle worked, partially.** Two counts fell (Canvas2D, three.js) where a DEEP single-ship rotated nothing — proof that WIDE is the only build-side ban-drainer. But the *manuscript block* (keyboard/DOM/prosody/near-dark) is still pinned at 4/4/4/5 because none of the four manuscripts aged out, and the lab manufactured a brand-new rut (raw WebGL2 field) in the very act of escaping the old one.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Twenty-first straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 14** (phosphene 2, driftchoir 2, terrafret 2, spectralbraid 3, longvigil 3, tonguescript 3, scriptorium 3, albumvoyage 3, cadencemap 3, chordnebula 3, harmonicswarm 3, attune 3, **gushbody 3, tunevoice 3**). Both new ships are honest 3s: **gushbody** cleared #2 (catalog loader + FFT + chord tracker + camera optical-flow + WebGL2 ping-pong feedback = 5 subsystems) + #3 (Adam Ferriss *Gush* / Andrew Benson Horn–Schunck shader) + #5 (RESEARCH §2026-09-04 c1218). **tunevoice** cleared #2 (6 subsystems: catalog + opening-filter chain + mic analyser + YIN pitch detect + time-matched chord/consonance scorer + WebGL2 halo) + #3 (Oliveros *Deep Listening* / Helmholtz roughness / arXiv 2509.01588) + #5 (RESEARCH §2026-09-05 c1219). Both correctly **declined** the false lab-first (pitch detection: 114-live-harmonize/718-duet-paths; optical flow: 1590-body-mirror/5096-theurgy — grep-checked, #1 not claimed) — the terrafret discipline lesson now applied five ships running.
- **Hit 4–5 criteria — 1 (manyreturns 4).** Distribution **`0 / 14 / 1` — frozen, identical to the last two windows.** The ship-for-ship trajectory is now **thirteen consecutive honest 3s.** **Sixth straight day at zero verified 5s.** Both new ships shipped stamped "not run in this env — `tsc`/ESLint only, needs Karel at the page." **The single load-bearing sentence, unchanged and now even harder to argue with:** the lab has cashed WebGPU-compute, multi-user/WebRTC, camera/optical-flow AND voice/audio-forward across the recent window — four fresh categories — and the top band gained **zero** points. Surface was never the gate. Verification is. The build lever moves the *diversity* number; it has never once moved the *ceiling*.

## Standouts (positive)
- **16848-tunevoice** — the headline, and the single most valuable piece in the backlog *for one specific reason*: it is the **fastest verdict Karel can give**. It's the lab's first audio-forward interaction (sing a chord tone → his recording's veil lifts, the root blooms; drift off → it closes), it cashes the "test the screen bias" menu category, the mic is a clean analysis-only dead-end (rule-10-safe, zero synth/howl), and — crucially — it ships a **solo auto-demo** so Karel gets a verdict with no second person and, if he denies the mic, on a simulated voice. That is a ~20-second ears-test on a phone. Everything else in this verdict is subordinate to Karel opening this one page.
- **16768-harmonicswarm** — still the technical high-water mark of the window and the strongest *eye*-call: the lab's only raw-WebGPU-compute piece, Karel's sounding chord rewriting a 6×6 inter-species attraction matrix so consonance is literally *visible as physics* (major → filaments, minor → scatter). Phone-renderable now that WebGPU is default on iOS 26.
- **16368-manyreturns** — for the **third** window running, the lone 4 and the most obvious deepen target the lab keeps walking past. Reached 4 by extending a real thing (a self-building incommensurate ensemble that genuinely ages) until it deepened. Still the likeliest honest path to a verified 5 — but on a NEW axis (memory you can return to and hear the accumulation of), NOT the ~6×-closed loop engine it stands on.
- **16832-gushbody** — worth naming as the cleaner of the two new sensor cashes conceptually: motion→feedback-advection where *his audio* is the injected light, so moving your hands "opens" the sound as well as the fluid. Honest limitations section is exemplary (cheap single-tap flow, RGBA8 clipping) — the kind of self-audit the manuscripts should imitate.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The manuscript block** (tonguescript → scriptorium → albumvoyage → cadencemap, all 3/5) — unchanged structural drag, now with a countdown worth stating: it is *the reason four of the six bans (keyboard, DOM, prosody, near-dark) cannot drop*, and those four will stay pinned until 16656–16720 age off the 15-window. This WIDE cycle pushed the window forward by two — **two more WIDE cycles (or a lot of DEEP ones) drain them; nothing else will.** Do not add a fifth manuscript under any framing.
- **The raw-WebGL2-fragment-field cluster** (attune, gushbody, tunevoice — 3×) — flagging this as a *concept-level* pruning note, not a quality knock on any of the three: the lab has, in three nights, made "off Canvas2D / off three.js → raw WebGL2 fragment field" its new default rendering answer. This is the exact shape of how Canvas2D became a rut (a good escape hatch used four times in a row). Break it *now*, at 3×, before it's the next thing this verdict is banning at 5×.
- **16496-terrafret** (2/5) — the false-"first" cautionary tale, now with *five* honest-first-declined counter-examples (harmonicswarm, chordnebula, attune, gushbody, tunevoice). Cite the set as the settled before/after of first-claim discipline; it's no longer an open issue.
- **16448-phosphene** (2/5) & **16464-driftchoir** (2/5) — two honest disciplined-2s that read well and reach for nothing (three.js-solid; audio-only). Fine as rested-surface breathers, not concepts to extend toward a 5.

## Provocations for tomorrow's dream cycle
1. **Verification is STILL the only lever — and `tunevoice` is the shortest path to pulling it that the lab has ever shipped.** A ~20-second solo ears-test on a phone, mic optional, auto-demo fallback. Nothing new should ship until Karel opens ONE ready piece — and this is the one to lead with, above the eye-calls (harmonicswarm, gushbody, chordnebula, attune-two-tab). A single "yes, this is a 4" unfreezes a six-day ceiling; a fourteenth honest-3 does not.
2. **Do NOT ship a fourth raw-WebGL2 fragment field.** It's 3× in three straight nights and is now the audit's live WATCH — the relocated rut, caught early this time. If the next fire renders, aim it at a **rested** surface: `three.js` just dropped to 2× (WATCH lifted), `audio-only` is 1× (driftchoir), or go non-shader entirely. Renderer diversity is exactly the muscle that just proved it works — keep using it.
3. **Build on the freshest research nobody has touched: the `tunevoice` "could still become" hook, dated TODAY.** RESEARCH.md §2026-09-05 proposes a **call-and-response deepen** — when you hold a consonant tone, briefly *duck* his recording so your voice sits inside a rest in his playing (a zero-synth conversation with the actual piano), plus a second consonance target (3rd/7th) so singers chase inner voices, not just the root. That is a same-surface DEEPEN of a fresh piece toward a 4 — but gate it on #1: it's a guess about a guess until tunevoice itself is verified. Don't build the deepen before the base is confirmed to land.
4. **If the fire goes DEEP, aim it at `manyreturns` (the lone 4) on a NEW axis — memory you can return to and read.** Its path to a 5 is real accreting state, not more phasing. The cashed multi-user (attune) and audio-forward (tunevoice) surfaces both offer natural versions — but every such deepen is a guess until one base piece verifies. See #1.
5. **The build lever is now officially a *diversity* lever, not a *ceiling* lever — plan accordingly.** This cycle proved WIDE drains bans where DEEP-single cannot. So while the ceiling waits on Karel, the only build-side work worth doing is WIDE fires that (a) push the window forward to drain the manuscript block and (b) do NOT relocate the monoculture to raw-WebGL2. Two goals, one move: WIDE, on rested renderers.

## Karel-facing line
Good news first — the lab ran WIDE, added its first voice piece and first camera piece, and a ban finally dropped; but the ceiling is *still* frozen at zero verified 5s for the sixth day straight, and the fastest way to break it is sitting ready: `tunevoice` is a 20-second test — press play, hum a note, and tell me if his recording opens to meet you.
