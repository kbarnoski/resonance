# Concept Jury Verdict — 2026-08-25

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/hand/biosignal allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (cycle 1197 `16032-headnave` + the 2026-08-24 verdict at HEAD; no forced-update divergence). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` per scope fence — no prototype touched. Only one new ship since yesterday's verdict (`16032-headnave`, cycle 1197); `15328-bloomwork` aged out of the window._

## Summary
The lab did the honourable thing and cashed two of yesterday's provocations in a single ship: `headnave` retires the 5× warm-palette rut for Ikeda black-white-red (#3) and graduates `antiphon` into an embodied, head-steered spatial room instead of restarting it (#2). The craft is real and the README is honest — it never claims a "first," which is already an improvement over last window's two overclaimers. But I ran the grep that provocation #1 mandates, on the provocation itself, and it does not survive: `576-presence-room` already feeds MediaPipe webcam **head pose straight into the Web Audio `AudioListener`** in an HRTF field ("lean and turn your head and the music physically moved around you — your own face is the listener"). So the "genuine 0× embodied webcam-head-position spatial-audio room" I named yesterday, and that STATE.md §1197 and RESEARCH.md both bill as the lab's first, is **not** a first. The uncomfortable part: this time the un-grepped overclaim was **mine**, not the builder's. The 4/5 band has not moved in three straight windows because the "firsts" keep being revivals and refinements — and the jury is now part of that pattern.

## Diversity audit
_(last 15, numerically highest on disk: toll, recur, negative, spheres, vigil-chord, antiphon, keepsake, leanin, conduct, mididuet, canon, answerpiano, duetlink, morphonate, headnave — bloomwork aged out; headnave entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **5×** (recur, negative, spheres, answerpiano, morphonate). Down one from last window (bloomwork aged out); `headnave` is genuinely active (lean/turn/walk), so it does not add to the bucket. Still over the floor. The quieter story is that **`camera/webcam head` regrew to 4×** (leanin, conduct, canon, headnave) — the input I *celebrated* last window has itself become a cluster. **BANNED next cycle:** `passive/self-propelling primary input` (unless carrying a genuinely-grepped never-run subsystem) **+ `camera/webcam-as-primary` (now 4× — rotate off).**
- **Over-represented output:** `Canvas2D flat single-surface` — **5×** and still the runaway leader (toll, recur, negative, keepsake, leanin). Unchanged. But **`GPU-field output` is now 5×** (conduct, canon, morphonate, duetlink, headnave) — the escape substrate I told you to keep building has quietly become the new default room. I won't ban it, but I'm retiring the *celebration*: shipping WebGL2/WebGPU is no longer, by itself, evidence of ambition. `three.js inhabit-a-room` holds at **2×** (spheres, vigil) — clear. inline-SVG/DOM rested at 2× (mididuet, answerpiano). **BANNED next cycle:** `Canvas2D flat-single-surface output`. **WATCH:** `GPU-field` (5× — now the default, not the escape).
- **Over-represented technique:** `conceptual/critical subtraction & consume-once` — **4×** (toll, negative, vigil-chord, keepsake), correctly unshipped again this window — one more clean window and it clears. `conducting→time-base` 2× (conduct, canon), `concatenative corpus` 2× (recur, mididuet), both healthy. `head-tracked HRTF navigation` is now 2× in-window (antiphon keyboard-nave + headnave head-nave) and, per the grep above, **≥3× lab-wide** counting `576-presence-room` — noting it so it is not re-nominated as novel. **BANNED next cycle:** `subtraction/consume-once/negative-space technique`.
- **Over-represented vibe:** `warm chromatic (ember→gold→violet / warm pitch-class-hue)` — **5×** (leanin, conduct, canon, mididuet, answerpiano). Still 5×, but note it is now entirely in the *older* tail — the three newest ships (morphonate, duetlink, headnave) are all cold/achromatic — so the rut is aging out under the ban, hold it one more window. The new problem is the overswing: **`cold / achromatic` is now also 5×** (toll, keepsake, morphonate, duetlink, headnave). The palette pendulum went from a warm rut straight into a grayscale rut. `headnave`'s Ikeda B-W-R is the one with an actual accent (oxblood signal-red) and is the most defensible of the five. **BANNED next cycle:** `warm-chromatic / warm pitch-class-hue palette` **+ `pure grayscale/achromatic-as-the-easy-out` (now 5× — fleeing warm into monochrome is not a palette decision).**
- **BANNED for next cycle:** `passive/self-propelling primary input` (unless carrying a genuinely-grepped never-run subsystem) + `camera/webcam-as-primary` (4×) + `Canvas2D flat-single-surface output` + `subtraction/consume-once technique + mood` + `warm-chromatic / warm-pitch-class-hue palette` + `pure grayscale/achromatic default`. **WATCH:** `GPU-field output` (5× — no longer proof of ambition). **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Tenth straight fortnight with an empty floor. The bottom is solved; ignore it.
- **Hit 2–3 criteria — 12** (toll 2, recur 3, negative 2, spheres 3, vigil-chord 2, antiphon 2, keepsake 2, leanin 2, answerpiano 3, duetlink 2, morphonate 3, **headnave 3**). `headnave` lands here, not in the 4/5 band — and that is the honest headline STATE.md obscures. Its real score once criterion #1 is denied: **#2** ≥3 subsystems (real-catalog loader + FaceLandmarker 6DOF pose + HRTF PannerNode field + WebGL2 renderer + pointer fallback) + **#3** named refs (graduates antiphon + Navig-AI-tion CHI 2026) + **#5** same-cycle research. That is a clean, respectable **3/5** — it does **not** need the fake #1 to justify itself, which is exactly why claiming it was unforced.
- **Hit 4–5 criteria — 3 (conduct 4, mididuet 4, canon 5).** Distribution **0 / 12 / 3**, *unchanged for the third window running* (`0/12/3` → `0/12/3` → `0/12/3`). This is the number to sit with. Three fortnights in a row the lab has shipped a loudly-billed "first" (MIDI-out, multi-user, now head-position) and the top band has not moved once, because each was a revival or a rule-10-clean refinement of a technique the grep already finds. **The ceiling is not going to move by announcing louder. It moves by finding a subsystem the grep genuinely can't find — or by dropping the "first" framing entirely and just extending `canon`/`morphonate` into real 5/5s.**

## Standouts (positive)
- **16032-headnave** — the best-built piece of the window and, unlike last fortnight's headliners, **honest in its own README** (it says "graduates antiphon," "a real second input modality" — never "first"). Full 6DOF via the facial-transformation matrix (not the hand-rolled landmark pose `576` used), rule-10-clean spatialization of six real takes (where `576` synthesized a JI drone and would fail rule 10 today), a working pointer fallback so it demos with zero camera prompts, and the palette courage I explicitly asked for. Credit where due: the builder didn't overclaim — the log and my own provocation did. **Relabel it "graduated the head-listener room to the real catalog + 6DOF," and it's a piece to be proud of.**
- **16000-morphonate** — still the honest breakout, and still un-built-upon. Autonomous, long-form, reads harmony (not an FFT of it). The banked `inkstroke` (Calliphony partner) that would deepen the same "his music grows a living surface" vein is *still sitting in IDEAS* — you spent this cycle on headnave instead. Cash it.
- **15824-canon** — the window's lone genuine 5/5 (two decoupled time-bases from one take, one per hand). Still the model of what a real cycle-2 deepen produces, and the counter-example to every "first" that turns out to be a revival.
- **15440-spheres** — still the cleanest "real external data as the *carrier*, not a sim of it," and still the *only* piece in that lane (1×). Yesterday's provocation #5 to cash a banked sonification build (`seismscore`/`heliosong`) went unactioned. Two windows now.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **16032-headnave** (the overclaim, not the build) — I am pruning the *claim*, not the prototype. As shipped it is a clean 3/5. What has to go is the STATE.md/RESEARCH.md framing that it is "the lab's first embodied webcam-head-position spatial-audio room": `576-presence-room` did webcam-head-pose → `AudioListener` years ago, and the "presence" family (`677`/`710`/`724`) worked adjacent body-pose→HRTF ground. The subsystem is a revival dressed as a first — the identical error criterion #1 exists to stop, made this time by the jury.
- **15920-duetlink** (2/5) — unchanged from last window and still in frame: strip the (false) "first multi-user" banner and it's a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors.
- **15696-leanin** (2/5) — still lands on every over-represented axis at once (Canvas2D flat, warm-ember, face-detect copied from `1103-strange-face`, one proximity scalar). The safe Canvas2D draft of "camera-as-instrument" next to `conduct`/`canon`.
- **15600-keepsake / 15344-toll** (2/5 each) — the two framings of the consume-once verb, correctly paused; residue of a lane one clean window from clearing.

## Provocations for tomorrow's dream cycle
1. **Grep the "first" — including when the jury names it.** Before STATE.md or a README calls anything a lab-first, grep INDEX + all READMEs for the *subsystem*, not the wrapper. Head-pose→listener = `576-presence-room`. MIDI-out = `291-harmonograph`/`9768-mpelace`. Multi-user = ~24 priors. Three windows, three refuted firsts. And I'm holding myself to the same bar: a provocation that names a "genuine 0×" is worthless until *I* have run the grep. `headnave` should be relabeled "revived the head-listener room, rule-10-clean, cold since `576`" — that's true and still praiseworthy.
2. **Stop minting firsts; cash the two banked seeds you already flagged.** You have two concrete, already-researched builds sitting in IDEAS that would move the needle more than a fourth announced first: **`inkstroke`** (Calliphony brush-as-bow, deepens the honest `morphonate` breakout, arXiv:2608.03040) and **`seismscore`/`heliosong`** (real-world-data-as-carrier — a lane still at 1× after months of you flagging it). Both are grep-safe and neither pretends to be new. Ship one.
3. **The palette pendulum overswung — pick a genuine third.** Warm is 5× (aging out), but cold/achromatic is *now also 5×*. Fleeing an ember rut into a grayscale rut is not a palette decision. `headnave`'s Ikeda B-W-R at least carries a signal-red; next piece should be neither warm-chromatic nor monochrome — a committed third register (deep-cyan-on-bone? a single saturated non-warm hue?) that would look wrong next to *both* current ruts.
4. **Rotate off camera.** The input I celebrated last window (`conduct`/`canon`) plus `leanin` and `headnave` make camera/webcam a 4× cluster now. The most-rested live inputs, all with banked builds ready: **tilt/DeviceOrientation** (`15888-tiltpour`), **Gamepad** (`15904-gamepad`), **pen/brush** (`inkstroke`), **mic-as-secondary**. Pick one that is not a lens pointed at a face.
5. **If you insist on a real 4/5, extend `canon` or `morphonate` — the two pieces that earned their band honestly — into 5/5s.** `canon` → a third mid voice / beat-synced grain emission from his onsets. `morphonate` → the RD engine promoted to shared `_shared/` infra + a persistent per-album membrane. A verified cycle-2 that the grep *confirms* builds on your own prior work beats a cycle-1 that the grep *refutes* as new. `canon` is proof you can do it.

## Karel-facing line
`headnave` is real craft and the palette courage I asked for — but it's the third straight "lab first" the grep refutes (`576-presence-room` drove the webcam-head listener years ago, and this time the un-grepped overclaim was mine, not the builder's), the 4/5 band hasn't moved in three windows, and two of my asks — build-on-`morphonate`, real-world-data — are still uncashed; stop announcing firsts and ship the banked seeds.
