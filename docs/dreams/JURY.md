# Concept Jury Verdict — 2026-09-26

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction; where any provocation here conflicts with AGENT.md ABSOLUTE rule 10, **rule 10 wins.** The 2026-09-16 EMBODIED-CAMERA directive is also binding: `camera` / `body-tracking` INPUT is EXEMPT from the ≥4-in-15 ban — it is *supposed* to repeat. This verdict drives diversity through the OTHER axes.
>
> _meta: `git fetch && checkout main` — origin/main had again been **force-updated** and the fresh clone diverged 50/50, so `pull --ff-only` aborted; I `git reset --hard origin/main` to survey the true head (read-only on prototypes, nothing committed from the reset). Numeric last-15 set: mirrorfold · mudra · limn · stillpoint · tremor · strand · overhead · firmament · scriptorium · ledger · reharmonize · cantormap · aureole · **fingerloom · formfold** (`resonantpair` and `bodycast` have now rolled OFF the bottom; `fingerloom` and `formfold` rolled in). RESEARCH.md is fresh — top entry dated **today, 2026-09-26** (full-body FORM-conducting + gait cadence), chaining into `formfold`; note the dive was **honest** that it found nothing verifiably ≤30d and refused to badge criterion E on a stale citation. I did not re-`curl` votes this cycle; the standing love signal (`17536-mudra` newest ♥, six-for-six embodied) is unchanged in STATE._

## Summary
The lab did the two right things and one wrong thing this window. Right: it cashed yesterday's provocation #3 hard — `formfold` (full-body openness + lean) and `fingerloom` (ten independent fingers) genuinely **rotate the body signal off the two-hand/face cluster**, and in doing so they finally broke the warm-amber palette rut and the three.js output rut (both are Canvas2D, one bioluminescent-teal, one prismatic). That is real, honest diversity progress inside the loved lane. Wrong: it **ignored provocation #2 and made the verification debt worse.** Yesterday there were three untested `wip` camera pieces; today `aureole` converted to `demoable` (good) but **two more** unverified pieces were minted, so the backlog is now **four** (`limn`, `cantormap`, `fingerloom`, `formfold`) — the lab's entire recent value, all sitting one 30-second Karel webcam check away from being real, and it is minting that inventory faster than it converts it. Cycle 1261's own log even quotes the warning ("I'm stockpiling untested camera pieces") and then shipped another anyway — the one concession being it fanned out 2 explorers instead of 3.

## Diversity audit
- Over-represented input: **camera / embodied tracking** (8×: mirrorfold, mudra, limn, stillpoint, cantormap, aureole, fingerloom, formfold) — **EXEMPT per the 2026-09-16 directive; not banned.** The real runner-up to ban is **real-world-data-feed** (5×: tremor, overhead, firmament, scriptorium, ledger).
- Over-represented output: **three.js** (4×: mirrorfold, overhead, scriptorium, ledger). Note the two newest pieces are **Canvas2D** (fingerloom, formfold) and are actively pulling this axis back toward health; raw WebGL2 is 2× (limn, cantormap), SVG 2× (strand, firmament), WebGPU still just 1× (mudra).
- Over-represented technique: **granular-scrub of the real take** (5×: stillpoint, strand, overhead, scriptorium, ledger) and **event/data sonification** (5×: tremor, overhead, firmament, scriptorium, ledger).
- Over-represented vibe: **warm-candlelit-amber, 4×** (scriptorium, ledger, reharmonize, cantormap) — **down from 5× and dropping**: both new pieces broke it (fingerloom = cool bioluminescent teal→bone; formfold = prismatic/spectral), and `aureole` is achromatic silver/bone. Cool-violet is 3× (mirrorfold, limn, strand); near-black cosmic-sky 3× (stillpoint, overhead, firmament). Keep it broken.
- **BANNED for next cycle:** real-world-data-feed input · three.js output · granular-scrub-of-the-take technique · event/data-sonification technique · warm-candlelit-amber palette *(at the 4× line — don't relapse into it)*. **Camera/body-tracking input remains EXEMPT** — but see Provocation #1: the constraint next cycle is not palette, it's **verification.**

## Ambition floor stats (last 15 prototypes)
_(criteria: A novel-technique-to-lab · B ≥3 subsystems · C specific named reference · D multi-cycle commitment · E research from last 14 days)_
- **Hit 0-1 criteria: 2** — `overhead` and `firmament`. Unchanged local minima from the data detour: both clear ≥3 subsystems nearly for free off the shared loader+analyser+renderer, but neither brought a technique the lab hadn't run (`tremor` established data-sonification the week before) and both lean on a generic ICAD/Sonification-Handbook-tier citation. They exist because the data lane was comfortable.
- **Hit 2-3 criteria: 11** — mirrorfold, mudra, limn, stillpoint, tremor, strand, scriptorium, ledger, reharmonize, **fingerloom**, **formfold**. The crowded, healthy middle. Both new pieces sit here honestly at 3/5: `formfold` = A (first "travel the FORM with your body" verb + Foote-novelty section derivation) + B + C (Foote 2000, a real named reference in the README) — but NOT E, because the dive correctly declined to badge a stale citation. `fingerloom` = A (first per-finger independent-flexion reader; conducts orchestration/voice-density, never touched before) + B + E — its citations (IJFMR/WJARR 2026) are the generic-latency-proof kind, so C stays unbadged. Neither reached for D.
- **Hit 4-5 criteria: 2** — `cantormap` (A first-spatialization + B + C Chowning + E) and `aureole` (A convolver-IR-from-its-own-buffer distance conductor + B + C Blesser & Salter + E). These remain the window's ambition ceiling — and `aureole` is the ONLY one of the four camera frontier-pieces that is actually verified (`demoable`).

## Standouts (positive)
- **`aureole`**: the window's best piece and the only frontier-lane build that shipped **verified** (`demoable`, not `wip`). Genuinely novel technique — the convolution IR is a decaying slice of Karel's *own* recording, so the cathedral is made of the piano's own tail — a real named lineage (Blesser & Salter, direct-to-reverberant = distance), and it broke the palette rut to achromatic before anyone asked. 4/5.
- **`cantormap`**: first piece to conduct **spatialization** (two hands place his band-voices around your head in a true HRTF field). A criterion-A verb the lab had never touched, Chowning-anchored. 4/5. Only knock: warm-amber and unverified.
- **`formfold` + `fingerloom` (as a pair)**: credit for *doing the diversity work the jury asked for* rather than diversifying on paper. Full-body form-navigation and per-finger orchestration are two distinct new body signals, both pitch-clean (no playbackRate/grain mangling of his piano), both breaking the output+palette ruts. This is the lab climbing, not coasting.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **`overhead`** (0-1): the ISS-pass data feed. Local minimum — data-sonification was already established by `tremor`, so it added a feed, not a technique; generic citation. It exists because the data lane felt safe.
- **`firmament`** (0-1): computed sun/moon diurnal morph. Same story — the third sky/data piece in a row, no new technique, no love. Both `overhead` and `firmament` are the concrete residue of the "pivot off camera" week the vote had already falsified.

## Provocations for tomorrow's dream cycle
1. **Verification debt is now the #1 risk — louder than yesterday. Do NOT mint a fifth `wip` camera piece.** Four unverified pieces (`limn`, `cantormap`, `fingerloom`, `formfold`) ARE the lab's recent value and all gate on one 30-sec Karel check the agent physically cannot run headless. If a BUILD fires before Karel checks, it must be **self-verifying** (non-camera, or camera with a genuine headless-testable core) — otherwise do a research/hold cycle. Converting inventory beats minting more of it; this is the second cycle in a row the lab minted instead.
2. **Cash score-following — it uniquely solves BOTH problems at once.** Onset/beat detection so a played (`reharmonize` keyboard) or hummed phrase steers his recording's tempo/section is (a) the still-uncashed criterion-A verb, a real lab-first reactive accompaniment, AND (b) **phone-testable / self-verifying** — it does NOT add to the camera verification debt. This is the single highest-value next build. Carried from yesterday #4, now sharper: it's the escape hatch from the wip trap.
3. **Ship gait — but only after the backlog clears.** `gaitpulse` is built + banked (IDEAS §1261) and lost only because "gait→tempo exists in `1200-gait-loom`" — but it's pitch-clean amplitude-gating, explicitly distinct, and gait is the LAST body signal from prov #3 never shipped. It's the resurrect-next for the embodied lane — just not while four camera pieces await a webcam.
4. **Retire or build `commons` — criterion D is claimed by literally nobody.** It rolled off the window un-deepened after four juries named it the multi-cycle build. Either commit a cycle-2 (conducted-ensemble layer / shared score) or write one honest STATE line retiring it as "single-cycle, not returning," so the log stops carrying a promise it doesn't keep. Carried, unmoved, from yesterday #5.
5. **Palette: hold the break.** Warm-amber is down to the 4× line and the three freshest registers (achromatic, bioluminescent-teal, prismatic) are healthy new ground. Don't relapse into amber to feel comfortable; the ruts you just broke stay broken.

## Karel-facing line
The lab finally put your whole body and ten fingers on the controls, in fresh colors your juries kept asking for — but it's now sitting on FOUR camera pieces it can't test itself, so the 30 seconds only you can spend (do limn / cantormap / fingerloom / formfold actually track you on your desk webcam?) is worth more today than anything it can build next.
