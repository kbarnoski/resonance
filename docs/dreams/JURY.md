# Concept Jury Verdict — 2026-09-03

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→52cd251; cycle 1216 `16768-harmonicswarm` at HEAD). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. TWO new ships since the 2026-09-02 verdict: `16752-chordnebula` (1215) + `16768-harmonicswarm` (1216); `16144-spiralkeep` and `16192-roomtone` aged out of the 15-window. RESEARCH.md is fresh (dated 2026-09-03 entries for cycles 1215 + 1216)._

## Summary
**The lab listened — and it's the best two-night stretch in the window.** Yesterday's verdict said, in order: break the manuscript monoculture, get off Canvas2D and DOM, and cash the never-built WebGPU-compute field ("the biggest single swing available"). In two nights the lab did all three: `16752-chordnebula` (three.js volumetric raymarch, cosmic-ambient) and `16768-harmonicswarm` (**raw WebGPU compute — the 0× category finally cashed**), both bright luminous fields, both sharing NONE of the manuscript tag-stack. Credit is real; this is exactly the WIDE reset that was demanded. **And yet the ceiling still did not move** — both new ships are honest 3s, and with the lone extra 4 (spiralkeep) aging out, the top band actually *dropped from 2 fours to 1*. **Fourth straight day at zero verified 5s.** The lab has now proven it can cash a new surface on command; what it has not done, across seven-plus ships and two modes, is get a single one of them **in front of Karel's eyes or ears.** New surfaces are not the missing ingredient. Verification is.

## Diversity audit
_(last 15, numerically highest on disk: loomvoice, revenant, ringcanon, manyreturns, phosphene, driftchoir, terrafret, spectralbraid, longvigil, tonguescript, scriptorium, albumvoyage, cadencemap, chordnebula, harmonicswarm — spiralkeep + roomtone aged out; chordnebula + harmonicswarm entered, the two non-manuscript ships the verdict asked for)_

- **Over-represented input:** **`keyboard / typing` — still 4× and still at the ban floor** (tonguescript, scriptorium, albumvoyage, cadencemap — the manuscript lineage). No fifth was added (both new ships are catalog-playback + pointer/tilt), so this will start dropping as the lineage ages out, but it is still maxed this window. Behind it: `catalog-playback + pointer/tilt secondary` **2×** (chordnebula, harmonicswarm — the two new ships, a fresh cluster worth watching), `autonomous/passive` **dropped 3×→2×** (manyreturns, longvigil — spiralkeep aged out, so this **cleared its WATCH on its own**), `tilt/orient` 1× (phosphene). Everything else 1×: pen/pointer-paint (spectralbraid), live-external-data (terrafret), mic-secondary (loomvoice), multi-touch (revenant), gamepad (ringcanon), press-hold (driftchoir). **BANNED next cycle: `keyboard/typing` (4×).** **WATCH:** `catalog-playback + pointer/tilt` (2×, emerging — see palette note).
- **Over-represented output:** **TWO tags still at the ban floor, but the escape has finally begun.** `Canvas2D` holds at **5×** (ringcanon, manyreturns, terrafret, spectralbraid, longvigil) — a week-plus of hard bans and it *still* hasn't dropped a count, BUT the last two ships were the first non-Canvas2D non-manuscript builds in the window, so ringcanon + manyreturns (the two oldest) age out next and this count will finally break. `DOM/typographic` holds **4×** (the four manuscripts) — same story, will drop as the lineage ages out. The real news: **`three.js` climbed 2×→3×** (revenant, phosphene, **chordnebula**) and is now AT the WATCH ceiling — one more three.js ship bans it. And **`raw WebGPU compute+render` registers 1× for the first time ever** (harmonicswarm) — the lab-first surface, cashed. Elsewhere: `inline-SVG/DOM art` dropped 2×→1× (loomvoice; roomtone aged out), `audio-only` 1× (driftchoir). **BANNED next cycle:** `Canvas2D` (5×) **and** `DOM/typographic` (4×). **WATCH:** `three.js` (3× — AT the ceiling, AVOID this cycle).
- **Over-represented technique:** `prosody / text-score cross-modal mapping` — **still 4×** (the four manuscripts), banned, will age out. `loops-of-one-take` family (canon + incommensurate phase-drift) holds **4× hard** (revenant, ringcanon, manyreturns, driftchoir) **plus the four manuscripts use incommensurate-loop phasing as substrate** — effectively **~8×**; keep hard-closed, no ninth. The healthy news is the two new singletons: **volumetric FBM raymarching** (chordnebula) and **GPU particle-life with a chord-written attraction matrix** (harmonicswarm) are each 1× and each genuinely new to the recent window — extend, don't re-ban. Other 1× to preserve: voice-balance polyphony (loomvoice), harmony-carved geometry (phosphene), spectral cross-synthesis (spectralbraid), stateful memory-accretion (longvigil), live-data→sample sonification (terrafret), harmonic-tuning-to-chord (cadencemap). **BANNED next cycle:** `prosody/text-score` (4×) + `loops-of-one-take family` (4× hard / ~8× w/ substrate — keep closed).
- **Over-represented vibe:** `near-dark / intimate typographic` — **still 5×** (driftchoir + the four manuscripts), banned, ages out with the lineage. `monochrome-base-plus-one signal` holds **3×** (graphite+chartreuse ringcanon, ashlight+bone manyreturns, silver+jewel phosphene) — WATCH, one from a ban. `two-signal duotone` holds **2×** (blue↔magenta terrafret, copper↔verdigris spectralbraid) — WATCH. `polychrome` 2× (loomvoice, longvigil). **NEW and worth flagging early: `bright luminous cosmic field` registers 2×** — chordnebula (violet→magenta→warm nebula) + harmonicswarm (luminous multi-hue field). Two in two nights. Not a problem yet, but this is precisely how the manuscript monoculture started: a good fresh direction, run twice, then a third and fourth. **BANNED next cycle:** `near-dark/intimate typographic` (5×). **WATCH:** `monochrome-base-plus-one` (3×), `bright luminous cosmic field` (2× — emerging, do not let it become the next rut), `two-signal duotone` (2×).
- **BANNED for next cycle:** `keyboard/typing input` (4×) + `Canvas2D output` (5×) + `DOM/typographic output` (4×) + `prosody/text-score technique` (4×) + `loops-of-one-take technique family` (4× hard, ~8× w/ substrate — keep closed) + `near-dark/intimate typographic palette` (5×). **WATCH:** `three.js output` (3× — AT the ceiling, AVOID), `catalog-playback+pointer/tilt input` (2×), `monochrome-base-plus-one palette` (3×), `bright luminous cosmic field palette` (2× — the new emerging cluster), `two-signal duotone palette` (2×). **Cleared on their own this window (aging-out did the work):** autonomous/passive input (3×→2×), inline-SVG/DOM art output (2×→1×). **Cashed this window:** `raw WebGPU compute` (0× for months → 1×). **Still 0× after months:** multi-user / WebRTC (the last uncashed big swing).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Nineteenth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 14** (loomvoice 3, revenant 3, ringcanon 3, phosphene 2, driftchoir 2, terrafret 2, spectralbraid 3, longvigil 3, tonguescript 3, scriptorium 3, albumvoyage 3, cadencemap 3, **chordnebula 3, harmonicswarm 3**). Both new ships land in the honest middle. **chordnebula** cleared #2 (catalog loader + FFT band engine + chord tracker + chord→pitch-class-core parser + volumetric raymarch renderer + pointer/tilt = 6) + #3 (Íñigo Quílez raymarched clouds / Refik Anadol look / Chord-Colourizer arXiv) + PRIMARY-DIRECTION (cosmic-ambient) — an honest 3, no #1 (raymarched clouds have priors). **harmonicswarm** cleared #2 (7 subsystems: catalog + FFT + chord tracker + triad/consonance parser + WGSL @compute sim + WebGPU render pipeline + pointer/tilt) + #3 (Reynolds *Boids* / Ventrella-Mohr *Particle Life* / Codrops "Run Rob Run" / SYTHM) + #5 (RESEARCH §2026-09-03) — an honest 3, and (credit) it **grep-checked and explicitly declined the #1 claim**: its README concedes it is NOT the lab's first GPU-compute particle physics (`16-particle-life-gpu` predates it), the honest first being the chord-driven attraction matrix. That is exactly the false-first discipline terrafret failed — learned and applied.
- **Hit 4–5 criteria — 1 (manyreturns 4).** Distribution **`0 / 14 / 1` — the top band DROPPED from 2 to 1.** This is an aging-out artifact, not a demerit on the new ships (spiralkeep, an honest 4, aged out and nothing 4+ replaced it) — but it is the honest read: **the ceiling is not just frozen, this window it fell.** The ship-for-ship ambition trajectory is now **… 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3 → 3** across ten straight ships. The lab cashed two brand-new *surfaces* in two nights and both still came in at 3, because the surface was never what was gating the 5. **What gates the 5 is verification** — a piece can't be scored a defensible 4/5 while it ships stamped "not run, tsc-only, needs Karel at the page." Every recent ship across BOTH modes (four manuscripts + two GPU fields) carries that stamp. The missing ingredient has not changed in a month: **Karel's eyes and ears.** The only difference is that now the un-verified backlog spans two completely different families.

## Standouts (positive)
- **16768-harmonicswarm** — the headline, and a genuine milestone: the lab's **first raw-WebGPU-compute piece**, cashing the exact 0× category the jury named for months as "the biggest single swing available," the night after the research confirmed the blocker (WebGPU on iOS) had lifted. And the concept earns the surface — Karel's real sounding chord rewrites the 6×6 inter-species attraction matrix, so consonant chords knit the swarm into filaments and tense/altered chords scatter it. That is harmony you can *see the physics of*, not a spectrum bar. Best-disciplined README in the window too (grep-checked, honest-first declined). Pending the look/framerate check — but this is the swing landing.
- **16752-chordnebula** — the cosmic-ambient pole done right, and the clearest single answer to the PRIMARY DIRECTION (altered-states through light + sound alone) in the whole window. A volumetric FBM raymarch you *drift inside*, his recording breathing the density and the sounding chord blooming pitch-class light-cores. After a fortnight of near-dark typographic manuscripts, a boundless bright drift-through-it place is the strongest possible palette break — and it's real WebGL, so it renders on his phone.
- **16368-manyreturns** — now the **lone 4** in the window and, by attrition, the single most obvious deepen target the lab keeps skipping. Its self-building incommensurate ensemble reached 4 by extending a real thing until it deepened. It is the likeliest honest path to a verified 5 — but NOT via the banned loop engine it's built on; the deepen has to come on a new axis.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **The manuscript lineage as a block** (tonguescript → scriptorium → albumvoyage → cadencemap, all 3/5) — not a quality knock; a structural lesson that now stands complete. Four consecutive DEEP cycles compounded four unverified bets on one un-listened-to base, and the lab correctly stopped and pivoted. The concept-level takeaway for the NEXT deepen: a deepen of an un-verified piece is a guess about a guess — get the verdict before compounding.
- **16496-terrafret** (2/5) — still the false-"first" cautionary tale, and worth keeping on the board precisely because harmonicswarm is now the counter-example: terrafret claimed a "first" its own window contradicted; harmonicswarm grep-checked, found the prior, and narrowed its claim honestly in the README. Cite the pair as the before/after of first-claim discipline.
- **16448-phosphene** (2/5) & **16464-driftchoir** (2/5) — two honest disciplined-2s that read well and reach for nothing (three.js-solid; audio-only). Fine as rested-surface breathers, not concepts to extend.

## Provocations for tomorrow's dream cycle
1. **Verification is now the ceiling — nothing new ships until Karel puts eyes/ears on the backlog.** Fourth straight day at zero verified 5s, and this window the top band *fell* to a single 4. The lab has conclusively proven it can cash a new surface on command; it has equally conclusively proven that cashing surfaces does not move the ceiling, because **the ceiling is gated by verification, not by surface.** There are now two phone-renderable eye-calls sitting ready (harmonicswarm, chordnebula — both mostly "does the harmony read in the structure?") and four ear-calls (the manuscripts). Put the two GPU pieces in front of Karel first — they're the fastest verdicts and the freshest work. A verified 4 on chordnebula or harmonicswarm would be the first ceiling movement in a month.
2. **Do NOT let the bright-GPU-cosmic-field become the next manuscript.** Two luminous cosmic fields in two nights (chordnebula three.js, harmonicswarm WebGPU), AND a THIRD is banked ready-to-ship (`16784-tidalchord`, the curl-noise flow-field sibling — same bright-cosmic family). Shipping tidalchord next makes it 3× luminous-cosmic and pushes three.js to the ban — the exact over-correction that built the manuscript monoculture, arriving from the opposite direction. If tidalchord ships, hard-pivot immediately after; better, pivot to a genuinely different family NOW and bank tidalchord one more cycle.
3. **One 0× category remains — multi-user / WebRTC — and the lab just proved it cashes 0× categories on command.** WebGPU-compute went from "never once, for months" to shipped in a single research-to-build cycle the moment the blocker lifted. Point that exact muscle at the last uncashed swing: a **shared listening room over WebRTC** where two people move through one of Karel's tracks together. It is not three.js, not Canvas2D, not DOM, not a cosmic field — it clears every ban AND the emerging watch cluster.
4. **If the fire goes DEEP, aim it at the lone 4 (manyreturns) on a NEW axis — not the loop engine.** manyreturns is now the only 4 in the window and has been untouched all fortnight. Its path to 5 is more real state you can *return to and read the history of* — but the incommensurate-loop substrate it's built on is at ~8× and hard-closed, so the deepen must add a new axis (memory you can inspect, a return you can hear the accumulation of), not another loop.
5. **Discipline check — confirm the GPU ships went through the FULL build gate, not tsc alone.** harmonicswarm's README says "verified by code + `tsc`, not by running," and chordnebula's says "compiles clean under `tsc --noEmit`." AGENT.md rule 3 is explicit that `tsc --noEmit` is NOT enough — Vercel runs ESLint + full production compile and fails on hook-rule / unused-var errors tsc ignores. These pieces are live on production, so presumably `npm run build` did pass at commit — but the READMEs should say `npm run build`, not `tsc`, so the gate that actually protects Karel's app is the one on record.

## Karel-facing line
The lab finally listened — two nights, two brand-new surfaces, and it cashed the WebGPU-compute swing I've been begging for; genuinely celebrate the swarm and the nebula — but the ceiling is *still* frozen at zero verified 5s (it actually slipped to one lone 4 this window), and it will stay frozen until you put your eyes on those two GPU pieces on your phone, because new surfaces were never the thing that was missing — your verdict was.
