# Concept Jury Verdict — 2026-09-01

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/keyboard/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→57fc80a; cycle 1211 `16656-tonguescript` at HEAD). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. Two new ships since the 2026-08-31 verdict: `16608-longvigil` (1210) + `16656-tonguescript` (1211); `15920-duetlink` and `16000-morphonate` aged out of the 15-window. RESEARCH.md is fresh (dated entries through 2026-09-01)._

## Summary
Two genuinely good wins and two genuine misses, and for once the misses are on the exact axes I named. The wins: **both 0×-for-15-cycles CATEGORIES got cashed in two days** — `longvigil` is the all-night projection/installation wall (Karel's own stated priority #5) and `tonguescript` is the cross-modal "your writing plays your piano" piece, and `tonguescript` finally got OFF Canvas2D onto a fresh DOM surface. The misses: **Canvas2D climbed 4×→5× and is still the single most-concentrated tag** — the ship I prescribed to escape it (`longvigil`) landed *on* it — and after five straight ships asking for a verified 4, the answer is a fifth honest-3; the window is still **`0/13/2` with zero verified 5s.** And a fresh embarrassment the audit caught: `terrafret` shipped claiming "the lab's FIRST real external-API sonification, greps to zero" while **`16064-seismscore` — still inside this very window — already sonifies the same live USGS earthquake feed through his piano.** The "first" was never a first.

## Diversity audit
_(last 15, numerically highest on disk: inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice, revenant, ringcanon, manyreturns, phosphene, driftchoir, terrafret, spectralbraid, longvigil, tonguescript — duetlink + morphonate aged out; longvigil + tonguescript entered)_

- **Over-represented input:** **none at the ban floor.** The spread is healthy and load-bearing: pen/pointer-paint (inkstroke, spectralbraid = 2×), tilt/orient (roomtone, phosphene = 2×), live-external-data-stream (**seismscore + terrafret = 2×** — see the technique note; two USGS-quake pieces), camera-head-6DOF (headnave = 1×), mic-secondary (loomvoice = 1×), multi-touch (revenant = 1×), gamepad (ringcanon = 1×), ensemble-authoring (manyreturns = 1×), press-hold (driftchoir = 1×), keyboard/typing (tonguescript = 1×). `passive/self-propelling` **fell to 2×** (spiralkeep + longvigil; and `longvigil`'s passivity is the *defining feature* of the sanctioned installation category, not the banned rut) — the long-standing input habit is finally starved. **BANNED next cycle: nothing on input.** **WATCH:** `live-external-data-stream` (2×, and it's a duplicated *category* — see technique).
- **Over-represented output:** **the headline, and it went the wrong way.** `Canvas2D` climbed **4×→5×** (ringcanon, manyreturns, terrafret, spectralbraid, **longvigil**) and is now the single most-concentrated tag in the whole audit. The escape I ordered didn't happen: `longvigil` was the ship I prescribed to get off Canvas2D, and it shipped on Canvas2D (built against the 08-30 verdict where the surface was only WATCH, and it self-flagged the debt — fair — but the net is 5×). `tonguescript`, built after the hard ban, *did* hear it and escaped to **DOM/typographic** (1×, fresh). Meanwhile the old tyrant relaxed: `GPU generative-field-you-watch` **dropped 5×→3×** (inkstroke, seismscore, spiralkeep — duetlink + morphonate carried two of its counts out) and is now merely WATCH. `three.js inhabited room` 2× (headnave, revenant), `three.js orbited solid` 1× (phosphene), `inline-SVG/DOM` 2× (roomtone, loomvoice), `audio-only/no-primary-visual` 1× (driftchoir). **BANNED next cycle:** `Canvas2D output` (5×, the new tyrant). **WATCH:** `GPU generative-field` (3×, relaxed off the ban), `three.js inhabited room` (2×), `inline-SVG/DOM` (2×).
- **Over-represented technique:** `the loops-of-one-take-against-itself family` (decoupled-time-base canon + incommensurate-loop phase-drift) — **4× and it did NOT decline as I predicted** (revenant, ringcanon, manyreturns, driftchoir), and `tonguescript`'s phasing polyphony is a **fifth soft member** (its lines loop at incommensurate lengths and drift — the exact Eno/Reich mechanism I retired). I said last verdict this family was "correctly starved, will fall to 3× on its own"; instead `tonguescript` re-fed it. Keep it hard-closed. **The audit's sharpest catch is here: real-world-data sonification of the live USGS quake feed ran TWICE** — `16064-seismscore` (one grain per quake, granular, GPU field) and `16496-terrafret` (enveloped phrase per quake, Canvas2D map) — yet `terrafret` shipped claiming "the lab's FIRST real external-API sonification … greps to zero." It did not grep to zero; seismscore was already in the window. That claim is void and `terrafret` is really a disciplined-2, not the 3 it logged. Elsewhere, healthy 1× and not to be re-banned into extinction: prosody cross-modal mapping (tonguescript's *real* novelty), stateful memory-accretion (longvigil), two-take spectral cross-synthesis (spectralbraid), convolution cross-synthesis (roomtone), Barkley excitable-medium (spiralkeep), harmony-carved geometry (phosphene), HRTF head-6DOF antiphony (headnave). `granular grain-triggering` 2× (inkstroke, seismscore). **BANNED next cycle:** `loops-of-one-take-against-itself family` (4–5×, keep closed). **WATCH:** `granular` (2×), `live-data→sample-trigger sonification` (2×, the duplicated category).
- **Over-represented vibe:** `cool-severe ink-on-bone/black` — **4×**, down from 6× (duetlink + morphonate aged out) but still on the ban floor (inkstroke indigo/bone, headnave dark-severe, seismscore Ikeda B-W-R, spiralkeep cyan-teal/bone). Still the true house style, still banned. `monochrome-base-plus-one-signal` holds at **3×** (graphite+chartreuse ringcanon, ashlight+bone manyreturns, silver+jewel phosphene) — WATCH, one from a ban. `near-dark typographic` **2×** (driftchoir, tonguescript). `two-signal duotone` holds **2×** (blue↔magenta terrafret, copper↔verdigris spectralbraid) — WATCH, the tell I flagged forming last verdict is still 2×, not yet calcified. `polychrome` **2×** (loomvoice, longvigil mineral/oxide — the strongest anti-mono answer in the window). Fresh 1× registers: roomtone high-key-magenta, revenant earthy. **BANNED next cycle:** `cool-severe-ink-on-bone/black` (4×). **WATCH:** `monochrome-base-plus-one` (3×), `near-dark typographic` (2×), `two-signal duotone` (2×).
- **BANNED for next cycle:** `Canvas2D output` (5×, the new tyrant) + `loops-of-one-take-against-itself technique family` (4–5×, keep closed — no sixth) + `cool-severe-ink-on-bone/black palette` (4×). **WATCH (not yet banned):** `live-external-data-stream input / live-data sonification` (2×, a duplicated category), `GPU generative-field output` (3×, relaxed off the ban), `three.js inhabited room` (2×), `inline-SVG/DOM` (2×), `granular` (2×), `monochrome-base-plus-one palette` (3×), `near-dark typographic` (2×), `two-signal duotone` (2×). **Not banned (rule 10):** catalog playback as the audio source. **Now CASHED (was owed 15+ cycles):** projection/installation (longvigil), cross-modal translation (tonguescript). **Still 0×:** WebGPU compute shader (never once), multi-user/WebRTC.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Seventeenth straight fortnight with an empty floor. Solved long ago; stop watching it.
- **Hit 2–3 criteria — 13** (inkstroke 2, headnave 3, seismscore 3, roomtone 3, loomvoice 3, revenant 3, ringcanon 3, phosphene 2, driftchoir 2, **terrafret 2** [downgraded — its #1 doesn't hold, see below], spectralbraid 3, longvigil 3, tonguescript 3). The honest middle. `longvigil` cleared **#2** (6 subsystems: catalog crossfade/preload/skip engine + circle-of-fifths harmonic reader + persistent memory accumulator + localStorage cross-session state + Canvas2D wall + operator layer) **+ #3** (Eno *77 Million Paintings*) **+ #4** (multi-cycle Tauri/venue foundation) — a real, honest 3; **no #1** (its own README concedes "installation" has ~38 priors). `tonguescript` cleared **#2** (catalog loader + prosody engine + lookahead scheduler + localStorage persistence + phasing polyphony + DOM playhead) **+ #3** (Cardew *Treatise* / Reich / Eno) and has a defensible #1 (first *committed* cross-modal text→his-piano; `lettertongue` was banked and `rm`'d). **`terrafret` is the correction: it logged a 3 on "#1 first external-API sonification, greps to zero" — but `seismscore`, still in this window, already sonifies the same live USGS quake feed through his catalog. The #1 is void; terrafret is a disciplined-2.**
- **Hit 4–5 criteria — 2 (spiralkeep 4, manyreturns 4).** Distribution **`0 / 13 / 2`** — **identical to yesterday, and this is the number the whole verdict turns on.** Two ships came in, two aged out, and the top band did not move: still two honest-4s, still zero verified 5s. The ship-for-ship trajectory since I began asking for a swing is now **2 → 2 → 3 → 3 → 3 → 3** — the middle climbed and stabilized at a competent 3, but the ceiling has not been reached once. **Both "verified-4 swing" ships (longvigil, tonguescript) were pitched to me as the swing and both landed honest-3.** The middle is solved; the ceiling is the entire remaining problem, and minting a sixth honest-3 in a fresh corner will not solve it. Extend one real thing until it deepens.

## Standouts (positive)
- **16608-longvigil** — the piece that cashed the category I've owed Karel for a month and a half: a hands-off, all-night venue installation that *remembers the night.* Each finished track deposits a persistent, harmonically-keyed geological STRATUM; the wall at hour 3 is genuinely the sum of the night, not a loop, and it survives a kiosk reboot via localStorage. That is real long-form state, built as a genuine installation and not a floor-minimum crossfade wall — exactly Karel's stated Tauri/venue priority #5. Its one shadow is the surface (Canvas2D, the tyrant), and it wants Karel's ears on the crossfades.
- **16656-tonguescript** — the cross-modal category cashed, on a genuinely fresh DOM surface, and the ship that finally heard the Canvas2D ban and escaped it. "Your writing plays your piano": commit a line and it becomes a persistent, looping voice of his real take; the poem accumulates into a phasing multi-voice score that survives a reload. The prosody-not-semantics mapping (word length→duration, vowels→brightness, punctuation→dynamics) is a clean, deterministic, no-LLM bridge — the *right* novelty. Deepen the prosody axis, not the phasing.
- **16368-manyreturns** & **16144-spiralkeep** — still the *only* two 4+ pieces in the window, and now the primary deepen targets. `manyreturns`' self-building incommensurate ensemble and `spiralkeep`'s persistent excitable medium each reached 4 by extending a real thing until it deepened — the exact move needed for a 5. (But not by re-running the now-banned loop engine.)

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **16496-terrafret** (2/5, downgraded) — the window's cautionary tale. A competent live-USGS-quake→his-piano map that shipped claiming a "first" the lab already held (`seismscore`). The plumbing is clean and the world-map render is nice, but the headline ambition was false, and running the same live-data category twice in 15 cycles is precisely the "too similar" the mandate exists to catch. Verify your "grep-0" claims against the *current window*, not just older history.
- **16016-inkstroke** (2/5) — the triple-default in one object: granular grain-triggering + GPU generative field + cool-indigo-on-bone. Its own README honestly concedes no #1 on any of its three axes. Competent pen-bow of his take, but it sits on three banned tag-classes at once — the house style, not a departure.
- **16448-phosphene** (2/5) — an honest disciplined-2 (harmony-carved three.js solid, mono-plus-one palette) that reads well but reaches for nothing; the README explicitly claims no first. Fine as a rested-surface breather; not a concept to extend.

## Provocations for tomorrow's dream cycle
1. **Canvas2D is the new tyrant at 5× — hard line, no Canvas2D next ship.** The escape I ordered failed because the prescribed ship (longvigil) stayed on the surface. tonguescript proved DOM is rested and legible; phosphene proved three.js-solid is rested (1×); driftchoir proved audio-only works (1×). And the old GPU-field ban has *relaxed to 3× / WATCH* — a genuinely fresh GPU-field or, better, the **never-once-built WebGPU compute shader** (still 0×) is available again. Pick any surface but Canvas2D.
2. **Land the verified 4 — this is now the entire ballgame, and I've asked six ships running.** Stop shipping honest-3s in fresh corners; the middle is solved (`0/13/2`, five 3s in a row). The only path left is to **deepen ONE live thing to a 5**: extend `manyreturns`' ensemble or `spiralkeep`'s persistence (NOT via the banned loop engine), OR push one of *this week's* genuinely-new pieces — `tonguescript` (per-word timbre variation, stereo-per-line, sustained-line advances the take's *section* so writing walks you through the form), or `spectralbraid` (full-take analysis past the ~17s cap, phase-vocoder tempo-align before crossing, saveable mask "scores") — until it verifies. Do NOT mint a 16th new corner.
3. **The retired self-loop family got re-fed — keep it actually closed.** tonguescript's phasing polyphony pushed the incommensurate-loop mechanism back to 4–5×. No sixth. If you deepen tonguescript (a good #2 move), deepen the **cross-modal prosody** axis — its real novelty — not the phasing.
4. **Verify your "firsts" against the CURRENT window, not just old history.** terrafret's void #1 happened because the builder grepped old prototypes but missed `seismscore` ten cycles back. Any ambition-#1 claim next cycle must be grep-checked against the last 15 ships explicitly, and the check quoted in STATE.md. A false "first" is worse than an honest "no first."
5. **Get Karel's ears on the backlog before compounding another audio bet.** FOUR consecutive audio-forward ships now want a real listen: `driftchoir` (audio-only), `spectralbraid` (painted cross-synthesis), `longvigil` (all-night crossfades), `tonguescript` (phasing prosody). A headless jury cannot tell whether any of them *reads*. Before deepening tonguescript or spectralbraid toward the 4–5 in #2, get one listen — you are four unverified audio bets deep.

## Postscript — cycle 1212 landed during this run
While this verdict was being written, `16672-scriptorium` (cycle 1212) pushed to `main`. The audit above is the honest snapshot of the 15 through `tonguescript` (1211); scriptorium enters the *next* window (inkstroke ages out). Worth recording now, because it **already actions provocation #2 the right way**: it's a genuine *deepen* of `tonguescript`, not a sixth new corner — one recording, where line N reads only region `[N/L,(N+1)/L]` so writing sweeps the *form* of his piece (real state: line 8 ≠ line 1 by construction), plus per-word golden-step grain-drift and per-line stereo placement. It stays OFF Canvas2D (DOM), so #1 holds too. Two caveats stand: it keeps the banned incommensurate-loop phasing as its substrate (acceptable *only* because it's a deepen, per #3 — do not spawn a sibling on it), and it is explicitly "not yet reviewed on device," so whether the form-voyage reads as a coherent journey vs arbitrary windows is exactly the ear-call #5 flags. **The "land a verified 4" ask is no longer a build question — it's Karel's listen.** If scriptorium's voyage reads, it's the 4 (maybe the 5 with the whole-album roll its README proposes); if it doesn't, the deepen continues, still not a new corner.

## Karel-facing line
Both categories I owed you finally got cashed in two days — the all-night installation wall (longvigil) and your-writing-plays-your-piano (tonguescript) — real wins — but Canvas2D grew to 5× because longvigil stayed on it, terrafret claimed a "first" that seismscore already held, and after five ships asking there are still zero verified 5s: tomorrow, get off Canvas2D and deepen ONE real thing to a 4 instead of minting a sixth honest-3.
