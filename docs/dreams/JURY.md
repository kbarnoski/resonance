# Concept Jury Verdict — 2026-09-13

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.**
>
> _meta: `git fetch && checkout main && pull --ff-only` fast-forwarded cleanly (330ce6b→1eef5a9). Read-only on `src/app/dream/**` — no prototype touched. **The window moved for the first time in ~16 verdicts:** `16672-scriptorium` rolls off the bottom, `17120-breathline` rolls on the top. So the review set is now albumvoyage · cadencemap · chordnebula · harmonicswarm · attune · gushbody · tunevoice · strata · nodalfigure · tensethread · vaultloom · inkpressure · voiceloom · firstlook · **breathline**. breathline is a real 879-line build (verified), not a stub. RESEARCH.md is fresh — today's cycle 1235 dive is dated 2026-09-13. This is the first verdict written against a lab that is BUILDING again._

## Summary
The freeze broke, and it broke well: `breathline` is the first genuinely non-chord, warm-palette, body-quiet piece the lab has shipped in the whole window, and it obeyed yesterday's provocation #2 almost to the letter. Credit where due — the builder read the jury and moved. But the same taste error the last three verdicts named survived the unfreeze intact: faced with three fresh builds, the lab shipped the **most glanceable** one (breathline) and banked the **boldest** one (`tidemark`, the memory piece this jury has named #1 twice). And the chord-as-subject reflex is untouched — still 6 of 15 — because the ban only ever applied to what gets built next, and what got built next was, correctly, not a chord piece.

## Diversity audit
- **Over-represented input:** `none / autonomous — no live body input` **9×** (chordnebula, harmonicswarm, strata, nodalfigure, tensethread, vaultloom, voiceloom, firstlook, breathline). This is the real headline and it got WORSE: breathline explicitly ships "no pointer/drag art input," adding a tenth quiet piece where the prior jury already warned "the lab quietly abandoned the body." Live inputs are all singletons now — camera 1× (gushbody), mic 1× (tunevoice), touch 1× (inkpressure), multi-user 1× (attune), keyboard 2× (albumvoyage/cadencemap).
- **Over-represented output:** **`Canvas2D` — 4× (NEWLY over the line):** gushbody, strata, voiceloom, breathline. The lab fled cool three.js and landed in warm Canvas2D — the exact renderer the prior jury called "most-rested (1×)." Meanwhile `three.js` (chordnebula/tensethread/vaultloom) and `SVG` (nodalfigure/inkpressure/firstlook) both sit at **3× (WATCH — do not make 4×)**. Truly rested: `audio-only` (≈1×), `WebGPU` (2× — harmonicswarm), `projection/spatial` (**0×**).
- **Over-represented technique:** **`chord-as-subject` — 6× (UNCHANGED headline):** chordnebula (hue), cadencemap (tuning), gushbody (hue), tunevoice (belongs-to-chord), nodalfigure (consonance-closes-the-curve), tensethread (tension-web). The harmony reflex is still the lab's deepest rut; breathline is the counter-move but it doesn't erase the six behind it. Also `analysis-of-his-own-take → visualize` is rising (strata, voiceloom, breathline = 3×) — healthier than the chord map, but watch it before it becomes the next monoculture.
- **Over-represented vibe:** `cool-luminous` **~4×** (chordnebula, tensethread, nodalfigure, strata). breathline's amber-rose-through-violet is the first real warm break in the window — reward it by not sliding back.
- **BANNED for next cycle:** `chord-as-subject` (any consonance/tension/harmony-hue mapping) · `none/autonomous` input (next build MUST take a live body: camera, hand, multi-user, or touch) · `Canvas2D` (rest it onto audio-only, WebGPU, or projection — NOT onto the 3× three.js/SVG either) · `cool-luminous`.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. A lure is not a concept; it's inventory dressed as a build, and it still fronts the wrong piece (it sells inkpressure, not the bold work). This is the local-minimum ship of the window.
- **Hit 2–3 criteria:** **9** — albumvoyage, cadencemap, chordnebula, gushbody, tunevoice, nodalfigure, tensethread, inkpressure, voiceloom. Honest mediums. `voiceloom` is the strongest of them (non-chord voice-separation, causal, named refs) and leans toward the top of the band.
- **Hit 4–5 criteria:** **0 confirmed — but 5 straddle 3→4 and are the extend list:** `harmonicswarm` (novel WebGPU-compute + *Particle Life* named ref + ≥3 subsystems), `strata` (only cross-session memory in the window), `vaultloom` (Xenakis named ref + true form-over-time), `attune` (only WebRTC/multi-user), and now `breathline` (grep-0 rubato-breath recovery + bioRxiv named ref + a research finding dated TODAY). None has been given the second cycle that would cross it cleanly to 4–5. Same ceiling as every prior verdict: not unreachable, walked away from.

## Standouts (positive)
- **17120-breathline** — the best new ship in a long time, and the reason this verdict isn't another hold-harder lament. It does the hard thing the mandate keeps asking: a grep-0 technique (recover a slow ~0.36 Hz "breath" from the signed rubato deviation of his real take — resample, strip drift, band-limit to infra-delta, autocorrelate for the live Hz readout) with a payoff you can *see in one glance* (the aperture inhales when he leans back, exhales when he presses forward) and a readout **measured from his timing, not faked**. It's bold AND legible — the combination the lab spent two weeks insisting it couldn't have.
- **16768-harmonicswarm** — still the most technically ambitious piece in the window: 12k particles, a 6×6 Particle-Life matrix on raw WebGPU compute, rewritten when his chord changes. "His harmony IS the physics law" reads in 3 seconds, muted.
- **16896-strata** — still the most conceptually novel: a recording that remembers being heard, accreting cross-session sediment. The only piece whose subject is listening-over-time.
- **16992-vaultloom** — the best "different at minute 5": a whole Xenakis nave lofting over the length of a take.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it lures for `inkpressure` (a mid SVG) while the bold pieces sit un-fronted. If you build a front door, build it for the work that makes Karel want more.
- **16944-nodalfigure** / **16960-tensethread** — the weakest of the chord-as-subject six: a harmonograph that closes on consonance, a web that reddens on tension. Elegant, and exactly the reflex now banned.
- **16720-cadencemap** — the third iteration of the typing→harmony-map format; with scriptorium off the board it's the tail of a played-out lineage.

## Provocations for tomorrow's dream cycle
1. **You broke the freeze but shipped the safe one — again. Ship `tidemark` next.** The memory piece is the pick this jury has named #1 in two straight verdicts and it is *still banked, not shipped.* breathline earned the unfreeze; don't let it become the excuse to keep the bold one on the shelf a third day. Ship tidemark, and if the payoff won't read in a cold glance, pair it with a lure the way breathline proved works.
2. **The body is gone — 9 of 15 take no live input, and breathline made it 10.** Next build MUST take a real body: hand/camera/multi-user/touch. The embodied+social shelf is still ≈1 (only `attune`) and un-extended. The `tidemark × attune` shared-listening-room from last verdict is *still unbuilt* and hits the two thinnest shelves — memory and co-presence — in one move.
3. **Do not add a 7th chord-as-subject.** The ban is real; the six are carryovers. Keep building about time / breath / silence / memory — `thehush` (silence-as-material, banked) is exactly right and, being audio-forward, also rests the newly-4× Canvas2D.
4. **Canvas2D just became the monoculture you fled to.** Four of the last builds went Canvas2D; three.js and SVG are both at 3×. If you rest a renderer, rest onto the *truly* thin ones — `audio-only` (≈1×), `WebGPU` (2×), or `projection/spatial` (**0× — still the biggest unclaimed swing**). breathline itself could have been audio-first.
5. **Stop citing the Breathing Mirror; build on it.** RESEARCH has logged arXiv 2609.01974 (Sep-2) six times as "corroborating" and its actual finding — the *blind spot between the measured breath and the felt breath* — is a real conceptual hook nothing has built. Same for the grep-0 **3D-cymatics / Faraday-geometry** candidate (cycle 1232) and today's Codrops WebGPU generative-garden (Sep-9, seeded tidemark's growth idea but not its surface). Fresh research is finally arriving; convert one dive into a build instead of a footnote.

## Karel-facing line
The freeze broke and breathline is the real thing — non-chord, warm, reads in one glance — but you shipped the safe piece again while `tidemark`, the bold one, still sits banked; ship the bold one next.
