# Concept Jury Verdict — 2026-09-14

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.**
>
> _meta: `git fetch && checkout main && pull --ff-only` fast-forwarded cleanly (330ce6b→2f12ad5, 6 commits). Read-only on `src/app/dream/**` — no prototype touched. **The window moved by TWO for the first time in the freeze era:** `16688-albumvoyage` and `16720-cadencemap` roll off the bottom; `17136-tidemark` and `17168-hearth` roll on the top. Review set (numeric last-15): chordnebula · harmonicswarm · attune · gushbody · tunevoice · strata · nodalfigure · tensethread · vaultloom · inkpressure · voiceloom · firstlook · breathline · **tidemark** · **hearth**. All three newest are real builds (breathline 879 lines verified; tidemark + hearth both passed the authoritative `npm run build`). RESEARCH.md is fresh — today's cycle 1237 dive is dated 2026-09-14. This is the first verdict in the lab's history that reviews a window where the lab **did exactly what the previous jury ordered.**_

## Summary
The lab executed. Yesterday's verdict named `tidemark` its #1 pick for the third straight time and the `tidemark × attune` shared-room as the standout swing — and the lab shipped **both**, back-to-back, as tidemark (c1236) and hearth (c1237). Three builds in three fires, each hitting a thinner shelf than the last. Credit in full: this is the strongest read-the-jury-and-move stretch the lab has ever had, and hearth is the first piece that genuinely *fuses two firsts* (co-presence + memory) into one cycle-2 — the exact ceiling-break every prior verdict begged for. Two shadows, though. First, the human lever is still dead: breathline, tidemark, and hearth all stand **unverified, zero opens, votes unchanged ~2 weeks** — the build engine is healthy again and the review loop is not. Second, in fixing the chord rut the lab grew a new one: **memory/accretion is now 5× and the last four builds are all warm** — the correction over-swung into its own monoculture.

## Diversity audit
- **Over-represented input:** `none / autonomous — no live body` **10×** (chordnebula, harmonicswarm, strata, nodalfigure, tensethread, vaultloom, voiceloom, firstlook, breathline, **tidemark**). tidemark obeyed jury #2's memory steer but *ignored its body steer* — dwell-under-the-playhead is autonomous, no live input. hearth is the counter (multi-user), but one live-body build in the newest two doesn't move a 10× headline. Live inputs remain thin singletons+: camera 1× (gushbody), mic 1× (tunevoice), touch 1× (inkpressure), **multi-user now 2×** (attune, hearth), keyboard 0× (both keyboard pieces rolled off).
- **Over-represented output:** **`Canvas2D` — 5× (worse):** gushbody, strata, voiceloom, breathline, **tidemark**. The prior jury banned Canvas2D at 4×; the lab shipped one more anyway (tidemark, by diversity-design "off the 3× three.js/SVG"). `three.js` (chordnebula/tensethread/vaultloom) and `SVG` (nodalfigure/inkpressure/firstlook) both hold at **3× (WATCH — do not make 4×)**. `WebGPU` climbed to **2×** (harmonicswarm, hearth) — no longer "rested," still healthy. Genuinely rested: `audio-only` (≈1×, tunevoice) and **`projection/spatial` (0× — untouched, the single biggest unclaimed swing all month)**.
- **Over-represented technique:** two now.
  - **`chord-as-subject` — 5× (DOWN from 6, the rut is finally receding):** chordnebula (hue), gushbody (hue), tunevoice (belongs-to-chord), nodalfigure (consonance), tensethread (tension). cadencemap rolled off; the three newest builds are all non-chord. The ban worked — keep it banned so the five age out.
  - **`memory / attention-accretion` — ~5× (the NEW rut, RISING fast):** strata (cross-session sediment), tidemark (collective-attention core-sample), hearth (remembered warmth), plus the analysis-of-his-own-take siblings voiceloom + breathline. The prior jury flagged this at 3× as "healthier than chord — watch it before it becomes the next monoculture." It became the next monoculture. Three of the last four builds are memory pieces.
- **Over-represented vibe:** `cool-luminous` **~4×** (chordnebula, tensethread, nodalfigure, strata; voiceloom leans cool) is now shadowed by an over-corrected **warm run — 5×** (gushbody, attune, breathline, tidemark, hearth): **the four most recent builds are all warm.** Warm was the right break; a fifth warm piece is just the cool rut in amber.
- **BANNED for next cycle:** `chord-as-subject` (any consonance/tension/harmony-hue mapping — the five are carryovers, keep aging them out) · `none/autonomous` input (next build MUST take a live body — and a *non-multi-user* one: camera/hand/touch, since multi-user is now 2×) · `Canvas2D` (rest it — but onto `audio-only`/`WebGPU`/`projection`, NOT the 3× three.js/SVG) · `memory/accretion-as-subject` (NEW — three of the last four; do not ship a fourth) · a **fifth consecutive warm palette** (bring a third temperature or a genuinely neutral one).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. A lure is not a concept; it's inventory dressed as a build. It still fronts `inkpressure` (a mid SVG) while three bolder, thinner-shelf pieces (breathline, tidemark, hearth) now sit un-fronted behind it. Local-minimum ship of the window, unchanged.
- **Hit 2–3 criteria:** **7** — chordnebula, gushbody, tunevoice, nodalfigure, tensethread, inkpressure, voiceloom. Honest mediums; `voiceloom` (non-chord voice-leading, causal, named refs) still tops the band.
- **Hit 4–5 criteria:** **0 confirmed — but 7 straddle 3→4, and the ceiling finally cracked once:** harmonicswarm (WebGPU-compute + *Particle Life* ref), strata (first cross-session memory), vaultloom (Xenakis + form-over-time), attune (first WebRTC/multi-user), breathline (grep-0 rubato-breath + bioRxiv + same-day research), tidemark (first per-position collective-attention sediment), and **hearth — the first piece in the window to satisfy #4 for real: a genuine cycle-2 that FUSES two prior firsts (attune's co-presence + strata/tidemark's memory) into one room.** Every prior verdict said "give a first its second cycle." hearth *is* that second cycle. If Karel verifies it, it is the lab's credible first 4/5.

## Standouts (positive)
- **17168-hearth** — the most important ship of the window, because it's the first that does the *structural* thing the mandate keeps asking, not just the topical one: it takes two prior firsts (multi-user co-presence, cross-session memory) and welds them into a single room that remembers its listeners. It cashes three standing provocations at once — live body (multi-user), off-Canvas2D (raw WebGPU), and the twice-named memory×co-presence swing — and reads warm on a cold muted glance via ~12 pre-seeded "prior sessions." This is what climbing out of the local minimum actually looks like.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab has: a grep-0 technique (recover the ~0.36 Hz "breath" from the signed rubato deviation of his real take) with a payoff you see in one glance (the aperture inhales on lean-back, exhales on press-forward) and a Hz readout measured, not faked.
- **17136-tidemark** — the memory shelf finally shipped: a recording that grows a geological core-sample of where collective attention pooled. The x=time axis makes "most-returned passage at M:SS" spatially honest.
- **16768-harmonicswarm** — still the most technically ambitious: 12k particles, a 6×6 Particle-Life matrix on raw WebGPU compute, rewritten when his chord changes. "His harmony IS the physics law," legible in 3 seconds.
- **16992-vaultloom** — still the best "different at minute 5": a whole Xenakis nave lofting over the length of a take.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it's now *aimed at the wrong target twice over*: it fronts inkpressure (a mid SVG) while hearth, breathline, and tidemark — the three pieces that would actually make Karel want more — sit un-fronted. If you build a front door, build it for hearth.
- **16944-nodalfigure** / **16960-tensethread** — the weakest of the (now five) chord-as-subject pieces: a harmonograph that closes on consonance, a web that reddens on tension. Elegant, and exactly the reflex still banned.
- **16848-tunevoice** — the thinnest live-input piece: a mic that opens a filter when you sing a chord tone. Nearly audio-only (a virtue on the rested shelf) but conceptually slight and still chord-anchored.

## Provocations for tomorrow's dream cycle
1. **You executed — now the build engine is healthy and the review loop is the only thing broken.** Three bold, thin-shelf pieces stand unverified for ~2 weeks (0 opens, votes flat). Stacking a *fourth* unverified bold build is no longer courage — it's a substitute for the one verification that would tell you which of these three is real. If nothing verifies again, the honest move is a piece engineered to be **impossible to ignore on a cold muted phone glance** (the firstlook *instinct* was right; it just fronted the wrong work). Front hearth, or build the lure hearth deserves.
2. **Memory is the new chord.** You spent the window climbing out of the harmony rut and landed straight in a memory/accretion rut — three of the last four builds. Next build is **NOT memory and NOT chord.** The two genuinely thin shelves now are `projection/spatial` (**0× all month**) and `audio-only` (≈1×). Pick one.
3. **Take a live body that isn't multi-user.** hearth answered co-presence; multi-user is now 2×. But `camera`, `hand`, and `touch` are each still lonely 1× singletons. A depth-camera or MediaPipe-hand piece would hit `projection/spatial` (0×) *and* embodied-non-social in a single move — the biggest unclaimed swing on the menu.
4. **The warmth pendulum over-swung.** The last four builds are all warm (gushbody → attune → breathline → tidemark → hearth). Warm was the correct correction to 5× cool-luminous; a fifth warm piece is just the same monoculture at a different color temperature. Bring a third temperature or a deliberately neutral palette.
5. **Turn hearth's fusion into the ceiling-break, don't footnote it.** hearth is the first genuine cycle-2 the lab has produced. If Karel verifies *any* of the three, give hearth its cycle-3: **>2 presences** (a real ensemble weave — cash the banked `commons`, IDEAS §1237) or a **projection/venue-wall framing** that hits the 0× spatial shelf. That is how a first finally reaches 5/5 — and it kills two provocations (projection + ensemble) at once.

## Karel-facing line
The lab finally did exactly what you asked — shipped the two bold pieces it kept banking (`tidemark`, then the shared-room `hearth`) — but all three new builds sit unopened; one tap from you turns two weeks of bold work into a direction.
