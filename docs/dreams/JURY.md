# Concept Jury Verdict — 2026-08-29

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/tilt/pen/gamepad/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (7a6c547→1265585; cycle 1205 `16368-manyreturns` at HEAD, plus the standing batch of Karel-directed out-of-fence work — middleware/tripwire, anon-token migrations, OG-image routes, now-playing-bar — none dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` — no prototype touched. Two new ships since the 2026-08-28 verdict: `16288-ringcanon` (1204) + `16368-manyreturns` (1205); `15600-keepsake` and `15696-leanin` aged out of the 15-window._

## Summary
For three weeks I said the same thing — finish the shot, extend `canon` into the self-building ensemble of ghosts, don't bank another honest 3 and start a fresh corner. **Today it landed.** `16368-manyreturns` is that ensemble, built by name: one of his takes grown into a phase choir of incommensurate-length loops that drift forever, self-builds to five voices hands-off, ages Basinski-style, and lays down an accumulating wear-trace so the picture at minute five is genuinely not the picture at minute one. It's the first ship in a month to **honestly claim ambition #1** (the ensemble scheduler greps to zero), and the top band moved for the first time in four windows: `0/10/5`, up from `0/11/4`. Credit in full — the deepen I named got finished. **But the win has a shadow, and it's the whole verdict this window:** `canon` → `revenant` → `ringcanon` → `manyreturns` means the decoupled-time-base canon *mechanism* is now **4× in the last 15** — it has crossed my own ban floor. The rut didn't disappear; it **relocated**, from shader/camera/granular into canon/Canvas2D/grey-monochrome. The task last month was "aim at the ceiling." The task tomorrow is the exact inverse: **canon is DONE — walk away from it.** Four variants is a lineage; a fifth is a rut.

## Diversity audit
_(last 15, numerically highest on disk: conduct, mididuet, canon, answerpiano, duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep, roomtone, loomvoice, revenant, ringcanon, manyreturns — keepsake + leanin aged out; ringcanon + manyreturns entered)_

- **Over-represented input:** `passive/self-propelling — no load-bearing human verb` — **4×** (answerpiano, morphonate, seismscore, spiralkeep), at the ban floor. `camera/webcam-as-primary` dropped to **3×** (conduct, canon, headnave — leanin's exit pulled it under the floor); WATCH, not banned. Credit the fresh active verbs newly in-window: `ringcanon` **gamepad** (~1×, the most-rested un-cashed verb, finally cashed) and `manyreturns` **ensemble-authoring** (add/thin/click-mute/sliders — an active compositional verb, not passive). **BANNED next cycle:** `passive/self-propelling primary input` (4×, unless carrying a genuinely-grepped never-run subsystem — the `spiralkeep` persistence exemption). **WATCH:** `camera-primary` (3×, one ship from re-banning).
- **Over-represented output:** `GPU generative field (WebGL2/WebGPU shader you watch)` — **7×** (conduct, canon, morphonate, inkstroke, seismscore, spiralkeep, duetlink) — unchanged, still the single most-used surface, still banned. `inline-SVG/DOM as the primary surface` — **4×** (mididuet, answerpiano, roomtone, loomvoice), still at floor. **New tell to watch: `Canvas2D` is now 2×** (ringcanon, manyreturns) — I *encouraged* it as rested, both ceiling-aimed ships took it, and it's the exact way three.js, SVG and the shader each started before hardening into a rut. `three.js inhabited room` holds at **2×** (headnave, revenant) — did NOT gain (manyreturns went to Canvas2D per my #2), good. **BANNED next cycle:** `GPU generative-field-as-the-visual` (7×) **+ `inline-SVG/DOM as the primary surface`** (4×). **WATCH:** `Canvas2D` (2×, newly the ceiling-ships' surface), `three.js inhabited room` (2×). Genuinely 0× and owed, now for many windows: **audio-only / embedded / haptic** (a piece with *no primary visual* — and it's built and banked, see #3), **projection / installation**.
- **Over-represented technique:** **the headline.** `decoupled-time-base canon (one recording read at two-or-more time-bases as counterpoint against itself)` — **4× and now over the ban floor** (canon, revenant, ringcanon, manyreturns). This is the mechanism I asked the lab to finish, and it did — so it is now over-represented *by my own instruction*, and it must come off the board or it becomes the thing I've spent a month warning about. `granular grain-triggering of his take` — **5×** (conduct, canon, mididuet, inkstroke, seismscore — keepsake's exit dropped it from 6), still ≥4, still banned. `dual-voice / band-split crossover` — **3×** (canon, duetlink, revenant), unchanged, one from a ban. Freshly-cashed and at 1× — do NOT re-ban into extinction: convolution cross-synthesis (roomtone), whole-take polyphony (loomvoice), gesture record/replay (revenant), physical-model modal resonance (ringcanon), incommensurate-loop phase-drift ensemble (manyreturns). **BANNED next cycle:** `granular grain-triggering` (5×) **+ `decoupled-time-base canon mechanism`** (4× — canon lineage is closed; ship no fifth variant). **WATCH:** `band-split crossover` (3×).
- **Over-represented vibe:** `cool-severe on bone-or-black` — **6×**, still the true house style (duetlink cold-achromatic, morphonate cyan/ink, inkstroke indigo/bone, headnave dark-severe, seismscore Ikeda B-W-R, spiralkeep cyan-teal/bone). `warm chromatic (ember→gold / warm pitch-class-hue)` — **4×** (conduct, mididuet, canon, answerpiano), aging tail, still at floor. **New tell:** the `grey-monochrome-plus-one-signal` register I *asked for* as a fourth register is now **2×** (ringcanon graphite+chartreuse, manyreturns ashlight/pewter+bone) — and it rode in on the two Canvas2D ships, so surface and palette are calcifying *together*. The other fresh registers hold at 1× each (roomtone high-key+magenta, loomvoice polychrome, revenant earthy-organic). **BANNED next cycle:** `cool-severe-ink-on-bone/black` (6×) **+ `warm-chromatic`** (4×). **WATCH:** `grey-monochrome-plus-one` (2×). **Encouraged:** the 1× fresh registers — keep each under 2×; find a *sixth* register wrong next to all of them.
- **BANNED for next cycle:** `passive/self-propelling primary input` (4×) + `GPU generative-field output` (7×) + `inline-SVG/DOM primary surface` (4×) + `granular grain-triggering` (5×) + **`decoupled-time-base canon mechanism` (4×, NEW — the lineage is finished)** + `cool-severe-ink-on-bone/black palette` (6×) + `warm-chromatic palette` (4×). **WATCH (not yet banned):** `camera-primary` (3×), `Canvas2D` (2×), `three.js inhabited room` (2×), `band-split crossover` (3×), `grey-monochrome-plus-one palette` (2×). **Not banned (rule 10):** catalog playback as the audio source. **Explicitly rested / owed:** audio-only/embedded/haptic (still 0× — and BUILT, banked `16352-driftchoir`), projection/installation, real-world-data sonification, cross-modal translation; gamepad / multi-touch active verbs; spectral freeze / cross-synthesis / additive resynthesis (still-0× non-grain audio); the 1× fresh palettes.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Fourteenth straight fortnight with an empty floor. Solved; stop watching it.
- **Hit 2–3 criteria — 10** (duetlink 2, answerpiano 3, morphonate 3, inkstroke 3, headnave 3, seismscore 3, roomtone 3, loomvoice 3, revenant 3, ringcanon 3). Still the honest middle. `ringcanon` cleared **#2** (seven subsystems: catalog loader + `loadTrackAnalysis` key-center + 12-mode bandpass modal bank + bounded regeneration loop + two-time-base canon state + gamepad + Canvas2D) **+ #3** (named refs: arXiv:2210.15306, JOS III PASP, Reich) **+ #4** (canon cycle-2). It **honestly declined #1** — resonator synthesis is ~170× in the lab — the disciplined-3 model, correctly claimed.
- **Hit 4–5 criteria — 5 (conduct 4, mididuet 4, canon 5, spiralkeep 4, manyreturns 4).** Distribution **`0 / 10 / 5`** — **the top band finally grew** (from four windows stuck at four pieces) because `manyreturns` entered it. `manyreturns` cleared **#1** (the incommensurate-loop phase-drift ensemble scheduler greps to zero in `src/app/dream/**`; `594-disintegration-loop` is a *single* self-overwriting synth loop, not a multi-loop catalog ensemble — the negation is honest) **+ #2** (five subsystems: catalog loader + coprime-loop scheduler + per-ghost Basinski aging chain + ensemble-authoring input + phase-portrait wear-trace) **+ #3** (Eno *Music for Airports* / Reich / Basinski) **+ #4** (canon cycle-3). It is the **first honest #1 claim in a month** and the reason the band moved. Whether it reads as a genuine 5/5 *by ear* — a rich, evolving choir vs. a soft mush — is Karel's call at the page; on the floor it is a clean, ambitious 4, and structurally it is the strongest bid at the ceiling the lab has made.

## Standouts (positive)
- **16368-manyreturns** — the piece of the window, and the payoff of a month of nagging. This is the self-building ghost choir I named as the thing that turns canon's 3 into a 5: N copies of one real take looping at incommensurate lengths (ratios ≈1.000, 1.037, 1.081, 1.129, 1.181, 1.237 — coprime-ish, never re-sync), self-assembling to five voices with zero input, each voice aging its own lowpass shut over four minutes, and a wear-ring memory trace that accumulates where phase-coincidences keep landing. It is the lab's first real **state-and-memory / different-at-minute-5** piece, its first honest ambition-#1 in weeks, and it's on Canvas2D (off the three.js room I warned about) in a fresh ashlight register. **The shot landed. Now the discipline is to stop shooting it.**
- **15824-canon** — still the lone *verified* 5/5, and now genuinely, fully extended: revenant (recorded-replay) → ringcanon (modal-resonance answer) → manyreturns (the ensemble). The lineage is complete. It remains the most valuable object in the lab — the proof a decoupled-time-base instrument reads as a real 5 — and its deepen is, at last, *finished* rather than *begun*.
- **16288-ringcanon** — the cleanest disciplined-3 of the window: his real recording as the *excitation* into a hand-tuned modal bank (resonators as a filter on his audio, never a synth pluck), the exact still-0× non-grain audio move I named, on gamepad (the most-rested verb), scrupulously honest about declining "first." The model for how to cash a non-grain axis without over-claiming.
- **16144-spiralkeep** — holds as the other top-band exemplar of the honest play (Barkley excitable medium + real persistence, no "first" claimed, a real 4). Still the template alongside canon: extend a real 4/5 honestly until it *becomes* a 5.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15920-duetlink** (2/5) — the window's local-minimum exemplar: strip the "first multi-user" banner (the lab has ~24 multi-user priors) and it's a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors. Competent plumbing, not a concept — and its crossover feeds the 3× band-split creep worth watching.
- **15872-answerpiano** (3/5) — passive/self-propelling with no load-bearing human verb; a note-answer piece that runs itself. Clears the floor on paper but adds no axis `canon` didn't, and lands on the most over-represented input class. Residue of the passive-input habit.
- **16064-seismscore** (3/5) — the triple-default in one object: passive input + GPU generative field + granular grain-triggering, Ikeda B-W-R. Technically clean, but it's the exact stack the bans exist to climb off; a competent re-run, not a new idea.

## Provocations for tomorrow's dream cycle
1. **Canon is finished — ship NO fifth variant, and BAN the decoupled-time-base mechanism for a week.** The lineage `canon → revenant → ringcanon → manyreturns` is now 4× and over my ban floor. `manyreturns` IS the ensemble I asked for; it landed; the deepen is done. A fifth canon — a canon that tracks his harmony live, a fused modal×string canon, variable-bar canon — is the *relocated* rut, and I will score it a 2 on arrival however clever the DSP. The whole verdict this window is: the lab proved it can drill this hole four ways; **now walk away from it entirely.**
2. **Go WIDE, not DEEP — the last three ships were all DEEP canon.** Alternation is broken and the mechanism is calcifying. Force a WIDE cycle: three genuinely un-adjacent concepts, none of them a canon/answering-voice/decoupled-time-base piece, each clearing the floor on a *different* input × output × technique × palette. Break the monoculture with breadth, the way roomtone/loomvoice's window did.
3. **Ship the still-0× audio-only / haptic lane — the banked `16352-driftchoir` (IDEAS §1205).** It's built, it was `rm`'d unshipped in favor of a screen piece for the *third* time, and it's the "no primary visual" piece I've named for weeks and the lab keeps declining. `manyreturns` just *proved the ensemble engine works* — driftchoir is that same engine with the screen removed and `navigator.vibrate` on phase-coincidence. Add the desktop *audio* coincidence-cue noted in STATE so it reads without a phone, and **test the screen bias directly.** This is the one deepen of the ensemble that isn't a canon re-run — it's a different *output modality*, which is exactly the axis that's owed.
4. **Cash a 0× *category*, not just a 0× technique.** Canon ate five cycles; entire menu categories haven't appeared in 15+: **projection/installation** (Tauri/kiosk — Karel's stated priority), **real-world-data sonification** (an external API as composition input — music *about* something other than music, with his catalog as the instrument), **cross-modal translation** (an unexpected sensor → an unexpected axis of his takes). Pick one and skeleton it — a rough skeleton of a genuinely new category beats a polished fifth canon.
5. **Palette + surface are calcifying together — hold both lines.** `grey-monochrome-plus-one` is at 2× (ringcanon, manyreturns) and rode in on the two Canvas2D ships, so the newly-encouraged surface and the newly-encouraged palette are hardening as a *pair* — the identical failure mode I'm warning about with canon. Do NOT reach for grey-minimal-on-Canvas2D a third time. Warm and cool-ink are banned; the three 1× registers (high-key-magenta, polychrome, earthy) are the win — keep each under 2× — or find a *sixth* register wrong next to all of them (a metallic/leaf register, a single saturated hue nobody's used, a duotone).

## Karel-facing line
The shot finally landed — `manyreturns` is the self-building ghost choir I've named all month, the first ship in weeks to claim a real new technique, and the top band moved for the first time in four windows — but that means **canon is DONE: four variants deep, the lineage is now its own rut**, so tomorrow go WIDE onto three fresh concepts, finally ship the screen-less `driftchoir`, and don't build a fifth canon.
