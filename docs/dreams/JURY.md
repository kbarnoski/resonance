# Concept Jury Verdict — 2026-08-16

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI allowed only as a SECONDARY layer), **rule 10 wins.** Mic-primary is PAUSED until Karel says otherwise.

## Summary
The last verdict's #1 demand — *"let Karel PLAY, not just watch"* — got cashed, hard: **12 of the last 15 are things you actually perform** (a beat-locked DJ deck, a body-conducted orchestra, a walk-through hall of your own songs, faders that un-mix your piano), and the only three watch-only pieces are the three *oldest* in the window. The analysis-visualizer rut is dead. But the lab has quietly built two new monocultures in its place: **it now paints almost everything the same cool-violet-on-near-black (~12×)** and **drives almost everything with a mouse/finger (~9×)** — the palette pendulum swung from last verdict's banned amber all the way to a cool monoculture, and MIDI, Karel's own instrument, has appeared **0×**. Technique diversity is genuinely excellent; input diversity and palette diversity are the new problem.

## Diversity audit (last 15: vocabularygraph, handreader, resonancefield, striketemple, conductorwell, canoncircle, dreambetween, callback, spectralsmear, auroraconductor, hallofsongs, unmixer, flipdeck, rhymeloom, songwheel)
- **Over-represented input:** `pointer/touch-on-glass` — **~9×** (conductorwell, dreambetween, spectralsmear, unmixer, flipdeck, rhymeloom, songwheel, striketemple, callback). Only **auroraconductor** (webcam optical flow) and **hallofsongs** (WASD/mouse-look spatial nav) break it; three are watch-only. RESEARCH §1149 already flagged "every verb is a finger on a screen." **BANNED next cycle: pointer-drag/click as the primary interaction verb.** `catalog-playback` audio source is **NOT banned** (rule 10 wins).
- **Over-represented output:** `WebGL2 full-field/shader` — **6×** (vocabularygraph, spectralsmear, auroraconductor, unmixer, flipdeck, rhymeloom). **BANNED next cycle.** Runner-up: `inline-SVG` at **5×** (striketemple, canoncircle, dreambetween, callback, songwheel) — not banned but at the ceiling; watch it.
- **Over-represented technique:** **no single algorithm repeats ≥4×** — technique diversity is genuinely excellent (PageRank, hand-kinematics, coupled-resonator WebGPU lattice, modal synthesis, granular time-stretch, BroadcastChannel consensus canon, ESN navigator, concatenative grains, phase-vocoder freeze, optical flow, HRTF spatial, HPSS median-filter, beat-tracking turntablism, chroma-SSM, Foote segmentation are all distinct and mostly first-in-lab). BUT a **meta-technique** is now visible and consecutive: **"take ONE recording → apply a classic DSP transform (freeze / un-mix / flip / jump / stretch) → hand back a scrub/loop/fader surface"** — **7×** (conductorwell, spectralsmear, callback, unmixer, flipdeck, rhymeloom, songwheel). **This "single-recording DSP toy" template is banned next cycle** — it is the analysis-visualizer rut's direct successor.
- **Over-represented vibe:** `cool-violet / ice on near-black` — **~12×** (vocabularygraph, canoncircle, conductorwell, dreambetween, callback, spectralsmear, auroraconductor, hallofsongs, unmixer, flipdeck, rhymeloom, songwheel). This is a *massive* over-representation and the clearest rut in the window. Last verdict banned warm/amber; the lab swung the whole pendulum to cool. **BANNED next cycle: cool-violet/ice-on-near-black.** (Warm/amber — handreader, resonancefield, striketemple — is now only 3× and no longer the problem.)
- **BANNED for next cycle:** `pointer-drag/click primary interaction` + `WebGL2 full-field output` + `the single-recording-DSP-toy template` + `cool-violet/ice on near-black`. **Not banned (rule 10):** catalog-playback audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** No local-minimum builds this window. This is the first fortnight with an empty bottom tier.
- **Hit 2–3 criteria — 5:** `13360-handreader` (2 — the weakest; no named ref, no research tie), `13328-vocabularygraph`, `13440-resonancefield`, `13488-striketemple`, `13696-callback`.
- **Hit 4–5 criteria — 10** (the ones to extend): `13536-conductorwell` (5/5), `13792-auroraconductor` (5/5), `13840-hallofsongs` (5/5), `13968-flipdeck` (5/5), `14048-rhymeloom` (5/5), `14096-songwheel` (5/5), plus `13568-canoncircle`, `13664-dreambetween`, `13760-spectralsmear`, `13904-unmixer` (4/5).

The distribution is **0 / 5 / 10** — a higher ceiling than last fortnight's 1/8/6, with an empty floor. Six 5/5 pieces, and the ambition floor is now doing its job so consistently that "hit the floor" is no longer the axis worth optimizing. The next constraint is **breadth**, not floor height — see the diversity bans.

## Standouts (positive)
- **13968-flipdeck** — the single cleanest cash of the standing #1 provocation across five verdicts: a genuinely *playable* beat-locked DJ deck on his rubato piano. First real beat/downbeat tracking in the lab (spectral-flux onset → autocorrelation tempo → phase-aligned 4/4 grid), and bar-quantized flips (scrub/loop/reverse/half-time/beat-repeat) that *stay in time*. An instrument, not a diagram — exactly what was demanded.
- **13792-auroraconductor** — **first live camera / optical flow in the lab**, and the one piece that broke the finger-on-glass input monoculture the research itself flagged. Hand-rolled Lucas–Kanade conducting his own recording by quantity-of-motion. A real technical first *and* a diversity win in one build.
- **13536-conductorwell** — 5/5 and the lab's first time-stretch instrument: pitch-preserving granular overlap-add, conducted by a circling baton, explicitly marked multi-cycle (it folded its two DEEP runners-up into cycles 2–3). The "conduct your own recording" idea done rigorously.
- **13840-hallofsongs** — the only piece to load *many* of his recordings at once and spatialize each as an HRTF object you walk among. Distinct input (first-person spatial nav) and distinct output (three.js FPS), in the Cardiff & Miller sound-walk lineage. Where-you-stand-is-the-mix is a genuinely different verb.
- **14048-rhymeloom → 14096-songwheel** — the model multi-cycle arc of the window: a flat self-similarity matrix (cycle 1) honestly deepened into a beat-locked, section-aware radial *form* wheel (cycle 2, DEEP), fixing rhymeloom's own flagged flaw (leaps landing mid-phrase). Foote checkerboard-novelty segmentation is real MIR, and the "different geometry, same idea" restraint is exactly the DEEP mode's job.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **13360-handreader** — the 2/5 and the purest survivor of the *old* rut: a note-roll → three.js hand-choreography you press Play and watch. Beautiful, but no named reference, no research tie, warm-amber (the then-banned palette), and zero interaction. It shipped as the analysis-visualizer template the previous verdict had just banned "next cycle." The clearest concept-level local minimum in the window.
- **13328-vocabularygraph** — also watch-only. The PageRank-on-his-chord-graph math is excellent and first-in-lab, but the *interaction* is "observe a force-directed graph accrete" — another thing to look at, not play. Strong intellect, banned-template shape.
- **13664-dreambetween** — a fine rule-10 fix of dreammedley, but concept-wise it is the **fourth** cycle of the reservoir line (echofold → recallorbit → dreammedley → dreambetween). The memory-constellation-you-steer is now its own template; the line should graduate to a new form or rest.
- **Concept convergence (the DEEP/WIDE machine is shipping *pairs*):** `conductorwell` + `auroraconductor` are the same "conduct his recording by motion → granular time-stretch" idea (pointer, then webcam); `rhymeloom` + `songwheel` are the same self-similarity-jump idea (matrix, then wheel). Both pairs are excellent *as deepening*, but two of the 15 are second-takes — the apparent output is inflated. Fine for a multi-cycle commitment; a problem if it becomes the default cadence.

## Provocations for tomorrow's dream cycle
1. **Break the cool-violet monoculture.** 12 of the last 15 are violet/ice on near-black. The palette swung from last verdict's banned amber all the way to a cool rut — neither is diversity. Next cycle needs a genuinely different visual register: high-key daylight / paper-white, a full chromatic spectrum, or a deliberately un-pretty text-only / no-color piece. Stop repainting in one scheme.
2. **Touch a MIDI keyboard — it is 0× and it is literally Karel's instrument.** Input has been a pointer/touch surface ~9×; the only escapes are one webcam and one WASD piece. Karel is a pianist. **Ship a Web MIDI piece:** his real recording underneath + his live MIDI playing on top voiced in his own timbre (concatenative grains of his sound, per callback), or his live playing conducting/branching one of his takes. The most obvious 0× lane and the most Karel-native input imaginable.
3. **Retire the single-recording-DSP-toy template.** Seven of the last fifteen are "one track → one classic DSP transform → a scrub/loop surface." Each is distinct and excellent; together they're the successor rut to the analysis-visualizer. Next piece should either treat the **whole catalog as one corpus** (as hallofsongs/vocabularygraph did) or leave manipulation-of-an-existing-take behind for a piece where Karel *composes/creates* rather than re-arranges.
4. **The AI-pipeline chain (music → image → video, 2+ models in series) is STILL the loudest never-shipped lane — now flagged three verdicts running.** It never ships because it needs a `FAL_KEY` budget + guarded route + Karel's go-ahead. Karel: green-light it with a per-prototype budget or tell the agent to drop it permanently. Perpetual "queued next" is a tell.
5. **Real-device / sound-on review is now the highest-leverage thing by a wide margin — because this window shipped 12 *playable* instruments, and playability can only be judged with hands on the thing.** flipdeck's beat grid on his rubato, auroraconductor's webcam latency, hallofsongs' HRTF on headphones, songwheel's Foote section arcs, striketemple's modal duet — all headless-unverified. Ten minutes of Karel with headphones, a webcam, and (ideally) a MIDI keyboard would validate or kill a dozen pieces at once.

## Karel-facing line
You asked to *play, not watch* — and the lab delivered: 12 of the last 15 are things you actually perform (a DJ deck, a body-conducted orchestra, a walk-through hall of your songs), zero duds this fortnight. But it now paints all of them the same cool-violet and drives all of them with a mouse, and it still hasn't touched your MIDI keyboard or shipped the AI video chain — tomorrow: a new color, your real instrument as input, or both.
