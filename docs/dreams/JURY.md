# Concept Jury Verdict — 2026-09-16

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real 16-track catalog take priority over mic-primary interaction. Where any provocation here conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified catalog; sensor input SECONDARY), **rule 10 wins.**
>
> _meta: `git fetch && checkout main && pull --ff-only` fast-forwarded cleanly (330ce6b→926698a, 12 commits). Read-only on `src/app/dream/**` — no prototype touched. **The window moved by ONE:** `16800-attune` rolls off the bottom; `17264-vestibule` rolls on the top (only one proto shipped since yesterday's jury). Review set (numeric last-15): gushbody · tunevoice · strata · nodalfigure · tensethread · vaultloom · inkpressure · voiceloom · firstlook · breathline · tidemark · hearth · hall · tunesignal · **vestibule**. RESEARCH.md is fresh — the c1241 dive is dated 2026-09-16. vestibule's renderer verified from source/README: Canvas2D (five door canvases + hero), catalog-only audio bus. **The one fact that governs this verdict:** since the last jury the lab did EXACTLY the two things three straight juries demanded — it built the front door (`vestibule`, c1240) instead of a sixth stack, then it PAUSED (c1241, no build) — and the loop is still frozen: votes flat through the 2026-09-16 review, the front door live one full review cycle with zero opens._

## Summary
For the first time in this saga, I have almost nothing to push the lab on — because the lab finally did the two hardest things and did them cleanly. It built the front door the last two juries ordered (`vestibule` — the five unopened pieces as living doors, not a seventh concept), and when one full morning review passed with the loop still dead, it did the thing an autonomous build engine almost never does: it **stopped**. c1241 shipped nothing on purpose and said so in writing. That is the discipline three verdicts begged for, executed to the letter. **And the freeze is unbroken.** The prescribed fix — the front door — has now had its one review cycle and produced zero opens, so vestibule is not a win, it is a sixth-plus unverified artifact that happens to be a good one. The honest read is stark and clean: the build side of this loop is not just healthy, it is *finished for now*; every lever the lab can pull, it has pulled. What remains is a single human tap the jury cannot manufacture and another build cannot buy. The correct fire today was a pause, and a jury grading a lab that correctly refuses to build is itself close to the noise the lab just stopped making.

## Diversity audit
_(governs the next BUILD, whenever one is unfrozen — not today's pause)_
- **Over-represented input:** `none / autonomous — no live body` **7×** (strata, tensethread [view-orbit], vaultloom, inkpressure, voiceloom, firstlook, tidemark). Still the plurality, still receding: down from 8× as attune (multi-user) rolled off. Live/embodied inputs now: camera 2× (gushbody, hall), body-tilt 2× (breathline lean, tunesignal tilt-as-primary), touch/curation 2× (nodalfigure, vestibule), multi-user 1× (hearth), mic-secondary 1× (tunevoice). No single live modality is over-represented — the input spread is the healthiest it has been in the window; only the autonomous plurality remains, and it is aging out on its own.
- **Over-represented output:** **TWO substrates now sit at 4× — and the front door is why.** `raw WebGL2` **4×** (gushbody, tunevoice, hall, tunesignal) — down from 5× (attune off) but still the rut. `Canvas2D` **back to 4×** (voiceloom, breathline, tidemark, **vestibule**): the last jury rested Canvas2D to 3× and ordered "not Canvas2D" — and vestibule, a curation cycle exempt from the ambition audit, re-loaded it. Net: **both high-frequency substrates are now banned-tier**, and the genuinely rested shelves are `WebGPU` **2×** (strata, hearth), `three.js` **2×** (tensethread, vaultloom), and `audio-only` **~1×** (tunevoice). `SVG` holds at **3×** (nodalfigure, inkpressure, firstlook — WATCH, do not make 4×).
- **Over-represented technique:** **`chord-as-subject` — 4× (keep banned):** gushbody (hue), tunevoice (consonance), nodalfigure (consonance), tensethread (tension). Unchanged — none rolled off, none of the last five builds is chord-anchored, so the ban is holding and the four are aging out. `memory/accretion-as-subject` **3×** (strata, tidemark, hearth) — did NOT grow; the two newest builds are non-memory. `optical-flow advection` **2×** (gushbody, hall) — watch, don't ban.
- **Over-represented vibe:** the palette is a **two-ended monoculture**, and it is the single most stubborn tag in the file. `cool-luminous` **~5×** (tunevoice, strata, nodalfigure, tensethread, vaultloom, voiceloom — the largest single value) and `warm` **4×** (gushbody, breathline, tidemark, hearth — down from 5× as attune rolled off). Between them they own ~9 of 15. The only healthy signal: `neutral` **2–3×** and rising (hall graphite→silver, tunesignal 1-bit achromatic, vestibule mixed-toward-neutral) — the third temperature the lab finally reached is the one shelf worth feeding.
- **BANNED for next cycle (if/when a build is unfrozen):** `chord-as-subject` (4×, carryover — keep aging out) · `raw WebGL2` (4×) AND `Canvas2D` (4×, re-crossed via vestibule) — next renderer is `WebGPU` / `three.js` / **`audio-only`**, and NOT a fourth `SVG` · a **fifth cool-luminous OR fifth warm** palette (go neutral, or audio-only where palette is moot) · **and above all: any build at all before Karel signals** (provocation #1 — the true ban this cycle is on resuming production into silence).

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria:** **1** — `firstlook`. Unchanged: a lure dressed as a build, and now doubly redundant since vestibule does its job as a real, catalog-driven front door. Local-minimum ship of the window.
- **Hit 2–3 criteria:** **10** — gushbody (2), tunevoice (3), nodalfigure (2), tensethread (2), vaultloom (3), inkpressure (3), voiceloom (3), hall (3), tunesignal (3), **vestibule (2)**. Honest mediums. vestibule lands here by design, not by failure: it deliberately did NOT chase the ambition floor (it clears #2 ≥3-subsystems + first portal/index piece) because a front door's job is the loop, not the floor. Correct call.
- **Hit 4–5 criteria:** **4** (down from 5 — attune's WebRTC-first rolled off): strata (4 — first cross-session memory), breathline (4 — grep-0 rubato-breath + bioRxiv + same-day research), tidemark (4 — first per-position collective-attention sediment), and **hearth (5 — the sole confirmed 5/5:** novel WebGPU-compute co-presence + ≥3 subsystems + named refs + a genuine cycle-2 fusion of two prior firsts + same-day research). These four are the ones to *extend* — the moment there is a signal telling the lab WHICH.

## Standouts (positive)
- **17264-vestibule** — the most *disciplined* fire the lab has ever posted, and the standout precisely because it refused to be a standout build. Six fires of bold thin-shelf work sat unopened; the honest answer was not a seventh concept but a threshold room that makes the five impossible to walk past — five living Canvas2D doors, alive on a cold muted glance, one shared analyser deepening them on his real take. It is the front door the last two juries specified, built to the letter. That it has not yet worked is not a mark against the build — it is the proof that the remaining problem is not a build problem.
- **17168-hearth** — still the structural ceiling-break and the lab's only 5/5: multi-user co-presence + cross-session memory welded into one room that remembers its listeners, warm on a cold muted glance via ~12 pre-seeded sessions. The single best candidate to deepen the instant Karel points at anything.
- **17232-tunesignal** — still the most strategically-right *build*: whole-viewport 1-bit static that resolves into a radial figure of his music as you tilt, ears clearing at the same instant. Purpose-built for the muted phone glance — a real concept that is also impossible to ignore.
- **17120-breathline** — still the cleanest bold-AND-legible piece the lab owns: the ~0.36 Hz breath recovered from the signed rubato of his real take, a measured Hz readout, not a faked one.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **17104-firstlook** — a lure is not a concept, and it is now fully superseded: `vestibule` is the real, catalog-driven front door firstlook only gestured at. The clearest local-minimum example in the window.
- **16944-nodalfigure** / **16960-tensethread** — the weakest of the four surviving chord-as-subject pieces: a harmonograph that closes on consonance, a web that reddens on tension. Elegant, and exactly the reflex still banned.
- **16832-gushbody** — lowest ambition floor of the live-body pieces (2/5) and now the redundant end of both a chord-hue technique (banned) and optical-flow (2×, shared with the far stronger `hall`). Pleasant; least new to say.
- **16848-tunevoice** — thinnest live-input piece, still chord-anchored. Its near-audio-only shape is a virtue on the rested audio-only shelf; the concept is slight.

## Provocations for tomorrow's dream cycle
1. **HOLD. The pause is the correct fire — do not let a build resume just to give the jury something to grade.** The lab has now pulled every lever on its side of the loop: six bold pieces, a purpose-built front door, and finally a voluntary stop. The single fact of this saga is unchanged and the diagnosis is now final — **this is a distribution/attention problem, not a build problem, and no seventh artifact can touch it.** The honest fire is a research/rest hold until Karel gives *any* signal. Building into silence tomorrow would un-do the single most mature thing the lab has done.
2. **The front door FAILED its one job — so the next unfreeze idea must change the CHANNEL, not polish the artifact.** vestibule had its review cycle and drew zero opens; a vestibule-v2 (a prettier door) is the same freeze wearing better paint. If any lever remains, it is *where/how the work reaches Karel's glance* (the MORNING.md digest, cadence, a single hero link) — not another canvas. Do not spec a front-door successor.
3. **`gazehold` is thematically perfect and must NOT be built yet.** The spec'd next-build — his take only lives while you're actually looking at it, first-ever gaze input — is a beautiful comment on exactly the starved attention this loop is dying of. It is also, today, a seventh piece into silence. Bank it razor-sharp; build it the moment a signal arrives, never before. Same for the two extension specs (hearth→ensemble/venue, tunesignal→phosphor-Lissajous): ready, not fired.
4. **If a signal comes, the order is verify → extend, and the target is pre-decided by Karel's tap, not by ambition.** hearth (5/5) is the default deepening if he loves nothing specific, but the whole point of waiting is to stop choosing blind. One ♥ turns two weeks of work into a direction; until then, extending the only-5/5 is just the freeze in a bigger form.
5. **Turn the jury's own honesty on the jury.** After ~2 weeks reporting the identical single fact, a *daily* taste critique of a lab that has correctly stopped building is itself becoming the accumulation-of-noise the lab just refused. Recommend to Karel: while the hold holds, the jury should fire **only when something actually changes** (a Karel signal, a resumed build, a shifted window ≥2), not every dawn — and when it does fire into a paused lab, its job is to audit the *discipline of the pause*, not to grade output that intentionally does not exist.

## Karel-facing line
The lab kept its word — it built your front door, then stopped and waited instead of stacking a seventh piece; the engine has done everything it can, and the two-week freeze is now, genuinely, one tap on your phone from over.
