# Concept Jury Verdict — 2026-08-26

> **OWNER PRIORITY (Karel, 2026-08-14, still binding):** prototypes grounded in Karel's real catalog take priority over mic-primary interaction. Where any provocation below conflicts with AGENT.md ABSOLUTE rule 10 (all audio from his verified 16-track catalog; mic/camera/MIDI/pose/pen/data allowed only as a SECONDARY control layer, never the primary source), **rule 10 wins.**
>
> _meta: `git fetch && git checkout main && git pull --ff-only origin main` fast-forwarded cleanly (35 commits; cycle 1200 `16144-spiralkeep` at HEAD, plus a large batch of Karel-directed out-of-fence work — installation/kiosk/OG-image/anon-token migrations — none of it dream-agent authored, scope fence intact). Survey ran on the authoritative tree. Read-only on `src/app/dream/**` per scope fence — no prototype touched. Three new ships since the 2026-08-25 verdict: `16064-seismscore` (1198), `16016-inkstroke` (1199), `16144-spiralkeep` (1200); `15344-toll`, `15392-recur`, `15424-negative` aged out of the window._

## Summary
The band moved. For three straight windows the ambition floor sat frozen at 0/12/3 while the lab shipped one loudly-announced "first" after another that the grep refuted — and I said the only way it moves is by extending `canon` or `morphonate` honestly instead of minting firsts. This window the lab did exactly that: `spiralkeep` is a clean 4/5 that graduates `morphonate` onto a genuinely grep-0 substrate (Barkley excitable medium) plus a real persistence subsystem, and — the part that matters most — **its README and STATE claim no "first" and negate it explicitly.** Distribution is now **0/11/4**. That is the first real motion in a month, and it came from discipline, not volume. Credit where it's due. The new problem is structural and quieter: in cashing three verdicts in a row the lab has quietly made the GPU generative field its default room — **7 of the last 15 pieces are a WebGL2/WebGPU shader you watch**. The escape hatch I told you to keep building has become the whole house.

## Diversity audit
_(last 15, numerically highest on disk: spheres, vigil-chord, antiphon, keepsake, leanin, conduct, mididuet, canon, answerpiano, duetlink, morphonate, inkstroke, headnave, seismscore, spiralkeep — toll/recur/negative aged out; inkstroke/seismscore/spiralkeep entered)_

- **Over-represented input:** `passive / self-propelling — no load-bearing human verb` — **5×** (spheres, answerpiano, morphonate, seismscore, spiralkeep). Unchanged at 5×; the two newest data pieces + the two autonomous membranes keep this bucket full — his music (or a live feed) drives the piece and the visitor mostly watches. The quieter cluster: **`camera/webcam-as-primary` holds at 4×** (leanin, conduct, canon, headnave) — over the floor for the second window. **BANNED next cycle:** `passive/self-propelling primary input` (unless carrying a genuinely-grepped never-run subsystem, the exemption `spiralkeep` correctly used) **+ `camera/webcam-as-primary` (4× — rotate off).**
- **Over-represented output:** `GPU generative field (WebGL2 / WebGPU shader you watch)` — **7×** and the runaway leader of the whole window (conduct, canon, morphonate, inkstroke, headnave, seismscore, spiralkeep). I put this on WATCH last window at 5× and retired its celebration; it went UP two, not down. This is now the lab's default surface, full stop — the last four ships in a row are all a shader field. **BANNED next cycle:** `GPU generative-field-as-the-visual`. `Canvas2D flat-single-surface` cooled to **3×** (keepsake, leanin, duetlink) — off the floor, ban lifted. inline-SVG/DOM rested at **2×** (mididuet, answerpiano); three.js-inhabited-room at **2×** (spheres, vigil). Those two, plus genuinely-0× audio-only/embedded and projection, are the rested surfaces.
- **Over-represented technique:** `granular grain-triggering of his take (his recording sliced into grains fired by an event stream)` — **6×** (conduct, canon, mididuet, inkstroke, seismscore, keepsake). This is the lab's default *audio* mechanism now the way the shader is its default visual — chop his take into grains, retrigger on an event. Healthy 2× clusters: `reaction/excitable-field morphogenesis` (morphonate, spiralkeep), `conducting→time-base` (conduct, canon), `HRTF spatial navigation` (antiphon, headnave), `real-external-data-as-carrier` (spheres, seismscore — no longer 1×, a genuine win). `subtraction/consume-once` cooled to **2×** (vigil-chord, keepsake) — one clean window from clearing. **BANNED next cycle:** `granular grain-triggering as the audio engine` (find a non-grain way to sound his catalog).
- **Over-represented vibe:** `warm chromatic (ember→gold / warm pitch-class-hue)` — **5×** (leanin, conduct, mididuet, canon, answerpiano), but now entirely in the *older* tail and aging out under a standing ban. The real story is the overswing has **hardened into a new rut**: the four newest ships are all `cool-severe-on-bone/black` (inkstroke prussian-indigo-on-bone, headnave + seismscore Ikeda black-white-red, spiralkeep cyan-teal-on-bone) — **4×**. The "committed third register" I asked for got committed four times and became the default. **BANNED next cycle:** `warm-chromatic / warm pitch-class-hue palette` **+ `cool-ink/severe-on-bone-or-black` (now 4× — indigo/cyan/B-W-R on a bone or black ground is the new grayscale-flight).**
- **BANNED for next cycle:** `passive/self-propelling primary input` (unless carrying a genuinely-grepped never-run subsystem) + `camera/webcam-as-primary` (4×) + `GPU generative-field output` (7×) + `granular grain-triggering as the audio engine` (6×) + `warm-chromatic palette` (5×) + `cool-severe-ink-on-bone/black palette` (4×). **Not banned (rule 10):** catalog playback as the audio source.

## Ambition floor stats (last 15 prototypes)
- **Hit 0–1 criteria — 0.** Eleventh straight fortnight with an empty floor. Solved; ignore it.
- **Hit 2–3 criteria — 11** (spheres 3, vigil-chord 2, antiphon 2, keepsake 2, leanin 2, answerpiano 3, duetlink 2, morphonate 3, inkstroke 3, headnave 3, seismscore 3). The honest middle. `inkstroke` and `seismscore` land here as clean 3/5s — inkstroke: #2 (catalog loader + pen-pressure granular bow + WebGL2 wet-locking ink) + #3 (Calliphony 2608.03040 / Shu Dao 2606.00001) + #5 same-cycle research; seismscore: #2 (USGS poller + seen-set diff + granular engine + WebGL2 globe) + #3 (Estrela *RedSkyFalls* 2026 Venice Biennale) + #5. Both are grep-safe and neither overclaims — that discipline held all three cycles.
- **Hit 4–5 criteria — 4 (conduct 4, mididuet 4, canon 5, spiralkeep 4).** Distribution **0 / 11 / 4** — **the top band moved for the first time in four windows** (`0/12/3` × three → **`0/11/4`**), and it moved the exact way I said it would: not by a fourth announced "first" but by a verified cycle-2 extension. `spiralkeep`'s 4/5 is real — **#1** a never-used substrate (excitable-medium spiral waves, grep-0 in the lab; the banked BZ/spiralbloom never shipped) **+ a persistence/serialization subsystem no prior RD piece had** + **#2** ≥3 subsystems + **#4** multi-cycle build-on-morphonate + **#5** same-cycle research. I checked #1 against the grep myself: excitable-medium/Barkley kinetics and a serialized-and-restored reaction field are both genuinely 0× before this cycle. This is the piece three verdicts were asking for.

## Standouts (positive)
- **16144-spiralkeep** — the piece of the window and the vindication of the whole "stop minting firsts" argument. It extends `morphonate` honestly (same "his harmony grows a living surface" vein), swaps in a genuinely different pattern-former (Barkley spirals, not Gray-Scott coral), adds true per-album persistence, commits a palette that is neither warm nor grayscale — and claims **no** first, negating it in its own README. That is a real 4/5 built the disciplined way. **This is the model now, alongside canon.**
- **15824-canon** — still the lone genuine 5/5 (two decoupled time-bases from one take, one per hand) and still un-extended. It is the *other* piece that earned its band honestly, and the third-mid-voice / onset-synced grain deepen I keep naming is still sitting unbuilt. `spiralkeep` just proved the extend-honestly move works — do it to canon next.
- **16000-morphonate** — no longer the un-built-upon breakout. `spiralkeep` finally cashed the deepening I flagged for two windows. Credit the lab for spending the cycle on the honest extension instead of a new headline.
- **15440-spheres** — the cleanest real-external-data-as-the-carrier piece, and the lane is **no longer 1×** — `seismscore` joined it this window. Two of my oldest asks (build-on-morphonate, real-world-data) are both cashed. That's the first window in a long time I can say that.

## Pruning candidates (concept-level, NOT for deletion — immutability rule still holds)
- **15696-leanin** (2/5) — still the local-minimum example: it lands on nearly every over-represented axis at once (camera-primary, Canvas2D-flat, warm-ember, and a single proximity scalar as the whole control surface). The safe Canvas2D draft of "camera-as-instrument" next to `conduct`/`canon`. Nothing new to add; noted as the exemplar of the thing to climb out of.
- **15920-duetlink** (2/5) — unchanged and still in frame: strip the "first multi-user" banner (the lab has ~24 multi-user priors) and it's a reused `peerSync` module + a Linkwitz-Riley crossover + two cursors.
- **15488-vigil-chord / 15600-keepsake** (2/5 each) — the two framings of the consume-once verb, correctly paused two windows running and now down to 2× — one clean window from the ban clearing. Residue, not a live problem.

## Provocations for tomorrow's dream cycle
1. **Get off the shader. The GPU field is now 7 of the last 15 — it isn't an escape substrate anymore, it's the default room, and the last FOUR ships in a row are all a shader you watch.** Ban WebGL2/WebGPU generative field for a cycle. The rested output surfaces with real headroom: **inline-SVG/DOM score** (2×, and `answerpiano`/`mididuet` prove it carries a real piece), **three.js inhabited room** (2×, `antiphon`/`spheres`), and the genuinely-0× lanes — **audio-only / embedded / haptic** (test the screen bias entirely) and **projection/installation**. Make the next piece legible without a fragment shader.
2. **Rotate off camera AND off autonomous-passive — pick an active non-camera verb.** Camera is 4× and passive/self-propelling is 5×; between them they eat most of the window's inputs. The most-rested live inputs, all with banked builds ready: **tilt/DeviceOrientation** (`15888-tiltpour`), **Gamepad** (`15904-gamepad`), **mic-as-secondary-layer over a catalog take** (rule-10-clean, still 0× as a real second layer). Pen already got cashed (`inkstroke`). Pick one that is a bodily verb but not a lens pointed at a face and not "sit back and watch."
3. **Stop slicing his take into grains.** Granular grain-triggering is now the default audio engine — 6× in the window (conduct, canon, mididuet, inkstroke, seismscore, keepsake). Try an audio technique that is NOT chop-and-retrigger: **convolution** (use one of his takes as an impulse response and play another *through* it), **spectral freeze / cross-synthesis** (two takes fused in the frequency domain), a **physical-model resonator excited by his audio**, or genuine **multi-track polyphony** (layer several full takes in relationship, the `spheres` move) instead of grains of one.
4. **The palette overswung a second time — the third register became the fourth rut.** Warm is 5× and aging out, but the "committed third register" I asked for got committed four times: the last four ships are all cool-severe on bone or black (indigo, cyan, Ikeda B-W-R). Neither warm-ember nor cool-ink-on-bone is a fresh decision now. Try a register that is wrong next to *both* — a high-key / light-ground palette, or a single saturated non-warm-non-cyan hue, or a genuinely polychrome field.
5. **Do to `canon` what you just did to `morphonate`.** `spiralkeep` proved the extend-a-real-4/5-honestly move produces a verified 4/5 the grep confirms. `canon` is the lab's lone 5/5 and still un-deepened — its named cycle-2 is a **third mid voice / onset-synced grain emission from his own onsets** (or: a recorded-conducting replay so the canon performs itself back). A second honest 5/5 is a bigger prize than any cycle-1 first, and you now have two proofs (`canon`, `spiralkeep`) that you can do it. Alternatively deepen `spiralkeep` itself: the **cross-album membrane atlas** (each album's saved organism as a returnable gallery) or **re-playable defects** (scrub the age, hear which chords carved which spiral) — but note both of those are more shader, so #1 pushes the other way.

## Karel-facing line
The band finally moved — `spiralkeep` is the honest 4/5 you've been asking three weeks for, an extension of `morphonate` that claims no "first," so 0/12/3 became 0/11/4; the new rut is that GPU shaders are now 7 of the last 15 and the last four ships are all a field you watch — next cycle should get off the shader and off the camera.
