# Morning digest — last updated 2026-07-19 (cycle 833, WIDE)

> **You asked in yesterday's brief: "scope a cycle on the AI-pipeline chain?"** — done. Tonight cashes the criterion that's sat **0× across ~27 juries**: the lab's **first ≥2-model in-browser AI chain**, running $0 with no dependency added.

> **Tonight: sing a phrase and the MEANING of your words paints the room.** Your voice → **Whisper-tiny** (speech→text) → **MiniLM** (text→a 384-dim meaning-vector) → that vector becomes the hue, flow and symmetry of a WebGL2 field and the timbre of the sound. It's deterministic — the same phrase always paints the same room — and phrases that *mean* similar things land near each other in colour and timbre. "Brass fanfare" pulls gold; "quiet snow" pale cyan; "red forest" deep red.

**Open this first — desktop Chrome; a mic is best but NOT required (there's a typed-text box):** https://getresonance.vercel.app/dream/1992-semantic-choir — hit **Start singing** (or **Type a phrase**), say something, watch the room re-paint. If you just wait, a ghost cycle of four phrases self-demos the mapping — and silently upgrades to the *real* embeddings once the models finish loading (~40–80 MB, one-time).

## New since yesterday
- **`/dream/1992-semantic-choir`** — *the meaning of what you sing is the brush.* Two real models in series, entirely in your browser, $0, **no package.json change** (Transformers.js via CDN — the same path `1960-depth-well` proved). WebGL2 interference field (**not** the banned log-polar warp); hue **spans the whole wheel** rather than defaulting to violet-cosmic; FM timbre derived from the embedding (**not** the just-intonation stack the jury banned). Ref: Memo Akten *Learning to See*, Refik Anadol. **First ≥2-model AI pipeline in the lab** — the discriminating novelty + recency criteria both real.
- **2 more explored, banked (IDEAS §833):** ⭐⭐ **`invisible-bells`** — a dark room of resonators you find only by **ear** (true HRTF binaural spatial audio; warm light blooms only where sound already is), cashing **SIGGRAPH 2026's "Light Architecture."** ⭐ **`seismic-choir`** — live USGS earthquakes struck as tones on a world map (banked *with a warning*: seismic is a worn lab theme — I'd only revive external-data sonification with a source we've never used).

## Needs your eyes/ears (I ran headless — no mic/WebGL/speakers here)
- Does **Whisper-tiny** reliably hear *sung* words (it's tuned for speech — spoken reads better; sung is the open question)?
- Does the embedding→**hue/timbre** mapping read as genuinely *meaningful*, or arbitrary? The projection axes give real *relative* distances but no absolute "blue = sad" grounding — tell me if it feels connected or random.
- First-load time on the ~40–80 MB model download — acceptable, or does it need a lighter model?

## Research finding worth a look
- **SIGGRAPH 2026 Experience Hall** is live this week (LA). **"Light Architecture"** (kinetic light + spatial audio making invisible algorithmic structure perceptible) seeded the banked `invisible-bells`. And the $0 in-browser multi-model path is now trivial — which is what made tonight's build possible. Full note in RESEARCH.md (§833).

## Open questions for Karel
- **Deepen `semantic-choir` into a 3-model chain?** Add a third stage — audio→text→embedding→**image** (a tiny in-browser text-conditioned generator) — to push from "chain" toward the full audio→image→video frontier. Worth a multi-cycle commitment?
- **Resurrect `invisible-bells` next?** The HRTF "find it by ear" room is the freshest spatial-audio idea in a while and keeps the SIGGRAPH chain alive.
