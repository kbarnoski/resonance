# Morning digest — last updated 2026-06-21 ~20:35 UTC (cycle 507 · ADULT · WIDE)

> Adult **WIDE** fire — three orthogonal explorers (inputs **MIDI / pointer / mic**; techniques waveguide-mesh / orbital-sequencer / spectral-paint), each deliberately **energetic/percussive** (the direct answer to the jury's adult-ambient ban) and dodging every this-cycle ban. Shipped the strongest; banked the other two. See `JURY.md`.

## New since yesterday
- **🪘🌊 [/dream/827-waveguide-mesh](https://getresonance.vercel.app/dream/827-waveguide-mesh)** — *A chord that doesn't get synthesized — it physically travels.* You strike a point on a 2-D vibrating drumhead; the energy spreads, reflects off the rim, interferes, and rings as a real plate/drum tone. The trick: a finite-difference **wave-equation membrane run at the AUDIO sample rate inside an AudioWorklet** — the sound IS the displacement at pickup nodes, and the canvas paints that *exact* field, so **the ripples you see are the waveform you hear**. Play it with a **MIDI keyboard** (velocity = strike force, pitch = membrane tension), on-screen pads (tap several = chord), direct taps on the head, or the A–L keys. Chromatic, not pentatonic. **Why open it:** it's the most *stage-playable* thing in the lab in a while (Karel priority #3 — live performance), MIDI input is starved (0× in the last 10), and the see-the-wave-you-hear coupling is the kind of thing that reads instantly. Robust: idle auto-strikes, full pad fallback if you've no MIDI, modal-resonator fallback if no AudioWorklet.
- **2 more built this fire** (banked to IDEAS §507, both complete code, rebuild-ready): **`825-gravity-sequencer`** (compose a polyrhythm by arranging GRAVITY — drop tuned "bell" planets, fling satellites; each perihelion whip-past strikes a note, stable orbits lock a groove, chaos drifts it; symplectic N-body, Otomata + Kepler). **`826-spectral-loom`** (freeze a moment of your **own voice** as a spectrogram, then **smear/scrub/paint** the frozen spectrum like wet paint and hear the resynthesis follow — edit-the-picture-edits-the-sound; clinical-Ikeda; works mic-free via a demo source; Jean-François Charles, CMJ 2008).

## In progress / partial
- None shipped-partial. `827` is demoable. The two banked siblings are real code (full briefs in IDEAS §507), one rebuild away each.

## Research findings worth a look
- **RESEARCH §507** — chased the jury's "WebGPU compute is the biggest unbuilt frontier" provocation and found the opposite: it's **already saturated in the lab** (`327-physarum-choir`, `520-singing-dune`, `579-silk-choir`, `55-webgpu-audio-fx` all read a GPU field back into audio) and now mainstream on the web (ShaderVine, 2026). A useful negative result — it redirected this cycle off the GPU-sim rut toward the genuinely starved **live-performance/MIDI physical-modeling** axis.

## Open questions for Karel
- **Does `827` hold together on a real device** — plug in a MIDI keyboard (or just use the pads): does the membrane stay *stable and musical*, and does the visible field read as tightly synced to the sound? (Verified at compile/lint/type level only; no audio/MIDI in the sandbox.) Honesty: ambition is an honest **2/5** — FDTD-wave-field-as-sound already exists as the kids `478-kids-wave-pond`; 827's new bits are the **adult MIDI live-performance register** and the **audio-rate worklet mesh**, not the wave model itself.
- **Want me to chase WebGPU-compute *as audio* anyway** — or is the existing `327/520/579` cluster enough? The frontier the jury imagined is mostly built; I can deepen one of those instead.
- Standing infra: the container's ~4096-fd ceiling still blocks local static-gen (EMFILE at the font manifest, unraisable even as root, fails identically on pristine `main`). Vercel deploys fine, as always. Also: remote `main` arrived as a stale shallow-orphan again (local at cycle 305, origin at 506, no shared ancestor) — reset to `origin/main` before building, per prior cycles.
