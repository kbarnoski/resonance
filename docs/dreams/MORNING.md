# Morning digest — updated 2026-07-07 (cycle 697, WIDE)

## Open this first
- **`1273-swarm-choir`** → https://getresonance.vercel.app/dream/1273-swarm-choir
  A swarm of particles where **every dot is also a musical note.** Drag to stir
  the field and steer two knobs (J, K) into the five *swarmalator* states —
  color = phase = pitch, so watching the swarm tune itself into unison is the
  *same event* as hearing it lock into a held chord; push it into the "active
  phase wave" and it shimmers into a moving arpeggio. A real dynamical system
  from the O'Keeffe–Hong–Strogatz equations (Nature Comms 2017 / Physics Reports
  2026 review) — **grep-0× in the lab**, so this is a genuine never-used-technique
  reach, the thing the concept jury keeps saying we decline.

## Why this one (the fire was WIDE — 3 built, 1 shipped)
- Three orthogonal instruments, each answering a *different* unmet jury note; I
  shipped the strongest and banked the other two. The winner cashes three gates
  at once: never-used technique + the banked-0× idea the jury named by name
  (`1208-swarmalator`) + a genuinely fresh (<14-day) research chain.

## Also explored — banked, ready to resurrect (see IDEAS §697)
- **⭐⭐ `1274-faraday`** — a played **Faraday-wave fluid**: drive a liquid and it
  self-organizes into stripes → squares → hexagons → a 12-fold quasicrystal (the
  same geometry as visual hallucinations), each symmetry a chord. Also grep-0×,
  numerically bulletproof, **near-ready to ship next fire.**
- **⭐ `1275-duet`** — a call-and-response **duet with your real Welcome Home
  piano**: you play, it answers with your notes re-pitched into your key. Cashes
  your long-standing "make the piano playable" ask — needs your browser to verify
  the real-audio path (it works standalone on a synth stand-in today).

## Open questions for Karel
- The one genuinely-0× top rung left is an **AI pipeline chain** (audio→image→video,
  2 models in series). It needs a per-prototype paid-API budget I won't spend
  unattended — say the word and I'll build it.
- Housekeeping: cycles **695 & 696 shipped code but skipped their STATE.md entries**
  (I resumed the discipline this cycle). And the container's **file-descriptor
  build ceiling** is still worked around, not fixed — raising `ulimit -n` on the
  runner would let me stop the isolation-build dance.
