# Morning digest — last updated 2026-08-03 ~17:10 UTC (cycle 1004, DEEP fire)

> **Second entry reclaiming the transcendent pole — and it's the un-cashed half of the jury's ask.** The jury said "force a WebGPU-compute *or* genuine 3D-embodied output." Yesterday's `5912` cashed WebGPU-compute; today cashes the *3D-embodied* half — a near-death passage you fly *through*.

## Open this first
- **[5976-luminous](/dream/5976-luminous)** — *Fly, wordlessly, through a near-death passage.* Out of a dark indigo void, up a tunnel of light the camera physically ascends, into an enveloping being of light, to a boundless still radiance, and gently back. No words, no meters, no chrome — a genuine 3D space you move **through**, not a diagram you watch. **Why open:** three.js embodied traversal (additive glow motes + ring "rungs" that ring a bell as you pass, no post-processing); a wordless just-intonation drone over 55 Hz that *adds* overtones as the light nears and lets them go on the return. Degrades to a drone-carried journey if 3D fails.

## How this cycle went
- **DEEP fire, 3 parallel builders → shipped 1.** ONE concept — a wordless long-form NDE passage (void → tunnel → being-of-light → boundary → return, ~4.5 min, real staged state, not a loop) — built three ways: **three.js embodied traversal** (winner), **raw-WebGL2 volumetric raymarch**, **WebGPU-compute particles**. Curated on jury-fit + *spread*.
- **Why the winner, not the WebGPU one:** the WebGPU sibling repeats yesterday's `5912` almost exactly (WebGPU + curl-noise + particles) — shipping it back-to-back is the monoculture the jury warns against. The three.js *passage-through* is a genuinely distinct phenomenology from `5912`'s *fall-into* nebula. Build green (only pre-existing `src/lib` warnings).

## Explored & banked (IDEAS §1004) — both built clean this fire, resurrect-ready
- **`5960-threshold` ⭐⭐** — *NDE as a volume of light.* Raw-WebGL2 volumetric raymarch through a domain-warped fog medium (front-to-back emission/transmittance). The long-banked `5928-lightfold` raymarch lineage, finally built — resurrect-first when raw-WebGL2 is the fresh lane.
- **`5992-crossing` ⭐** — *You ARE the ~240k points of light.* WebGPU-compute curl-noise particle field reorganizing per stage, degrading to a Canvas2D nebula. Banked but flagged: technique overlaps `5912` — resurrect on a real-GPU slot / once WebGPU rests, so it reads as spread not repetition.

## Research finding worth a look (RESEARCH §1004)
- The 2026 immersive-**light**-art moment converges *this June* on the NDE tunnel-to-light: **Turrell's _As Seen Below – The Dome_** (ARoS Aarhus, opened 19 Jun — a corridor into a 40-m boundless light-dome) + **Karolina Halatek's** NDE-testimony practice (*Terminal* 2024, *Echo* Istanbul Jun 2026). The browser-native, no-model form is a staged light-passage you move through — the chain into today's build.

## Open questions for Karel
- **Verify on device:** 5976 wants your phone/browser + speakers — does the forward flight *feel* like an ascent, does the drone's overtone-homecoming land? (Headless can't ear- or GPU-verify.)
- **Spread, not stacking:** the transcendent pole now has 2 entries — next cycle should go WIDE across unrelated lanes, not a third light-piece. Flag if you'd rather I keep deepening this register instead.
- **Standing yes/no (25+ cycles), both need *your* call — I won't fake either autonomously:** (1) the AI-pipeline chain (music→image→video) — fund `FAL_KEY` and build it, or strike it; (2) the two-device-WebRTC / depth-camera installation room — genuinely un-buildable/un-verifiable in this serverless+headless env without a durable signaling store or a new dep. Fund-or-strike?
