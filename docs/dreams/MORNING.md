# Morning digest — last updated 2026-07-16 (cycle 796)

## New since yesterday
- **[1792-replay-svg](https://getresonance.vercel.app/dream/1792-replay-svg)** — *A dream isn't noise made into geometry — it's your cortex closing the sensory gate and replaying the day's own forms, recombined.* Press **Begin**: a sharp "day" of seven warm luminous glyphs and a short melody holds for ~14 s — then, **with no further input**, the sensory gate closes over ~52 s. The veridical scene fades and a top-down pathway replays those exact forms — drifting, superimposed, two-melting-into-one — until the dream is made entirely out of its own memory. Then it gently wakes and loops. The audio does the same: the motif comes back fragmented, transposed, overlapping. No mic, no camera, no clicks — it runs the whole arc itself.
  - *Why this one:* it's the lab's **first piece rendered 100% in SVG vector art** — the "try SVG instead of Canvas2D" your 2026-07-15 jury explicitly named as the empty escape lane — and it lands on the **DREAM/hypnagogic pole** the jury called thin-to-empty. The whole visual is real `<g>/<path>` glyphs; the dream is those *same* glyphs re-used via `<use>` and melted through an SVG filter graph, so the dream is *provably made of* the day. Grounded in a real 2026 paper (Bredenberg et al., eLife 2026) that models the psychedelic visual as the cortex drifting into sleep-phase memory *replay* — a single α "wake→sleep" dial.
  - *Honest note:* one older piece — `1078-dream-replay` — already owns the memory-replay *concept*, but on Canvas2D + touch, as an instrument you play. This is the off-Canvas2D, input-free, self-demoing, dream-pole re-approach on the new SVG substrate, not a first-of-idea. Tuned by eye (no display in the build box) — the melt's frame-rate + feel want your browser.

## Made tonight, DEEP race — 2 more built, not shipped (banked in IDEAS.md §796)
- **⭐⭐ `1794-replay-feedback`** — the SAME wake→sleep dream, rendered on **three.js GPU feedback**: the day scene is snapshotted into a memory texture, then replayed at warped/mirror-folded coordinates with bloom + tracers. The lush, full-GPU version — ship-ready; my pick when a GPU cycle is next.
- **⭐ `1796-replay-ringbuffer`** — the same idea with a **6-frame episodic memory ring-buffer** (discrete remembered snapshots recombined, dream *condensation*). Wants one live driver check before it ships.

## Research finding worth a look
- **The psychedelic/dream visual may not be noise → geometry at all.** Bredenberg et al. (eLife 2026) map the *Wake-Sleep algorithm* onto cortex: one parameter α slides control from bottom-up sensory inference (wake) to top-down generative **replay** (sleep) — so hallucination is your brain replaying *learned memory* with the senses gated off, not pure noise. Tonight's build makes that dial the whole interface. (RESEARCH.md §796.)

## Open questions for Karel
- **The AI-pipeline chain (audio→image→video, ≥2 models)** is still the one genuinely-empty lane — blocked only on your go-ahead for a small per-prototype paid budget (rule #6). One word and it's next.
- **Two jury-endorsed empty lanes are still un-built** and waiting: the **SVG-filter aurora** (§795 `1788-aurora-veil`) and the **eyes-closed HRTF sound bath** (§795 `1790-inner-bath`, the audio-only / screen-bias test). Want either next?
- **Heads-up (not blocking you):** the sandbox still hits a 4096 file-descriptor ceiling that stops a *local* full `npm run build` of the ~1787-route lab (`EMFILE`). Verified environmental (reproduces on the untouched baseline; Vercel builds fine); shipped on a clean full-project typecheck + lint + compile-mode build instead. Nothing for you to do.
