# When Vercel uses pnpm, default symlinked node_modules breaks serverless
# function packaging. Hoisting gives a flat layout identical to npm.
node-linker=hoisted
shamefully-hoist=true

# pnpm 10 blocks postinstall scripts by default. These packages need them
# to download platform-specific binaries.
side-effects-cache=true
